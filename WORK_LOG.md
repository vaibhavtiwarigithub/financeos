# WORK_LOG.md — Active Task Tracker

| US paper fractional shares and 15-name mandate | Codex / GPT-5 | completed | 2026-07-26 | Production confirmed $5,178.58 US paper cash was stranded by whole-share `Math.floor` sizing. Applied the owner-approved US mandate 10 -> 15 with decision-journal audit. Added US-only six-decimal fractional entry/partial-target quantities over existing numeric transactional RPCs; India remains whole-share and live paths are untouched. Focused tests + TypeScript clean. |

| Leveraged-ETF sleeve and execution-session architecture | Codex / GPT-5 | completed | 2026-07-26 | Wrote the US-only, paper-first, default-deny architecture. It isolates leveraged products from the generic ETF path, caps an eventual sleeve at 3% per name / 5% aggregate, requires instrument classification, path-risk/liquidity/correlation gates, separate lineage, and broker-protected live design. Verified existing two-session paper cadence and recorded the US UTC/DST local-window defect. No instrument, mandate, cron, or live-trading change. |

| Symbol-type scoring applicability audit | Codex / GPT-5 | completed | 2026-07-26 | Confirmed active applicability is US equity: fundamental/technical/sentiment/US macro/insider; ADR: no insider; US ETF: technical/sentiment/US macro only; India: fundamental/technical/sentiment only; holdings reuse the matching type before deterministic exit handling. Fixed ETF evidence truth: structurally inapplicable company fundamentals are no longer recorded as successful observations. Identified the remaining implementation-gated gaps: unknown ETFs depend on a static classifier, metal funds inherit generic ETF/macro treatment, and setup-specific ETF/India archetypes remain shadow-only while v1 is actionable. Focused tests + TypeScript clean; no score, signal, provider, mandate, position, cash, or order behavior changed. |

| Instrument registry and type-router shadow | Codex / GPT-5 | completed | 2026-07-26 | Added the default-deny `instrument_registry`, an immutable classification snapshot on new research observations, and Research Journal visibility. Migration `20260726110000` is applied and verified: RLS enabled, no client grants/policies, database denies permission unless explicitly reviewed. No score, eligibility, PaperTrader, live order, or provider-call path changed. Focused tests and TypeScript checks passed. |

| Expanded pipeline and market-local risk audit | Codex / GPT-5 | completed | 2026-07-26 | Production disproved the claimed India exit/closed inconsistency; no unsafe backfill was applied. Fixed misleading LearnerAgent summary: orphan reconciliation and total market-local closed corpus are now distinct. Confirmed market-local stops/targets/thresholds and entry-time exit plans; documented the remaining global sizing-cap defect as an approval-gated per-market migration. Corrected Edge PIT/FDR architecture claims and added a record-only external-skill/GARCH proposal. Focused tests + TypeScript clean; no broker action. |

| Webull, closed-day research, order-maintenance, and ETF-cap adversarial review | Codex / GPT-5 | completed | 2026-07-26 | Confirmed production catch-up crons self-skip correctly and staged rows remain non-executable. Fixed Webull MCP session reuse, incomplete capital-flow refusal, robust market-local earnings-date parsing, order-maintenance race/fill persistence/issue resolution, bounded closed-session behavior, owner reconciliation refresh, ETF-cap fail-open/current-cost valuation defects, and added current ETF examples. All focused tests, TypeScript, full test suite, production build, JSON parse, and docs/map update pending final validation. No broker call or order was made. |

| Capital-rotation P0 readiness completion | Codex / GPT-5 | completed | 2026-07-22 | Completed measure-only readiness: canonical exit-plan ownership, fresh-source and self-rotation refusal, full candidate-to-book measured correlation, post-swap constructor replay, distinct-run persistence, same-market turnover, friction/tax/economic blockers, and Agents -> Rotation visibility. Applied service-only revision-collapsing return RPC; all execution flags and the DB off-constraint remain intact. Gates: 1,243 passed/6 skipped, TypeScript and production build clean. |

| Exit-policy handoff audit and regression fixes | Codex / GPT-5 | completed | 2026-07-22 | Verified ETF cap is applied before champion/challenger persistence and India macro contracts now match pipeline reality. Fixed duplicated ETF cap ownership, fail-open supersession writes, stale macro parity expectation, a stale India holdings test, and UTF-8 BOM that broke system-map JSON parsing. Added `features/atr-paper-exit-policy/FEATURE_ARCHITECTURE.md`: fixed ATR and entry-score-drop exits are not approved because they would duplicate the transactional exit policy and bypass validation; proposed next step is measure-only, market-local ATR evidence. Gates: 69 focused tests passed and production build clean. |

| PositionMonitor run-timing telemetry | Codex / GPT-5 | completed | 2026-07-22 | Production verification found India operationally healthy on Jul 20/21: both cron transports succeeded, no failed runs/alerts, Jul 21 checked 10, closed KPEL.NS at its stop, updated 9, unpriced 0, stale-score holds 0. Fixed the only defect found: agent_runs completion preceded its insert-default start by milliseconds. One request-start timestamp now persists on success, empty-book, and error rows. Telemetry only; no exit, price, position, cash, or gate behavior. Gates: 1,211 tests passed/6 skipped and 52-page production build clean. |

| Research Journal filtering and market-local clocks | Codex / GPT-5 | completed | 2026-07-21 | Added explicit Score Tracker clear/select/add-filtered controls over the full bounded market candidate set, including per-market candidate/selection isolation; added a 250-row-bounded all-date view and exact server-side symbol filtering to Daily Funnel; added current ET/IST to each market status pill while retaining open/close schedule text. Live UI proof: 29 high-score matches selectable, clear returns empty chart, AVGO shows 7 independently expandable dated observations; desktop and 390px mobile clean. Read-only UI/query changes; no scoring, provider, trading, or cross-market behavior. Gates: 1,211 tests passed/6 skipped and 52-page production build clean. |

| Paper Trades exit-plan column | Codex / GPT-5 | completed | 2026-07-21 | Reused the position-level exit projection in the Trades table exactly between Score and Realized P&L. Open BUY lots receive the same-market current plan; completed or non-held trades cannot inherit a newer position's plan. Desktop/mobile UI verified. No new reads, writes, providers, schema, or trading authority. Gates: 1,206 tests passed/6 skipped; TypeScript and 52-page production build clean. |

| Canonical live-account names in Risk Analytics | Codex / GPT-5 | completed | 2026-07-21 | Reused latest broker snapshot nicknames across live and daily risk surfaces, with broker + masked-ID fallback. Existing historical risk runs are enriched read-only; future captures persist the canonical label. Verified Joint, Managed, Default, Agentic, Trading, and Autopilot labels in Risk Analytics, with zero full account IDs rendered; Live Portfolio uses the same contract and no longer carries a misleading one-account badge. Desktop/mobile UI verified. No schema changes, broker writes, or risk-decision changes. Gates: 1,206 tests passed/6 skipped; TypeScript and 52-page production build clean. |

| Paper Portfolio exit-plan visibility | Codex / GPT-5 | completed | 2026-07-21 | Added a read-only Exit Plan section to every open paper position: current persisted trailing stop, optional remaining target, latest same-market validated deterministic score with freshness and exit threshold, and the effective entry/user/learner time horizon. Projection preserves PositionMonitor precedence and stale/missing-score abstention; server reads are batched once per active market. Production read proof found complete stop/target/horizon/current-score inputs for all 19 positions (10 US / 9 India). No schema, provider, broker, order, score, threshold, position, cash, or trading-authority change. Gates: 1,201 tests passed/6 skipped, TypeScript and 52-page production build clean. |

| Edge calibration readiness monitor | Codex / GPT-5 | completed | 2026-07-21 | Added deterministic US/India per-factor/horizon readiness over immutable EdgeIC history. Same-window provider revisions collapse; only windows at least 5 days apart count. Six stable historical windows request PIT/walk-forward/cost/FDR validation; four qualified validation windows request shadow review. Daily provider-free monitor updates the Edge dashboard, batches one-time notices, and warns after 10 stale days. RLS deny-by-default projection and cron migrations applied/verified. No scoring, signal, mandate, lifecycle, position, cash, provider, broker, or order authority. Gates: 1,192 tests passed/6 skipped, TypeScript and production build clean. |

| Technical factor calibration shadow (Vibe P0/P1) | Codex / GPT-5 | completed | 2026-07-21 | Built a reusable measure-only calibration path inside the native Edge lab: exact current technical composite, ATR-normalized MACD(12/26/9), signed ADX(14), existing relative-strength/momentum factors, and market/sector IC slices for US+India. Every result appends with formula, dataset, segment, configuration, and run fingerprints; exact reruns are idempotent, historical rows are immutable, and cleanup no longer deletes experiment lineage. Pinned audited Vibe commit `a5eb30fd` as a non-admitted future comparator; no external runtime was imported. Migration `technical_factor_calibration_segments` applied and verified with all 72 legacy rows preserved, RLS deny-by-default, indexes, and append-only trigger. No score, weight, signal, mandate, position, cash, provider quota, broker, or order influence. Gates: 1,185 tests passed/6 skipped, TypeScript and production build clean. |

| Research freshness + concentration-advice correction in live-risk surfaces | Codex / GPT-5 | completed | 2026-07-21 | Added latest same-market canonical ResearchAgent score/direction/exact timestamp/session age/scope to each US/India live holding in newsletters as a display-only join. Corrected `hr-v3`: global name/sector/correlation references are review-only for every account, asset-class sleeves do not enter equity-sector caps, no simulated sell quantity is exposed, and the shared app/newsletter read boundary downgrades legacy hr-v1/v2 trims while preserving source posture for audit. Protective stop/thesis `exit_review` remains first; no provider, order, schema, or money-path change. Gates: 1,178 tests pass/6 skip, tsc and production build clean. |

| Live holding risk in pipeline newsletters | Codex / GPT-5 | completed | 2026-07-21 | US/India briefings now replay the latest complete deterministic Holding Risk run for every live broker account, separated by account/market/currency. The email shows every attention posture plus the three highest-risk ordinary holds, current price/P&L/NAV weight, account and holding scores, reasons, freshness, confidence, missing inputs, omitted-count truth, and a full Risk Analytics link. Excluded the `internal` paper adapter, masked embedded account IDs, rejected row-scope mismatches, preserved null as unavailable (never zero), and sanitized risk context before LLM prose. No risk recompute, provider call, schema, order, or money-path change. Production data proof: six US Robinhood accounts selected; India Kite selected and truthfully stale. Gates: 1,171 tests passed/6 skipped, TypeScript clean, 52-page production build clean, GitGuardian clean. |

| Paper trading cadence and US utilization audit | Codex / GPT-5 | completed | 2026-07-21 | Production diagnosis: US was healthy but name-cap constrained (10/10 names, $3,635 cash, 4 historical exits); India had 9 names and 14 exits, so it created/refilled slots more often. US held HOOD 54 and AXP 55 because both remained above the configured 45 exit threshold; no sell gate was stuck. Fixed a real cadence asymmetry: removed ResearchAgent's duplicate PaperTrader tail-call (which filled US pre-open), moved the sole standalone jobs to in-session DST-safe times (US 15:15 UTC, India 04:10 UTC), added a market-session/holiday refusal in the route, and persisted RPC denial reason counts to pipeline events and `agent_runs.workload_metrics`. No threshold, 10-name cap, hysteresis, position, cash, rotation flag, or live path changed. Migration `align_paper_trader_schedule` applied and verified; India off-session smoke returned HTTP 200/no-op with cash and 9 positions unchanged. Gates: TypeScript clean; 1,165 tests passed / 6 skipped; production build and GitGuardian clean; Vercel Ready. Commit `5db70483`. |

| Router evidence hardening: cache-only cohorts + safety/quality gates | Codex / GPT-5 | completed | 2026-07-21 | Shipped cache-only cohort evaluation, a no-fetch Kairos compatibility adapter fed by ResearchAgent's already-acquired evidence, one macro lookup per market, separate safety/quality gates, validated market-session dating, a rolling 10-session activation proof, and daily market-local cohort crons. Applied production migrations `router_evidence_hardening` and `router_cohort_session_source`; Vercel production Ready. Live smoke proof: US evaluation `a2226bb7` (session 2026-07-20) and India `78770c01` (session 2026-07-21) both persisted against their exact v2 candidate and v1 baseline with primary calls 0, reverse calls 0, and ledger proof true. Both correctly fail current parity while compatibility-cache coverage accrues. Active US/India policies remain v1 with `router_enabled=false`; activation RPC remains service-role-only. Security advisor found no Router issue. Gates: TypeScript clean; 1,162 tests passed / 6 skipped; Next production build clean. Commits `866548b3`, `cac4cffa`. |

| System Health action/noise separation + India accuracy-breaker correction | Codex / GPT-5 | completed | 2026-07-20 | Shipped action-vs-notice separation, removed Theme Scout activity from the health funnel, and aligned the accuracy breaker to 20 directional outcomes with breakevens excluded. Corrected India safety truth was 4W/7L/2BE, below the sample floor, so the stale latch was audited and re-enabled without changing thresholds. Production now has 1 action (Kite re-login) and 5 informational notices. Commit `7917419c`; Vercel production Ready. |

| Guarded kill-switch reset + open paper-trade P&L | Codex / GPT-5 | completed | 2026-07-20 | Shipped owner-only, book-aware reset of the actual market-control latch with pause refusal, fail-closed risk reads, originating-book recheck, audit-before-enable, and checked control writes. Paper Portfolio/Trading now show signed persisted Current P&L beside Realized P&L without provider calls or currency mixing; desktop/mobile overflow verified. Live holdings already expose unrealized P&L; a unified live trade view remains gated on reconciled `broker_orders`. Gates: 1,159 passed/6 skipped, production build and GitGuardian clean. Commit `73a03e38`; Vercel production Ready. |

| Research Journal position/outcome parity + dormant live-exit hardening | Codex / GPT-5 | completed | 2026-07-20 | Added separate immutable research-plan, current paper/live position, exact-signal paper outcome, and same-market/horizon MAE-MFE readiness truth to Research Journal without provider calls. Live snapshots are active-account scoped; unlinked/mismatched holdings are explicitly unmanaged and receive no invented protection. Hardened dormant live autonomy: strict mandate reads, active-account NAV/caps/order lineage, proposal-time deterministic exit policy before submit, FIFO remaining-lot basis, fill-policy rebound, market-session time stops, and fail-closed DB/idempotency checks. Fixed prior cross-account open/order counting, hardcoded US NAV account, partial-sell cost-basis drift, hardcoded live stops/targets/time stop, and missing India proposal account. All live/Webull/protective flags remain off; no broker call, canary, migration, threshold, mandate, account, or Supabase setting changed. Gates: TypeScript clean; 1,145 tests passed/6 skipped; 52-page production build clean; diff check clean. |

| Research Journal terminal-state truthfulness | Codex / GPT-5 | completed | 2026-07-20 | Fixed completed research/portfolio stages being mislabeled as pending at the stage they had already passed. Added explicit staged, revalidated/superseded, expired/not-traded, awaiting-paper-gates, awaiting-execution, and audit-gap states through a pure resolver. Production AVGO verification: 2026-07-19 was weekend research score 70/long and was subsequently revalidated/superseded; 2026-07-20 score 53/short was correctly rejected below threshold 60. Immutable history unchanged. Gates: 1,134 tests passed / 6 skipped, TypeScript and production build clean. |

| Research-time indicative trade plans | Codex / GPT-5 | completed | 2026-07-20 | Added deterministic native-currency research reference/risk/target plans for future observations and Research Journal, with stale/missing/held/rejected states shown honestly. Research now records the actual scoring-candle close without extra API calls. PaperTrader re-prices from the actual fill and now correctly applies the approved MAE/MFE policy only after 60 same-market/horizon eligible-long outcomes; thin/malformed/custom-horizon samples fall back to the current mandate. Fixed the prior rejected/neutral/short sample contamination and user-vs-champion horizon mismatch. PositionMonitor/live broker authority unchanged. Production-data audit confirmed no cohort currently meets 60, so current fill behavior remains mandate-based. Gates: 1,141 tests passed / 6 skipped, TypeScript and 52-page production build clean; no migration required. |

| US/India agent-source pipeline remediation | Codex / GPT-5 | completed | 2026-07-20 | Reviewed and strengthened the plan, then implemented it end to end. PositionMonitor now uses market-local bounded quotes, rejects stale US/India prices, reports every unevaluated holding, continues valid exits, and applies conservative paper sell slippage. Research discovery is market-scoped before external work; routine unconsumed options calls are removed; evidence records carry the provider that actually served each dimension. Theme Scout is independent weekly, GDELT-first, Alpha Vantage reserve-only, and still discovery-only. India quality applicability is fundamental/technical/sentiment (not US macro); Learner no longer mutates open positions and does not consume US macro for India; dormant autonomous-live uses per-market mandates, calibration, and pricing while all activation flags remain false. Holding Risk shows canonical validated research as separate advisory context without changing deterministic posture. India Scanner exposes fresh rotating coverage and truthful India providers in API/UI. Added architecture, cron/agent docs, diagram, two production migrations, and focused regression tests. Production verified: migrations recorded as 20260720124528/20260720124539, Theme Scout cron active at `0 0 * * 1`, `v_decision_quality` retains `security_invoker=true`, recent India rows use the corrected dimensions. Gates: TypeScript clean; 1,130 tests passed / 6 skipped; Next production build clean; `git diff --check` clean; Supabase advisor counts unchanged from baseline. Router/Vibe/autonomous-live remain disabled; no weights, thresholds, mandates, accounts, kill switches, or broker orders changed. |

| India System Health and paper performance truth remediation | Codex / GPT-5 | completed | 2026-07-20 | Verified live India pipeline and retained the legitimate accuracy kill-switch. Fixed both paper-performance writers to share canonical per-market seed/P&L/alpha/outcome derivation, fail loudly on truth reads/writes, repaired all historical rows in production, restored Learning/Mentor curves by removing a nonexistent column read, made US-only macro structurally inapplicable to India, and collapsed evidence-degradation Health alerts to one current condition per market. Cleared false/recovered alerts; India remained trading-disabled. Production repair verified: India NAV ₹997,498.38, total return -0.2502%, 4W/7L/2BE. Gates: 1,118 tests passed/6 skipped, typecheck and production build clean. |

| DeepSeek V4 retirement cutover | Codex / GPT-5 | completed | 2026-07-20 | Replaced legacy model aliases before their official 2026-07-24 retirement. Migrated all 16 DeepSeek flows plus vault metadata to concrete V4 IDs; Flash is explicitly non-thinking, Pro explicitly thinking/high, and Pro tool loops replay required reasoning state. Updated conservative official pricing, fallbacks, tests, and architecture docs; cleared 16 resulting Health criticals after verifying zero legacy assignments. Production migration `20260720114315` applied. Gates: 1,121 tests passed/6 skipped, typecheck and production build clean. |

| Weekend research catch-up + agent capacity dashboard | Codex / GPT-5 | completed | 2026-07-19 | Added Saturday/Sunday per-market catch-up over only the carry-forward queue; writes non-executable `weekend_staged` signals and keeps candidates queued for fresh session revalidation. PaperTrader, TraderAgent, and PositionMonitor conviction exits positively require `session_validated=true`; mechanical exits remain independent. Added Agents → Capacity with unprepared/staged work, observed median throughput, ceilings, age, and honest clear-time estimates. Prod migration + Vercel deploy verified. Live Sunday proof: US 26/26 and India 4/4 staged against 2026-07-17, all unvalidated; queues retained for Monday; zero PaperTrader runs, paper trades, trade proposals, or unvalidated-pending leaks. Gates: 1,101 tests pass/6 skip, tsc clean, 52-page production build clean, GitGuardian clean. |

| 2026-07-18 edge evidence collection correctness + automation | Codex / GPT-5 | completed | 2026-07-18 | Fixed dormant US-only evidence collection. Prod migration `20260718160531` applied+verified: frozen fingerprinted signal revisions, honest observed/provenance metadata (747 signals + 778 input rows retained as `legacy_unverified`), IC N/universe/date/provider metadata, market-scoped lifecycle state, and 4 bounded US/India crons. Fixed catalog status resets, cross-market overwrite, Newey-West sampled lag, thin N=12 gate (now 36), owner auth, stale-check UTC rollover, health alerts, and per-market dashboard freshness/N. Still measure-only; costs remain null and block capital phases. Gates: tsc clean, 1092 passed/6 skipped, Next build clean, GitGuardian clean; prod trading controls unchanged. |
| 2026-07-18 protective-stop, Webull adapter, Router cohort adversarial audit | Codex / GPT-5 | completed | 2026-07-18 | Reviewed `9a63995e..10f07728` across all 10 requested categories and fixed every confirmed defect. Removed the caller-enabled Webull network transport; aligned offline signing/body/endpoints with official OpenAPI; made pause/kill/breaker/token/account/SELL-quantity gates fail closed; kept account `605420660` as the only permitted order account; prevented false fills and invalid numeric protective states; runtime-rejected legacy disaster-floor mode; added an UNAPPLIED schema-hardening migration; made Router cohort thresholds market-configured, cohorts strategy-coherent/deduplicated, GET cron-only, and ledger/replay proof mandatory. RLS was owner-read/service-write; corrective migration revokes unnecessary sequence/function access. Timer fix has scoped real-timer restoration. Prod rechecked read-only: both order flags false; Router false US/India. No broker call, flag change, Router activation, or prod migration. Gates: tsc clean, 1085 tests passed/6 skipped, Next build clean. |
| Hybrid-stop SHADOW SCAFFOLD — built UP TO the placement line, NO live order ever | Claude / Opus 4.8 | completed | 2026-07-18 | **Money path — so built everything up to the placement line and STOPPED. No real broker order, not a $1 test; a false-by-default flag gates all placement and stays false.** Prod verified FIRST (`dionkikgdmlaotvtbnfr`): no `protective_orders`/gtt/disaster table exists; `broker_orders` already carries `kite_gtt_id`; no `learning_scope` column anywhere; `paper_trades` exit reasons in use are `direction_flip`/`score_exit` (no `protective_disaster_floor`). **Built (all pure, deterministic, NO LLM on the money path), `lib/protective/`:** (1) `capabilities.ts` — the broker-neutral `BrokerProtectiveCapabilities` matrix + `evaluateProtection()`, capability-driven by order-type/TIF/session/account (a flat broker boolean is forbidden); GTC ≠ triggers-in-every-session (session eligibility is separate from TIF persistence); an adapter with no eligible MULTI-DAY order for the exact position is `unprotected-by-broker`, never silently protected. (2) `kite-capabilities.ts` — Kite's capability DECLARED from the proven GTT code (`placeKiteGtt`/`buildKiteGttBody`): Kite protects via the WEAKER `gtt_limit` (LIMIT child, unfilled-trigger risk surfaced in caveats), and its DAY-only regular SL-M is declared honestly and REJECTED as a multi-day floor. (3) `disaster-floor.ts` — pure `computeDisasterFloor({mode, distance, …})`; Q1 unanswered so `mode` DEFAULTS to `wider_disaster_floor` (outage + catastrophic-loss mitigation, NOT touch-at-analytical-stop), distance is a CONFIG input with no hardcoded value, monotonic ratchet so a falling high-water mark can never lower the floor, optional hard max-loss bound can only raise it. (4) `reconcile.ts` — pure `reconcileProtectiveOrder()` detecting out-of-band triggers, partial fills, cancels, expiry, broker edits, and corporate-action qty drift; unknown state → `needs_reconcile` (never assumed active/canceled), a trigger WITHOUT a confirmed fill never closes the book, a gap through a limit child is reported unprotected (not filled), expiry is critical, residual protection clamped to reconciled held qty. (5) `state.ts` — the `protective_order` record shape, status machine (`placing`→…→`needs_reconcile`), exit provenance, and long-only / cancel-before-replace invariants. (6) `placement-gate.ts` — **THE MONEY LINE**: false-by-default `strategy_config.protective_orders_enabled` gates ALL placement and stays false; `planProtectivePlacement()` returns the intended action as a plain object and NEVER calls a broker. **Exit provenance (Codex's correction):** a disaster-floor fill records `exit_reason=protective_disaster_floor` + `learning_scope=risk_policy_only` — the loss STAYS in P&L/NAV/drawdown/mandate/risk-policy evaluation (real money) and is excluded ONLY from the Learner's signal-weight attribution + genome promotion (`includedInSignalWeightLearning()` false, `includedInPnlAndRisk()` true); a generic `excluded_from_learning=true` is insufficient because the evaluation engine also filters that field. **Long-only:** total executable SELL (resting protective + competing) never exceeds reconciled held qty; a competing SELL is blocked until a resting cancellation is CONFIRMED (fail-closed, no accidental short) — the same rule the shipped standalone Kite route enforces. **Per-market:** US/India never cross (capability scope rejects the other market). **Migration is a PROPOSAL, NOT APPLIED:** `supabase/migrations/20260718000000_protective_orders_shadow.sql` (creates `protective_orders` with a `protected_qty <= reconciled_held_qty` CHECK + one-active-per-position partial unique index, append-only `protective_order_events`; adds the false flag + `learning_scope` on `paper_trades`/`broker_orders`). `docs/arch/04` intentionally NOT updated until it runs. **Tests:** `tests/protective-hybrid-stop.test.ts` — 32 tests implementing all 14 spec acceptance tests, each falsifiable; **mutation-verified** (broke the ratchet `max()` → AT3 fails; broke the cancel-guard → AT1 fails; reverted, all green). **Gates: tsc clean, build clean, vitest 973 passed / 6 skipped** (941 baseline + 32 new). **Pre-existing UNRELATED baseline failure flagged, not mine:** `tests/kill-and-exit.test.ts` "live drawdown trip" expects `tripped='drawdown'` but a −50% snapshot trips `daily_loss` first — fails on pristine main before any of my changes; out of hybrid-stop scope. Docs: arch-08 (header + "Hybrid protective-stop shadow scaffold" section), `system-map.json` (history #77 — deliberately NO node/edge because the scaffold places nothing; drawing one would imply an active flow that doesn't exist; the real EXITS→KITE/REALORDER protective edge lands when the owner approves + wires live), `features/hybrid-stop/FEATURE_ARCHITECTURE.md` (shadow-scaffold status section). **What remains for the owner before any live dollar:** approve Q1 (broker-hosted touch at all), the floor distance (rec: mandate-ATR + hard max-loss bound), the ratchet step/cadence, and Kite-first vs Webull-first; then apply the migration, wire the plan to a reviewed live path, flip the flag, and run one owner-approved small live test per market. Branch `worktree-agent-aea2477b20b3104de`, **NOT pushed**. |
| Signed webull_trade REST adapter (fixtures only, no live/sandbox call) | Claude (Anthropic) | completed | 2026-07-18 | Built `lib/brokers/webull-trade/*`: HMAC-SHA1 request signing with golden fixtures (mutated method/path/body/query/nonce/timestamp fail); vault-only credential accessor (`webull_trade` provider) with bounded cache + sandbox/prod separation and host pinning (a sandbox credential can never resolve a prod host); stable `client_order_id` (≤32); order lifecycle (place/query/cancel/reconcile) where timeout or missing order id → `needs_reconcile`, never success, never blind resubmit; full 9-gate ladder (all fail closed); token lifecycle NORMAL/INVALID(15 idle days)/EXPIRED(5-min verify) fail-closed; order-type scope LOCKED to market+limit+GTC STOP_LOSS in CORE (SHORT/options/algos/OCO/trailing rejected at the adapter boundary); `BrokerProtectiveCapabilities` declaration against the hybrid-stop spec shape. Deleted the inert MCP-based `lib/brokers/adapters/webull.ts` order scaffold and removed its registry entry; read-only Cloud MCP registry (`webull`) untouched; registered `webull_trade`. Migration FILE `20260718120000_webull_trade_orders_enabled.sql` written but NOT applied (false-by-default flag; column absent in prod → adapter fails closed). Verified prod first: no `webull` row in `broker_accounts`, vault has only `webull_mcp` provider rows (no `webull_trade`), and the flag column does not exist. 97 new tests across 6 files cover the spec's Failure and Security Tests 1-14; the SHORT guard and the body-sensitivity of the signature were confirmed **falsifiable** by deliberate mutation (removing each makes its test fail). Also updated `docs/arch/02-tech-stack.md` and `public/agent-diagrams/system-map.json` (GATEWAY node + history entry; no agent-to-agent edge changed so the diagram is unchanged). Gates: `tsc` clean, `next build` clean, `vitest` 1038 passed / 6 skipped / 0 failed. NO live order, NO live/sandbox network call — needs owner entitlement confirmation + a manually-approved sandbox test before any live line. |

| Router cohort builder — first parity evaluations exist (SHADOW ONLY) | Claude / Opus 4.8 | completed | 2026-07-18 | **`evidence_policy_evaluations` had 0 rows.** The router-cutover prerequisites all shipped (intent classification, degradation guard, parity comparator, bound activation RPC) but the piece that FEEDS the comparator was deliberately left unbuilt — "a builder that fetches is the step that risks provider bursts." Built it. **PROD VERIFIED BEFORE AND AFTER: both markets `router_enabled=false`, active policy v1, zero live effect on scoring/signals/sizing/orders. No migration applied — every column `persistEvaluation` writes already existed.** **THE GOVERNING CONSTRAINT (§4.2) — and the bug it caught in my own first draft:** a dual-run must not double provider calls. My first implementation resolved TWICE (primary + reverse) and its own ledger proof **failed it — the reverse pass made 12 real bursts**, exactly the defect the constraint exists to prevent. Fixed structurally, not by tuning: `assembleCohort` is **pure and takes the frozen snapshot as an argument**, so the reverse-shadow leg has no resolver and no network — its only path to evidence is the frozen `Map`. Reuse is now a property a refactor cannot quietly undo, plus `verifyReverseShadowReuse` asserts every `(symbol,intent)` was served from the frozen set rather than re-resolved. **LEDGER PROOF (`provider_call_ledger`, by run_id):** US primary 50 rows / 40 fresh / **10 bursts**; US reverse **0 rows, 0 bursts** while serving all **75** pairs from the frozen set. India primary 12 rows / 6 fresh / 6 bursts; India reverse **0 rows, 0 bursts**, all **60** pairs served. Dual-run cost therefore **equals** the single primary pass; the primary's bursts are the explicit cache misses the constraint allows. A `denied` lease is deliberately NOT counted as a burst — that is the pacing system refusing a call, i.e. the protection working. Reuses the EXISTING `resolveEvidence` + `evidence_cache_v2` shared with the `evidence-shadow` harness, so a warm cache short-circuits at zero cost. **LEGS — legacy is the RECORDED decision, not a guess:** `agent_signals` joined to `research_packets.raw_data` supplies the exact persisted availability mask (`_data_quality`) and the weights that actually drove the score (`_profile_weights`); the mask is rebuilt with research-agent's own rule. **The legacy leg reproduces the recorded `analyst_score` EXACTLY — 27/27 symbols across both runs** (CELH 50=50, MELI 76=76, BBAI 28=28 …), which is what makes it a baseline rather than a reconstruction. Candidate = the SAME frozen dimension scores with availability decided by the router snapshot. Uses the **production** scorer + **production** direction gate via `evaluateCohort` — no re-implementation. **FIRST EVALUATIONS (real, persisted): US `4135ff48` — 15 symbols, PASSED all machine gates**, 0 new-eligible / 0 new-ineligible, 0 hard semantic failures. Required fields at parity (`technical.daily_bars` 15/15 vs 15/15; `fundamental.reported_core` 15/15 vs 15/15). Router **GAINED** insider coverage 7→10 (+20%) — classified `availability_gain`, measured not approved (§5). Lost `macro.regime` (−100%) and `sentiment.news_tone` (−86.7%) — neither intent has a registered adapter yet and neither is a required field, so no coverage failure. **VERIFIED RATHER THAN INFERRED:** scores moved materially (deltas −28 to +5, e.g. BBAI 28→0, SOUN 33→13, ASML 63→68) yet **no name crossed the 60 threshold** — I queried all 15 rows rather than trusting the zero-flip count, because a big availability change with zero flips is exactly the kind of result that is usually a bug. **India `160e850a` — 12 symbols, correctly FAILED:** `coverage_below_non_inferiority_margin` on `technical.daily_bars`, candidate **0.0%** vs legacy 100.0%. Root cause confirmed in the registry: **every `price.daily_bars` adapter is `markets: ["us"]`**, so the router cannot serve India's required field at all. That is the gate working, not a defect — US results provide no evidence for India (§7) and the two were evaluated independently. **v1 SCOPE STATED HONESTLY:** legacy never persisted raw field values or per-field provenance, so value/basis/period parity against it would be **fabricated**. Both legs therefore carry canonical contract semantics, `value`/`periodEnd` are null (not compared), and the real serving provider is recorded for audit. Consequence stated plainly rather than hidden: because both values are null, `compareField` short-circuits at the nullability axis, so **basis/period/currency/unit are not exercised on the production path in v1** — they are exercised in tests and go live when the legacy path is itself routed. Dimension scores are NOT re-derived from router evidence (no scorer consumes `EvidenceEnvelope`; inventing one would make the evaluation stop predicting the thing it claims to predict). Same honesty pattern as the degradation guard's `ageSeconds: null`. **TESTS: +8** (`lib/evidence/evaluation/cohort-builder.test.ts`) — a dual-run reuses ONE frozen set (dedup: 3 unique keys → 3 fetches; reverse → **0** new), `assembleCohort` never resolves, parity checks semantic axes BEFORE values (a TTM-vs-QUARTERLY basis mismatch hard-fails **even with identical numeric values on both sides** — a value-first comparator would call them equal), abstained/failed rows are RETAINED as first-class, the legacy leg reproduces prod exactly, and — the key safety scenario — **a new long created only by renormalizing around a dropped unrouted dimension is BLOCKED** as `artifact_created_eligibility`. One test initially failed and taught me a real property (null values short-circuit before the basis axis); I fixed the test to exercise the axis honestly rather than weakening the assertion. **GATES: tsc clean · build clean (52/52 pages, new route registered) · vitest 950 passed / 6 skipped** (942 baseline + 8 new, **0 regressions**); `tests/arch-diagram-drift.test.ts` 20/20 after the map edit. **DOCS:** `docs/arch/04` (new "who writes them" section + the **missing `20260716210000` migration row**, confirmed APPLIED in prod via `information_schema` — a pre-existing gap in the table), `system-map.json` (new `EVIDCOHORT` node + **dotted** edge from RESEARCH terminating at the evaluation store, never at a scorer; history #77), `features/router-cutover/FEATURE_ARCHITECTURE.md` §15. **NOT DONE, deliberately:** no activation (owner-gated RPC, out of scope); §7 numeric floors/tolerances/TTL remain owner decisions — `coverageNonInferiorityMargin` 0.05 and `maxAdverseRankDisplacement` 5 are **placeholders, not approved thresholds**; §7 outage drills still unbuilt. **KNOWN APPROXIMATION (reported, not hidden):** `shapeOf` maps every non-ETF/non-metal symbol to `equity`, so a US ADR (TSM, STM, ASML in this cohort) over-includes the insider field — conservative (a coverage miss, never a fabricated eligibility) but an approximation, not a modelled distinction. Branch `worktree-agent-a79783b0e5fc4e8a3`, **not pushed**. |
| Full 2026-07-17 adversarial review and confirmed fixes | Codex | completed | 2026-07-17 | Reviewed the full handoff plus US/India scoring, research capacity, watchlist/Theme Scout growth, paper exits, live risk boundaries, Canonical Evidence Router naming, Kite GTT safety, and Webull transport claims. Fixed market-partitioned/research-enabled watchlists, holding provenance, per-market score history, per-market cron idempotency, candidate queue age/attempt bounds, seven-day owner-scoped Theme Scout rows, candidate-only LLM narratives, one reserved candidate worker without added concurrency, sample-shrunk US+India sentiment, observational-only analyst targets, and PositionMonitor hysteresis/provenance. Corrected Kite GTT children to LIMIT and made explicit SELL cancel/confirm every recorded GTT first; partial SELL with a resting GTT now fails closed. Removed fictional Webull MCP write tools/scopes; rewrote the unapproved Webull Trading API and hybrid-stop specs. Updated architecture chapters and diagrams. Production cleanup expired 156 null-owner theme rows and removed 30 exhausted US queue rows, leaving exactly 50 active distinct watchlist symbols. Verified tsc, diagram JSON/drift, 928 tests passed (6 skipped), and Next production build. |
| Vibe external-research runner implementation plan | Codex | completed | 2026-07-17 | Wrote `features/external-research-shadow/IMPLEMENTATION_PLAN.md`: pinned/mirrored Vibe source; zero Kairos API/provider/broker/database/LLM secrets; separate no-key public acquisition and `--network none` analysis containers; deterministic backtest/alpha/validation capabilities first; parallel-only External Lab UI; no scoring/trading influence; source-update governance, acceptance tests, disable path, and a 7-10 engineering-day target for the first end-to-end parallel result. Documentation only; no runtime enabled. |
| Vibe plan independent-review corrections | Codex | completed | 2026-07-17 | Accepted Claude's real Mode-B lookahead concern and added a hard `retrospective_public_history` restriction: no alpha, DSR/PBO, promotion, or performance evidence until data is prospectively frozen. Tightened sequencing with a build-start readiness gate, but corrected the claim that 10 trades certifies calibration: 10/market proves one Learner cycle functions; performance conclusions require at least 30 eligible outcomes/market plus uncertainty and multiple-testing controls. Live production check: India 10 learning-eligible closed paper trades; US 1 closed and 0 learning-eligible after exclusions. Timeline now starts only after readiness and owner authorization. Documentation only. |
| Diagram drift gate — de-duplicate arch-03 topology + make the rule a test | Claude / Opus 4.8 | completed | 2026-07-17 | **Two sources of one fact, both rotted.** `docs/arch/03-agents.md` restated the agent-to-agent topology that `public/agent-diagrams/system-map.json` already owns (the map renders at /dashboard/agents and carries a `history` audit trail). Both asserted `MACRO --> RESEARCH` unqualified — false since the India-macro fix (ffd8821d) made `macro_score` US-only. The duplicate did not double-check the fact; it gave the stale claim a second place to hide. **(1)** Removed arch-03s topology mermaid, replaced with a pointer to the map; kept the chapters own altitude (the table-to-agent matrix + prose). **(2)** `tests/arch-diagram-drift.test.ts` (plain vitest, no CI config, no new deps) now enforces it: parses the maps mermaid into directed edges and fails any docs/arch chapter that re-declares one. **Proven falsifiable, not assumed:** temporarily re-added `MACRO --> RESEARCH` to arch-03 → test failed naming that exact edge → reverted. A canary assertion ("finds arch chapter diagrams to check") caught a real defect in my own first draft — the block regex missed every diagram on CRLF line endings, so the gate would have passed vacuously forever. **Designed against the false-positive that gets tests deleted:** arch-08s gate ladder (a decision sequence) shares no node ids with the map and passes untouched; arch-09s learning loop and arch-01s onboarding wheel legitimately share node PAIRS (e.g. CHAMPION --> RESEARCH), so a naive "no shared edges" rule would have false-fired and been deleted by the next person — overlap is allowed only via a reason-bearing `DECLARED_OVERLAPS` entry, and stale entries fail too, so the exemption list cannot rot into a blanket amnesty. Also asserts every `public/agent-diagrams/*.json` parses and holds agentId/agentLabel/diagram/nodes (object keyed by node id, NOT an array)/non-empty history — a malformed file breaks /dashboard/agents. **(3)** Rule written into `docs/arch/00-index.md`; arch-03s "adding an agent" line now names the per-agent diagram too. Touched only 03/00 + the new test — system-map.json and the per-agent diagrams are owned by concurrent agents. |

| Agent & Flow Architecture diagrams lying — all 8 per-agent diagrams re-verified against code + prod | Claude / Opus 4.8 | completed | 2026-07-17 | **Documentation-only. Every per-agent diagram was last touched 2026-07-04; ALL 8 were materially false — none were accurate.** Method: read the real implementation per agent and queried prod (`dionkikgdmlaotvtbnfr`); never trusted a doc, since docs in this repo are themselves the known liars. **The single worst finding — `paper-trader.json` invented a human approval gate.** `trader_proposals` + "Human approves?" + "unapproved proposals expire" describe a control that **does not exist in any route code** (grep: only the diagram, a plan .md, AgentsPage.tsx, WORK_LOG.md). `paper-trade/route.ts:609` fills **directly** via the `execute_paper_fill` RPC — no proposal, no approval, no human step. The diagram promised the owner a veto they have never had. **Mitigating truth established before reporting:** the route is **paper-only** (no account id, no broker, no order call), so the missing gate has never let a real order through; the locked "live orders only from 605420660" rule is enforced elsewhere and was simply never true of this node. **Other confirmed-false claims:** `signal_weights` asserted as the scoring path — code says it is **NO LONGER read** (`research-agent.ts:1470-1474`); champion `weights_snapshot` → `PROFILE_WEIGHTS` → literal → tilt is the real chain, so the diagram pointed at a table the owner could edit with **zero effect**. Research model claimed **Groq llama-3.3-70b** (the 2026-07-04 "correction" was itself wrong) — it is per-flow `agent_config`, default **deepseek-reasoner**; scheduler was never Windows Task Scheduler but **pg_cron**, with a second India job undrawn on 5 of 8 diagrams. `research-agent` "top 3/day by analyst_score" — **no such cap exists**; real caps are pre-scoring (screener 6 / candidate 40 / India 8). `learner-agent` claimed it ingests `[RESCORE]` flags — **zero RESCORE references in the route**; the only consumer is MentorCoach (and `rescore-check/route.ts:14`'s own comment corroborates the lie). Learner "closes trades >7d" → **14d, ORPHANS ONLY** (it no longer force-closes live positions at all). Learner RAG "Voyage voyage-finance-2" → **Jina jina-embeddings-v3**. `position-monitor` "llm_exit set by the LearnerAgent" → LLM-discretion exits were **removed 2026-07-15**; current path is deterministic `score_reassess_exit`; "Polygon fallback" **does not exist**; "batch-fetches" is per-position; target hit is now **partial** (sells half, stop→breakeven). `macro-sentinel` claimed **daily + deepseek-chat + Alpha Vantage + risk-on/off + writes macro_signals** — it is **weekly Mon**, has **no LLM at all** (the deepseek-chat claim traces to a dead migration-031 seed row), reads **FRED**, emits **green/yellow/orange/red**, and writes the regime to **macro_regime** (macro_signals has no regime column). `theme-scout` "on-demand from Agents page" → **cron-secret-only, chained inside the US research cron**; "7-day expiry" → **30**; "3-5 candidates" → **max 6**; "AI Scout" tag → **does not exist**; model deepseek-chat → **Groq llama-3.3-70b-versatile**. `deepseek-agent` "same pipeline via DeepSeek" → a **standalone AV+LLM scorer** whose premise is void (primary research **is** DeepSeek); "AgentComparisonCard shows DeepSeek vs primary" → that card is **US-vs-India**. `deep-dive` "zero Alpha Vantage budget" → **1 AV OVERVIEW call/run**; per-node chat/reasoner split → **one configurable model** (migration 087 deleted the split). **PROD-VERIFIED data claims (not inferred):** `macro_regime`/`macro_signals` have **no `market` column** (information_schema); **CPI has never loaded — 0 times all-time**, latest run 7/8 indicators; the `<3` guard's null **never reaches the table** (column is NOT NULL DEFAULT 0, insert coerces null→0) so safety rests on the regime *string* — the 2026-06-30 row `regime:green, danger_score:0` on **zero** indicators is the exact fake-confidence failure the guard was added to kill; learner **Phase 0 intact** (1 closed US / 7 India trades vs a per-market 10 gate) and **zero challengers ever created**, so every node right of the gate is plumbing that has never run. **CODE BUGS FOUND, REPORTED, NOT FIXED (documentation-only scope):** (1) **ThemeScout's ▶ Run button 401s** — route is cron-secret-only with no owner fallback (`route.ts:84-86`) but `AgentsPage.tsx:223` sends no header; (2) **CPI `>= 13` off-by-one** costs 1 of 8 indicators every run since the FRED cutover (structural, not transient); (3) `learner/route.ts:205` model fallback is **still `claude-opus-4-8`** — only the prod DB row keeps Opus out; (4) `learner/route.ts:801` tool description **still tells the LLM it uses Voyage**; (5) `deep-dive/route.ts:72` hard-requires a DeepSeek key regardless of configured model; (6) `rescore-check/route.ts:14` documents a learner consumer that doesn't exist. **OWNERSHIP RESPECTED:** `system-map.json` **NOT touched** (macro-read + risk-visibility agents own it — confirmed unmodified in git status); zero code files touched. **Not invented churn:** where a claim survived verification it was kept and logged as such — ANALYST's champion/promotion claim, POLICY advisory-only, deep-dive's entire debate structure + long-only enforcement (the one already-true diagram), position-monitor's US 4:15 PM time and ratchet property, PaperTrader "deterministic, not an LLM". **Gates:** tsc clean; **vitest 802 passed / 6 skipped (= main baseline, no regression)**; build clean. **Verified, not assumed:** all 9 diagrams (incl. untouched system-map) **parsed through real mermaid v10 + jsdom** using the exact `diagram`+classDefs+classLines string `AgentDiagram.tsx:99-107` builds, and every `nodes` key confirmed present in its mermaid (an orphan key emits `class X` on an unknown id and breaks the page). Build initially failed on **missing `.env.local` in the worktree** — an env gap, not the change; passed once env was supplied (`.env.local` is gitignored, not committed). jsdom installed `--no-save`; package.json + lockfile confirmed untouched. Branch `agent-a995d081b4d9085e5`, **not pushed**. |
| Allocator applied the US macro regime to India — market-scope + usability contract | Claude / Opus 4.8 | completed | 2026-07-17 | **Bug INDEPENDENTLY VERIFIED in prod** (`dionkikgdmlaotvtbnfr`) before fixing: `macro_regime` has **no `market` column** (id, week_of, danger_score, regime, signals_triggered, ai_bubble_score, summary, raw_indicators, created_at) and MacroSentinel is US-only by construction (8 US FRED series), yet `lib/allocation/allocator.ts:100` filtered `strategy_sleeves` by market and then read `macro_regime` **unscoped** — stamping the US Fed's verdict onto Indian sleeve weights. Same contamination class as the `macro_score` defect merged 2026-07-16. **Live-capable, not theoretical:** India sleeves exist in prod (equity 70 / defensive_etf 20 / cash 10). Masked ONLY by `allocation_enabled=false` (verified `false`) — luck, not design. **Reproduced against pre-fix code** (verbatim copy, same stub): a US `bear` verdict tilted INDIA equity below its 70 target and `macro_regime` WAS queried for India; the zero-indicator fossil still allocated. **FIX** — new `lib/allocation/regime.ts` mirrors the `lib/data/scores.ts` contract and **imports its `MAX_MACRO_AGE_DAYS`** rather than re-declaring the bound (scores.ts untouched, per ownership): India → UNAVAILABLE **without querying `macro_regime` at all**; US rows need a real verdict (`regime != 'unknown'`) + within **10d** of `week_of` + **`raw_indicators >= 3`**. The **indicator floor, not the age bound**, rejects the prod `2026-06-30` fossil (`green`, danger 0, signals 0, `raw_indicators []`, "No recession signals. Economy in expansion.") — a failed run fossilized as a calm verdict that an age bound cannot catch while fresh. `signals_triggered=0` deliberately NOT the discriminator (calm weeks look like that; rejecting them biases bearish). Also switched row ordering `created_at` → **`week_of`** (matching scores.ts): `created_at` can invert true order — the `2026-06-29` row was written `2026-06-30`. **CALL: macro UNAVAILABLE → NO allocation (`null`), never an untilted config echo** — base `target_pct` with tilt=0 is byte-identical to a real "macro says neutral" verdict, i.e. the neutral-50-and-included fake in a different hat. Whether India should run *static* sleeve targets is a product call left to the owner, not made silently. **Tension stated, not hidden:** the equity target only ever SHRINKS the gross cap, so `null` leaves the owner's configured `max_gross_exposure_pct` in force — *looser* than any regime outcome; accepted because that is the documented default (identical to today's live `allocation_enabled=false`) and tightening on absent evidence would be an unapproved position change. Callers unchanged (`null` is the shipped-off path); `computeAllocationDetailed()` adds an inspectable `reason`. **NOT FIXED, reported:** `normalizeRegime` matches bull/bear/risk_on/risk_off but MacroSentinel writes green/orange/red/unknown → **every real label maps to `neutral`, so the tilt is dead in prod today**; fixing it would ACTIVATE tilting (behaviour change, own approval). **PROPOSAL, not applied:** add `market` to `macro_regime` (schema change — no migration run). Gates: tsc clean, build clean, **813 passing / 6 skipped** (802 baseline + 11 new). system-map: allocation has **no node** — diagram unchanged, `history` entry keyed to TRADER (sole consumer); MACRO→RESEARCH edge left to the macro-read agent. |
| India insider dimension (SEBI PIT) — qualified against live NSE, verdict DO NOT WIRE | Claude / Opus 4.8 | completed | 2026-07-17 | **Task was to wire India's SEBI PIT insider analog into `insider_score` (a genome dimension) if — and only if — the data qualified. It does not. Nothing was wired; the gap is real but the data cannot honestly fill it.** `fetchNseInsider` (`lib/nse-data.ts` → `/api/india/insider` → `SmartMoneyPage`) exists and reads NSE `/api/corporates-pit`, but was measured live against the **34 real India symbols in the book** (`agent_signals market='india'`) before touching any wiring. **(1) COVERAGE FAILS:** any PIT row within the US 90d window = **2/34 (6%)**; symbols clearing the US bar of ≥3 open-market txns in 90d (`MIN_INSIDER_TRANSACTIONS=3`) = **0/34 (0%)**. Widening to 365d rescues only 1/10 large caps. A dimension available for 0% of the universe is not a dimension. **(2) SEMANTICS FAIL — and would have been actively misleading:** US `insider_score` is a 90d open-market (P/S only) buy/sell **value** ratio. Only ~30% of SEBI PIT rows are open-market (Market Purchase 9.7% + Market Sale 19.5%); the rest are ESOP allotments (29.2%), Off Market (22.1%), Gift (5.3%), pledges, amalgamations. Critically PIT's `tdpTransactionType` marks an **ESOP allotment as "Buy"**, so a naive wiring reads routine equity compensation as insider conviction — a same-named field with different cross-market meaning is worse than an absent one. **(3) FRESHNESS is fine** (`intimDt`−`acqfromDt`: p50 2d, p90 34d, max 71d; 83% disclosed after the transaction) — so PIT lag was NOT the disqualifier; density and semantics were. **(4) No Vercel geo-throttle issue reached** — the cookie handshake + every call succeeded from here (0/34 fetch errors); moot given the verdict. **Score-impact: ZERO by construction** — nothing wired, `applicableDimensions()` already omits insider for India, availability mask already `false` on all India rows. India insider stays honestly UNAVAILABLE (weights renormalize onto the remaining dimensions), which was already the prod behaviour; the `agent_signals.insider_score`=50 default-fill remains a display artifact, not evidence. **Shipped: the decision + its evidence, pinned.** `tests/india-insider-not-wired.test.ts` (7 tests) guards it — **mutation-verified**: simulating a naive India wiring (importing `fetchNseInsider` into `research-agent.ts`) FAILS the guard, and `normalizeInsiderScore` is pinned to fail CLOSED on the raw NSE `InsiderTrade[]` shape and on a throttled/blocked `[]` (never a fake-50-included); US Massive→EDGAR→AV cascade pinned unchanged. **NOT fixed (reported, display-only, no consumer):** `fetchNseInsider()` with no symbol returns `{"data":[]}` unconditionally (NSE's market-wide PIT needs `from_date`/`to_date`, never sent) — `SmartMoneyPage` calls it with no symbol, so **the India insider tab has never shown a row**, same class as the 2026-07-16 EDGAR 404; and the per-symbol call sends no date window, so NSE returns the latest ~20 disclosures regardless of age (RELIANCE spans back to **24-Sep-2021**) while the route reports `available: trades.length>0`. Both are cosmetic *only* because nothing consumes them — worth a scoped display fix, owner's call. docs/arch ch.03 updated (dimension table + India insider evidence block). **No migration; no prod write; no push.** |
| Macro-read narrative: India read KILLED (not faked) + 4-day silent-outage fix | Claude / Opus 4.8 | completed | 2026-07-17 | **VERIFIED IN PROD FIRST** (`dionkikgdmlaotvtbnfr`), then fixed. **Brief confirmed:** `macro_regime` has NO `market` column (information_schema), and `macro_interpretations` id=6 (2026-07-13, `market=india`) really does carry Fed-funds reasoning against a 13-position India book. **REACH CONFIRMED NARRATIVE-ONLY** (checked before fixing, as instructed): `macro_interpretations` is written by the route and read back by its OWN GET alone — no scoring/sizing/gate/order/exit consumer exists, so severity stands as narrative. **FINDING THE BRIEF MISSED (the important one):** the leak was NOT only `macro_regime`. `learning_priors WHERE category='macro'` ALSO has no `market` column and is explicitly US (prior id=8 "Rising Fed funds rate environment..." @ confidence **0.80** is verbatim the 80% belief in the quoted prod row; also DXY, 2Y/10Y, ISM/PMI, VIX). In row id=6 the regime was **already `unknown`** — so gating ONLY the regime, as briefed, would have left that exact sentence reproducible. Both US-only inputs are now withheld. **DECISION — KILL, not honest-narrative:** with both inputs honestly withheld an India read has ZERO macro evidence left (only the held symbols). An LLM over no evidence either restates a constant ("we know nothing" — which does not need a `deepseek-reasoner` call every weekday) or reaches into training knowledge for RBI/rupee/FII colour, inventing precisely what the prompt forbids. So the route **refuses `market=india` on BOTH verbs before any LLM call or DB write** (zero spend, zero rows, legacy India rows unreachable) and `MacroReadCard` renders a deterministic not-supported note. Also resolves a live self-contradiction: the India Markets view ALREADY renders `NotSupportedNote` for "...macro sentinel..." while a cron wrote an India read derived from that same US-only sentinel. PARITY = both markets honest, not both get an LLM call. `lib/india-macro.ts` (FII/DII) deliberately NOT wired in as a substitute regime. **CRON SILENCE — ROOT-CAUSED, not guessed:** none of the briefed suspects. `kairos_call_agent`, `verifyCronSecret` and the `agent_config` gate are all fine; both crons are `active` and pg_cron logs "succeeded" — but that only proves `net.http_post` was ENQUEUED. `llm_call_log` shows every run since **2026-07-13 13:30:11** failing identically: `finish_reason=length, reasoning_len~2500`, empty content. prod `agent_config` sets macro-read to `deepseek-reasoner`, which streams chain-of-thought into `reasoning_content` BEFORE emitting `content`; `maxTokens: 600` was too small. The boundary is exact: the first failure is the first run AFTER `macro_regime` id=4 (orange, 7 indicators) landed at 12:30:32 that day — before it, `raw_indicators=[]` made the prompt trivial enough to fit. llm-router threw → catch returned HTTP 200 `{ok:false}` → nothing written, cron reported success. **600 → 1500**, the identical fix `research-agent.ts` already made (512→1500). **MacroReadCard dead prop confirmed + fixed:** `<MacroReadCard market={market} />` sat inside `{!isIndia && (` (1793-1851) so the prop was ALWAYS "us" (India rows unreachable AND the card silently vanished for India). Now `market="us"` explicitly at the US call site (US layout untouched) + a real `market="india"` instance in the India block — the prop is genuinely live. **US path** reuses `lib/data/scores.ts` (unknown-guard + 10d bound + `raw_indicators >= 3` floor + fail-safe UNAVAILABLE); `MAX_MACRO_AGE_DAYS` imported, floor restated (module-private there; scores.ts NOT edited). Absent verdict → prompt says UNAVAILABLE and interpolates NO regime/danger/summary/indicators. **Tests:** +15 (`tests/macro-read.test.ts`), **817 passing / 6 skipped** (802 baseline + 15, no regression). Proved they bite by replaying the pre-fix logic verbatim against them — all 5 contaminating behaviors reproduce, including that the Fed-funds leak survives an `unknown` regime. **Docs:** arch-03 (new Macro read section; corrected a stale "looks back up to 3 weeks" line that contradicted the same chapter's own 10-day contract), arch-05 (both macro-read crons were MISSING from the table; + a pg_cron "succeeded != ran" warning), `system-map.json` (see below), `lib/market-support.ts`. **system-map.json:** fixed the already-shipped gap — `MACRO -->|macro_signals| RESEARCH` was wrong on BOTH scope (macro_score is US-only since ffd8821d) AND table (`danger_score`/`regime` live on `macro_regime`; `macro_signals` has neither column — verified in prod). Added the MACROREAD node (the agent had 2 active crons + a table dependency but was never on the map). +2 history entries (68→70). **NO migration applied.** **FLAGGED FOR OWNER (not done — prod DB mutations):** (1) drop the `kairos-macro-read-india` cron (jobid 47) — now a harmless no-op; (2) 3 contaminated India rows in `macro_interpretations` (ids 2/4/6) remain but are unreachable; (3) `learning_priors` has no `market` column — fine while priors are US-only + US-gated, but a real India prior set would need market-tagging. **Not pushed.** |
| `markets/overview` vs `price_cache` — arch-05 was WRONG; route kept on grouped, doc corrected | Claude / Opus 4.8 | completed | 2026-07-17 | **Question posed: should `markets/overview` read the warm `price_cache` as `docs/arch/05` says it should? Answer, on evidence: NO — and arch-05 is simply wrong. The doc was corrected; the route was not changed.** Treated the doc as a claim to test, not ground truth. **VERIFIED in prod (`dionkikgdmlaotvtbnfr`), not assumed** — three independent findings, each of which alone kills the cache read: (1) **Index coverage does not exist.** The tile needs 15 symbols on ONE session; `QQQ`, `DIA`, `VIXY` hold **2 bars each** (07-14, 07-15) — the daily fill only began covering them 07-14 and the ~400d backfill is sector-XL-only (11 XLs: 273-274 bars back to 2025-06-12; SPY 223). Across the WHOLE table only **two** fully-aligned 15/15 sessions exist (07-14, 07-15); 07-13 has 11, and 07-10/09/08 have 12 (no QQQ/DIA/VIXY at all). The owner's suspicion that index symbols might lack coverage is **confirmed**. (2) **The cache head is RAGGED — a cache read would re-open the exact bug just fixed.** On 07-17 the newest bar was **07-16 for SPY and XLV but 07-15 for the other 13**. A per-symbol "latest bar" read therefore renders SPY's 07-16 close beside XLK's 07-15 close as one snapshot — the **cross-session mix** the grouped rewrite (`ffd8821d`) exists to prevent. (3) **The cache is a full session STALER.** Requiring honest 15/15 alignment resolves to **07-15**, while grouped serves **07-16**. Cache-first would trade a session of freshness for nothing. (4) **The "immune to the 5/min ceiling" argument is OBSOLETE.** It was written when the route fired 15 per-symbol `/prev` calls; the route now spends 2–3 grouped calls, each `revalidate:3600` (past sessions immutable) behind a 5-min route memo + process `sessionCache` ⇒ **~2 calls/hour ≈ 0.7% of a 300/hour budget**. The premise was overtaken by the rewrite. Reading `price_cache` is also **not** free — it is a Supabase round-trip and a DB dependency this route currently does not have. **Option 2 (cache-first + bounded fallback) considered and REJECTED with reasoning, not ignored:** `price_cache` is filled by `kairos-price-cache-fill` from the **same provider's same grouped endpoint**, so an outage/rate-limit that breaks the route correlates with a stalled fill — it would fall back to a staler copy of the same failure, while adding a DB dependency + a new failure mode for a degradation whose frequency already dropped ~8x when calls went 15→2. The existing degraded payload + Retry is honest. **Confirmed the owner's own caveat rather than "fixing" it:** `mostRecentWeekday()` storing the previous *completed* session (latest=07-15 on 07-16) is correct by design — left alone. **`fetchedAt` sub-task was ALREADY DONE at `ffd8821d`** — the brief described pre-`ffd8821d` state. `MarketsPage:1494` already stamps the chip from `json.fetchedAt` (the server's upstream fetch time, not the browser clock) and the footer already renders `Fetched {new Date(data.fetchedAt).toLocaleString()}` + `session`/`prior close`. **Reported as already-fixed rather than re-done; pinned with tests instead.** **REAL BUG FOUND EN ROUTE (in my region, fixed):** the US slow-fetch note read *"Fetching live quotes from **Robinhood** via **AI subprocess** — may take up to 90s"* — **false on three counts**: the route calls Massive grouped-daily (no broker), there is no subprocess, and the closes are **end-of-day, never "live"**. That is a freshness assertion the data does not have — the one non-negotiable. Now: *"Fetching end-of-day closes from Massive — resolving the latest published session…"*. **arch-05 CORRECTED** (header + the `kairos-price-cache-fill` row): `markets/overview` removed from the warm-cache reader list with the prod evidence and the rejected-fallback reasoning recorded inline. **Verified before editing that `markets/synthesis`, `markets/quotes`, `charts/sector-returns` DO genuinely read `price_cache`** — so the correction is scoped to the one false claim and does not overreach. `system-map.json` **unchanged and correct** — it references neither `markets/overview` nor `price_cache`; this is a display route, not an agent flow, and no flow/table/schedule changed (the decision *declines* a table dependency). **Gates: tsc clean, build clean, vitest 810 passed / 6 skipped** (802 baseline + 8 new, 0 regressions). New tests pin the contract so a future "optimisation" back to a cache read cannot land silently: session-alignment (all 15 on ONE declared session), never-label-a-prior-session-as-today (Monday clock + Friday session ⇒ `sessionDate` must be `2026-07-17`, **not** `2026-07-20`), grouped-endpoint-only (never supabase/price_cache/robinhood), `fetchedAt` stable across the memo window (data's age, not the client's clock), and two source-level copy assertions. **Copy tests PROVEN to fail on pre-fix code** (stashed `MarketsPage.tsx` ⇒ 2 failed). **Build gate needed the gitignored `.env.local` copied into the worktree** (byte-identical, `git check-ignore` confirmed uncommitted) — the failure was environmental, not the change. **Constraints honoured: NO migration applied, NO write to `price_cache` from this worktree, no broker/LLM/money path, read-only display only. India untouched** (`snapRowToQuote`/its own snapshot left alone; the US slow-fetch note is inside `{!isIndia && …}`). Files touched: `docs/arch/05-crons-and-scheduling.md`, `components/dashboard/MarketsPage.tsx` (US loading-note region only), `tests/markets-overview.test.ts`, `WORK_LOG.md` — **`app/api/markets/overview/route.ts` deliberately NOT changed; that is the finding.** No files owned by other agents touched (`lib/allocation/*`, `macro-read`, `MacroReadCard`, `lib/nse-data.ts`, `lib/research-agent.ts`). Branch `worktree-agent-a324493b050d63d29`, **not pushed**. |
| Risk Analytics research visibility — score + AGE, coupled to nothing | Claude / Opus 4.8 | completed | 2026-07-17 | **Why: on 2026-07-16 AVGO was 6 days unscored while Risk Analytics told the owner to trim it, and nothing on screen said so. THE AGE IS THE FEATURE, NOT THE SCORE** — a 6-day-old 91 and a fresh 91 are not the same claim. `GET /api/portfolio/risk-daily` now attaches a nullable per-holding `research` block (score, direction, `scored_at`, `sessions_since`, `days_since`, `state`, `scored_as_holding`) joined from the latest `agent_signals` per **`(symbol, market)`** — never symbol alone. New pure module `lib/research/risk-annotation.ts`; rendered in `PortfolioRiskPage.tsx`; rows deep-link to the research journal. **INVARIANT R1 — this is a DISPLAY JOIN and nothing more.** No field is read by `computeHoldingRisk`, `sba-v1`, `constructPortfolio`, the execution kernel, or any gate. Deliberate: **a sector is over-cap *because* research liked that sector, so letting `analyst_score` also veto the cap double-counts the same signal** — the risk layer exists to be the one thing not persuaded by the score. Supersedes the unmerged `features/risk-research-integration` (research *ordering* trim absorption — not pursued). **T1 is falsifiable and was falsified, not asserted:** it has a behavioral half (risk bytes identical with the block present vs absent, and invariant across every score/direction/staleness variant incl. `never`) and an architectural half that scans `lib/risk/*` for any *code* reference to research. **Verified by sabotage:** wiring `analyst_score` into `holding-risk.ts` AND conviction-ordering into `sector-breach.ts` → **T1a fails (3 tests)**; reverted clean (`git diff lib/risk/` empty). The detector **strips comments first** — `sector-breach.ts` contains prose *asserting* the invariant ("reads no research score, no analyst_score") that a naive grep flags as a violation; the detector is itself falsification-tested both ways (fires on real coupling, ignores prose). **T3 is falsifiable and was falsified:** staleness is counted in market-local **SESSIONS** via the EXISTING `marketSessionsSince` (`lib/trading/paper-exit-policy.ts` → `isMarketHoliday`), reused not reimplemented. Swapping it for a calendar-day count → **T3 fails (4 tests)**. A Friday score read Monday is **3 days but 1 session**; a day-rule would paint the book stale every Monday and train the owner to ignore the warning. US/India holiday calendars verified to genuinely diverge (2026-10-02 Gandhi Jayanti: India 1 session, US 2). **Four states, not collapsed:** `fresh` (≤ **2** sessions, owner Q1) · `stale` (warning + day count; annotates the SCORE only, never the action — owner Q2) · `never` (**not a link** — a link to nothing is a lie) · `unavailable` (abstained — never a number). **PROD-VERIFIED before trusting the join** (`dionkikgdmlaotvtbnfr`): AVGO in read-only `965848641` = 4 sessions → **stale**, exactly the reported failure; India book (scored 07-17 04:00) → fresh. **`is_holding` is FALSE in 463/463 rows — no holding-path score has EVER been written**, so this is not an AVGO quirk: every annotated row is labelled a screener **candidate** score, and a `neutral` there does NOT mean "no exit signal" (the direction gate only emits `short` when `isHeld`) — T7. **Q3 (omit `conviction`) is right for a better reason than the spec gave:** it diverges in **5 of 463** rows, all 2026-06-28 with `score_source=null` — the pre-`deterministic_v1` LLM era; since then the two have never diverged, so it is redundant *by construction*, not merely empirically. **Spec §6 was factually wrong and is corrected:** the `toggle(symbol)` expander it described lived in a local `FunnelTab` in `app/dashboard/research-journal/page.tsx` that **was never rendered** (dead the day it was written — the page has always used `components/dashboard/ResearchFunnel.tsx`), and the page *did* already read `searchParams` (for `tab`). Deep-link `?symbol=&market=` therefore went into `ResearchFunnel.tsx`; the dead `FunnelTab` was **deleted**. The market sync deliberately converges rather than running once at mount: `MarketProvider` restores the persisted market in its OWN mount effect and React fires child effects BEFORE parent effects, so a one-shot sync would be silently clobbered by localStorage. **Fail-soft (T8):** an `agent_signals` error still renders the risk table with an explicit "research unavailable"; a failed read (`research:null`) stays distinct from a successful read that found nothing (`state:'never'`), so the UI never claims "never scored" when it merely failed to look. **Mobile (Q4):** 375px keeps Symbol/Risk/Action/Research (score + age badge); the rest behind the existing row expander. **Additive only: no schema change, NO MIGRATION, no new cron, no provider call, no LLM, no order path touched.** Gates: tsc clean · build clean (52/52 pages) · **vitest 846 passed / 6 skipped** (802 baseline + 44 new, **0 regressions**; a single `congress-source-honest` timeout on one cold-cache run did NOT reproduce on two subsequent full runs nor on baseline-in-isolation — pre-existing 5s-timeout sensitivity in an unrelated test, flagged separately). `system-map.json`: **diagram DOES move** (new table dependency) — a dotted `RESEARCH -.->|DISPLAY ONLY · never enters computeHoldingRisk or sba-v1| RISKDASH` edge that deliberately terminates at the **dashboard**, not the **engine**; verified by rendering the mermaid (`RISKDASH` has exactly 2 incoming edges — `HOLDINGRISK` verdict + `RESEARCH` annotation — and **no** `RESEARCH→HOLDINGRISK` edge exists). RISKDASH node description + history entry (#69) updated. arch 08 + `features/risk-research-visibility/FEATURE_ARCHITECTURE.md` (§10 decisions recorded as DECIDED) updated. Branch `worktree-agent-ae2e686eb58859511`, **not pushed**. **COULD NOT VERIFY:** the rendered `/dashboard/risk` and the deep-link expansion — both require an auth login and I did not enter credentials; UI verified via tsc + build + unit tests + the prod-data join only. **`unavailable` and `never` have NO production witness**: `agent_signals` has no abstain representation (0/463 null scores) and every current holding resolves to a signal, so both states are defensive and fixture-tested only. |
| Sector Performance timeframes inert — honesty guard + sector history backfill | Claude / Opus 4.8 | completed | 2026-07-17 | **Owner's root cause INDEPENDENTLY CONFIRMED in prod** (`dionkikgdmlaotvtbnfr`): all 11 sector ETFs held exactly 2 bars (2026-07-14, 07-15), so every cutoff (`now − 7/30/90/180/365`) preceded both bars, every window selected the SAME two bars, and every period returned the SAME one-day move. Reproduced the pre-fix guard against those bars: **1W/1M/3M/6M/1Y all returned +2.00%** — identical, and rendered as e.g. "1Y return". Confirmed an **honesty bug**: `if (candles.length < 2)` is a COUNT check, not a SPAN check. **Owner WRONG on one secondary point (corrected):** the daily fill is NOT a day behind — it stores the *previous completed session* by design (`mostRecentWeekday()`), so latest=07-15 on 07-16 is correct, not a bug. Nothing to fix there. **FIX (1) span-aware honesty guard** — pure, unit-tested `lib/markets/sector-returns.ts`; a window reports a return ONLY when the bars actually span it, else `returnPct:null` + machine-readable reason the UI renders as what/why/next (never a bare "—"). THREE checks, all load-bearing: start edge (`START_GRACE_DAYS=4`; 4 because the longest non-trading run is 3 — a holiday-adjacent weekend; exact-match would false-reject every window) catches missing early history on long windows; **span coverage (`MIN_SPAN_COVERAGE=0.4`)** catches degenerate spans on short windows — **my first guard was insufficient and only driving the real endpoint caught it**: on 07-17 the 1W cutoff (07-10) sat *exactly* 4 days before the oldest bar (07-14), so both edge checks passed and a 1-day move was served as a "1W return" of −1.11%; 0.4 sits below the worst legitimate 1W (holiday week, span 3/7=0.43) and above the degenerate case (1/7=0.14); end edge (`STALE_GRACE_DAYS=7`) rejects a window ending at a stale point. **FIX (2) backfill** — extended the EXISTING `kairos-price-cache-fill` cron (no new cron, **no migration**): ~400d of daily bars for the 11 XLs via `range/1/day`, which returns the full series in ONE request ⇒ **11 provider calls total**, lease-paced 5/min via `try_acquire_provider_slot`, wall-clock bounded, resumable. Ordered **before** the per-symbol fallback — otherwise a grouped-endpoint failure starves it forever (observed). **FIX (3)** SectorBreadth labelling: A/D and the period return are now two independently-scoped clauses ("latest session only" / "period-scoped"); **A/D semantics deliberately UNCHANGED** (always latest session, per the existing comment) — period-scoping it would be a design change, not shipped. **CAUGHT EN ROUTE — PostgREST 1000-row cap:** `sector-returns` batch-read 11 ETFs unpaginated; a full 1Y is ~3000 rows so it silently returned only the 4 alphabetically-first sectors complete and **7 with ZERO rows**. Latent at 22 rows; bit the instant real history landed. Read now paginated. The honesty guard degraded correctly here — reported `no_data` instead of inventing a number. **VERIFIED END-TO-END against prod, not just unit tests:** backfill drained all 11 XLs (273 bars each, back to 2025-06-12); post-fix every sector × every period returns a **distinct real** value (XLK: −2.26/−2.27/+17.48/+28.02/+39.11, spans 5/28/86/176/363d), coverage 11/11 on all five windows. **India parity: no equivalent defect** — `MarketsPage` gates US analytics behind `{!isIndia}`; India renders its own NSE `SectorHeatmap`, a single-session snapshot with **no period selector**, so it cannot make an unsupportable period claim; structural gaps already carry `NotSupportedNote`. No change needed, none made. **Provider budget:** this path is **Massive, not Alpha Vantage** — the AV budget guard/`lib/av-cache.ts` is untouched, so the capacity-starved research agent is unaffected. Also verified Massive's grouped 403 ("today's data before end of day") is an **off-hours artifact of my local test** (UTC date ahead of ET), NOT a prod break — the 13:25 UTC cron never hits it; deliberately did not "fix" it. Display-only, read-only: no LLM, no broker, no money path, no `price_cache` schema/semantics change. Gates: tsc + build + **vitest 589 passed / 6 skipped** (569 baseline + 20 new; regression tests **proven to fail on pre-fix logic**). **Could NOT verify:** the rendered Markets page (requires auth login — declined); component render verified via typecheck/build + API contract only. Branch `worktree-agent-a3a11edcd4630d5bc`, **not pushed**. |
| Markets page honesty batch — intraday-as-daily, dead chart, count-not-span, silent zeros, false provenance, inverted colour ramp | Claude / Opus 4.8 | completed | 2026-07-17 | **US Markets display path. All findings independently reproduced against live Massive data before fixing; two of the six were worse than reported.** (1) **`/api/markets/overview` reported the INTRADAY move as the DAILY change** — `(c - o) / o` off a single `/prev` session. Verified live: XLK 179.00→177.52 = −0.83% displayed vs true −2.24% (close 177.52 vs 07-15 close 181.58); SPY −0.27% vs true −0.54%. Owner's hypothetical sign-inversion is **real and not rare**: on the 07-15 session **2 of 15** tracked symbols inverted — XLRE displayed −0.11% (RED) on a **+0.18% GREEN** day, XLV displayed +0.58% on a **0.00% flat** day. (2) **NEW — the provider key is rate limited at ~5 req/min** ("You've exceeded the maximum requests per minute"), and the old route fired **15 parallel `/prev` calls**, so ~10 failed on every cold cache and the zero-fallback painted each as a green `+0.00%` pill. Reproduced end-to-end against `main`: exactly 5 symbols resolved, 10 returned null/zero. So bug #4 was not latent — it was **live on ~2/3 of the page**. **FIX:** rewrote the route onto **grouped-daily** (`/v2/aggs/grouped/...`) — every US ticker's OHLC for one session in ONE request; two calls (latest + prior session) now serve all 15 symbols vs 16 before, keep every symbol on the SAME session, and fit the 5/min budget (~0.6/min). Walks back from today's **ET** date skipping weekends, past 403 "before end of day" (grouped for the current calendar date is refused until after midnight ET) and past holidays (200 with no `results`). Session date is derived from the bars, never the wall clock. Verified live post-fix: XLK **−2.24%**, SPY **−0.54%**, 15/15 resolved, `sessionDate 2026-07-16` vs `priorCloseDate 2026-07-15`. Matches the pattern already established by `price-cache-fill` (grouped needs no pacing lease; only per-symbol fallbacks do). (3) **Silent zeros → honest states:** `price/change/changePct` are now `number \| null` with per-symbol `status` + `reason` and payload-level `stale`/`unavailableCount`/`degraded`; degraded payloads are **not cached** so they clear on recovery. The no-API-key path returned `stale:true` that the client's interface never declared or read — now wired to a real banner. (4) **`PriceChart` + `showRecessions` were dead:** `selectedSymbol` was written only inside `{isIndia && …}` and read only inside `{!isIndia && …}` — mutually exclusive. **Decision: wired for US (SectorTreemap → chart; sector ETFs are US symbols `price-history` genuinely resolves), removed the false affordance for India** — `/api/charts/price-history` resolves via `fetchUsCandles`, which cannot price NSE index symbols (`^CNXIT`, `^NSEBANK`), so wiring India would ship a permanently empty chart: one silent lie for another. India follow-up needs an India candle path, not a click handler. (5) **`lib/chart-data.ts` count-not-span:** `rows.length > 0` + newest-bar freshness let a `days=180` request for a 2-bar symbol short-circuit the REST backfill → a 6M label on a 1-day return (29 symbols hold exactly 2 bars). New shared helper `lib/data/history-span.ts` (`spansRequestedWindow` / `actualSpanDays` / `isShortHistory`) — deliberately a NEW file; `sector-returns` + `SectorBreadth.tsx` are another agent's and were **not touched**, but can adopt it. `PriceChart` now labels the span it actually covers and withholds a return below 2 bars. (6) **`SectorTreemap` colour ramp verified inverted:** `cellColor(0)` → `rgb(52,46,153)` **indigo**, `cellColor(-0.1)` → `rgb(65,109,109)` **teal** — a negative sector rendered greener than a positive one; neither legend swatch was reachable. Every channel now scales WITH intensity; extracted to `lib/markets/sector-color.ts` to unit-test without recharts. Treemap **area** (was ETF share price — not cap/weight/volume) is now **uniform and disclosed**; colour carries the only real signal. (7) **`/api/markets/thesis` was an unlisted consumer** (found by typecheck) feeding these numbers to an LLM labelled "REAL-TIME INDICES (today's session)" — double-wrong; now omits unavailable symbols (never 0) and states close-vs-prior-close. **Gates:** tsc clean, build clean, **610 passing / 6 skipped** (was 569/6 — +41 new, no regressions). New tests **proven to fail on the original code** (10 fail on the old cellColor/span guard; zero-fallback tests fail on the old formula). **NOT verified:** the authed `/dashboard/markets` render (login required — did not enter credentials); UI verified via unit tests + build + live API payload only. `MASSIVE_API_KEY` rate limit (~5/min) is a **shared app-wide ceiling** — flagged, not re-architected. No migration; no schema change; read-only display path. |
| macro_score money-path fix: India scored with US macro regime + unbounded stale-regime reach-back | Claude / Opus 4.8 | completed | 2026-07-16 | **Both bugs independently re-verified against prod (`dionkikgdmlaotvtbnfr`) before fixing.** BUG 1: `macro_regime` has **no `market` column** (verified via `information_schema`); `lib/data/scores.ts` read it unfiltered, so India inherited the US FRED verdict — prod showed `us` and `india` both at `macro_score 60.00` (= 100 − danger 40, the 07-13 `orange` row). BUG 2: the selector skipped `unknown` rows with **no age bound**, so on 07-13 it reached back **13 days** to the 06-30 `green` row → 13 India rows scored `macro_score 100.00` (maximally calm). **The 06-30 green is provably a FAILED RUN, not a calm verdict:** `raw_indicators = []` (zero indicators) and its summary lacks the current code's "across N/8" phrasing — it predates the unknown-guard. Same fossil class: 06-29 `red`/danger-100 off **1** indicator. **FIX (`lib/data/scores.ts` only):** (1) market derived from the symbol via the same `isIndia()` research uses → India ⇒ macro **UNAVAILABLE** (`macroDataAvailable:false`, excluded, weights renormalize); India never queries the table. Did **not** invent an India regime; did **not** wire in `lib/india-macro.ts` FII/DII (separate approved build); did **not** fake-neutral-50-and-include. (2) `MAX_MACRO_AGE_DAYS = 10` — weekly cadence (`week_of` Monday, cron `30 12 * * 1`) needs >7 (a valid row is exactly 7d old on the next Monday pre-cron) + 3d slack, and **<14** so a fully missed run can never be masked. (3) `raw_indicators >= 3` (mirrors MacroSentinel's own classification floor) rejects zero-indicator fossils even when fresh. (4) `fetchMacroScore` now returns `available` explicitly — the old caller-side inference (`regime` is a string ≠ "unknown") could only express the unknown case and would have read an India/stale/fossil row as real evidence. **Fail-safe throughout: no usable row ⇒ UNAVAILABLE, never green/calm, never a deeper reach-back.** **IMPACT — India long→neutral flips: ZERO.** Reconstructed India's included dims as exactly `{fundamental, technical, macro}` (matches 48/50 contaminated rows; the other 24 rows already ran `{fundamental, technical}` and match 24/24). Post-fix India = 2 usable dims ⇒ **not** `thinEvidence` ⇒ gate still transacts. Over the 24 rows (07-14..07-16) where the current gate provably reproduces recorded direction (24/24), **0 of 24 flip** (0 long→neutral, 0 neutral→long); score deltas −4..+7. US macro 60 sat *below* India's fund/tech blend, so removing it mostly *raises* India scores — the contamination was mildly *suppressing* India longs, not inflating them. **Could NOT prove:** direction on 07-07..07-13 is not reconstructible (the gate rule disagrees with recorded direction on 27 of 50 rows there — an older//additional neutral-forcing path), so no flip count is claimed for the 07-13 `macro_score=100` rows. **NO migration applied; no rows mutated.** A `market` column on `macro_regime` is the right long-term fix → reported as a proposal, not applied. Historical contamination logged as an OPEN ITEM in `docs/arch/09` (4 closed + 13 open India trades entered on a contaminated score; India still Phase 0 at 7/10 closed so no weight mutation driven yet; taint proposal uses the existing `tainted`/`excluded_from_learning` vehicle — no migration needed — pending owner decision). Also found: `app/api/agent-mind/macro-read/route.ts:47` reads `macro_regime` unfiltered for an LLM **narrative** path (out of scope, reported); `lib/allocation/allocator.ts:100`, `app/api/agents/{trader,downside-hedge}/route.ts`, `lib/trade-alert.ts` also read it unfiltered. Gates: `tsc` clean, `build` pass, **587 passing / 6 skipped** (569 + 18 new; +18 in `tests/macro-score.test.ts`, **12 of which fail on pre-fix code** — verified by stashing the fix). |
| Risk-internal sector-cap breach allocator (`hr-v1` → `hr-v2`) | Claude / Opus 4.8 | completed | 2026-07-16 | **Defect (owner-reported, confirmed in code and prod): a sector-cap breach is a property of the SECTOR and carries NO per-name allocation, so `sectorUtil >= 1` was the identical number for every holding in the sector and hr-v1's `if (nameBreach \|\| sectorBreach \|\| clusterBreach) → trim` handed EVERY Technology name the identical verdict** — the live AVGO line, *"Trim your position because Technology holdings exceed the 30% sector cap (at 65.6%), with a risk score of 63"*, never decided WHICH names should absorb the breach or HOW MUCH each gives up. Arbitrary (no name distinguished) and unactionable (no quantity). **FIX:** new pure module `lib/risk/sector-breach.ts` (`sba-v1`) — deterministic, **risk-internal: a function of position weights and ONE owner-set cap, with ZERO research/`analyst_score` coupling** (the competing `features/risk-research-integration` proposal on branch `worktree-agent-abaf0b16ef6af4175` is untouched). **Rule: water-fill** — trim the sector's largest names down to a common level `L` where `Σ min(wᵢ,L) = cap`. Justified in the doc against the two alternatives: *pro-rata* rejected because it re-ships the same blanket verdict (every name still trims) and preserves the sector's lopsidedness, leaving the **name** cap as breached as it was; *marginal-contribution-first* rejected because for a sector-WEIGHT cap a name's marginal contribution **is its weight** (`∂Σwᵢ/∂wⱼ = 1`), so it's largest-first with extra abstraction that invites smuggling correlation/beta into the ordering. Water-fill uniquely minimises the largest residual name weight (and the sector Herfindahl) among reduce-only allocations, so it discharges the sector cap **and** does the most it can for the name cap — the two limits the owner actually set point the same way. Honest cost recorded: it sells the biggest name first, which is often the best name; risk has no conviction input **by design**. **Denominator decided, not assumed: NAV.** `live-portfolio-gate.ts` — the code that enforces `max_sector_exposure_pct` on the live money path — builds the book as `value/NAV`, so the reduction is `S − c·NAV` = **35.6pp** for the Tech example; the invested basis would give **50.9pp** (~43% wrong). This also fixed a real inconsistency: hr-v1 scored the NAME cap vs NAV but fed `sectorWeightPct` from `computeRiskMetrics().sectorBreakdown`, which is **invested**-relative — two denominators against two caps the owner set on one basis. **Safety properties, each proven by a test verified to FAIL on the pre-fix code:** (1) **an exit is never suppressed** — the `exit_review` branch is first and unconditional; tested across all four allocation roles + no-allocation (same invariant as `lib/evidence/degradation-guard.ts`, which was the model); (2) **defaults to honest, never to the bug** — a sector breach with no usable allocation → `review` + `missing_inputs:["sector_breach_allocation"]`, NOT a fallback to blanket-trim; (3) **the allocator cannot suppress a DIFFERENT limit** — name/cluster breaches still trim a `not_selected` name; (4) **sector-unknown degrades honestly** — excluded from every sector total, never bucketed into a synthetic sector (which `constructPortfolio` does), never assumed cap-compliant, and its presence doesn't perturb the others' allocation; (5) **ties need no arbitrary winner** — water-fill is continuous, so equal weights get equal trims; output ties break `symbol` asc; (6) **LLM prose-only, provably** — `parseStrategyNotes` (`lib/risk/strategy-notes.ts`, extracted from the route) can only emit `Map<requestedSymbol,string>`: a `{"AVGO":{"trim_pct":99}}` or a `"risk_posture":"hold"` key is dropped, not coerced. **PARITY: both markets** — India fixture (INR, `.NS`, incl. an India-only sector label absent from the US map) allocates identically to the equal-weight US book, per-currency, never cross-summed. Non-selected names now say `hold` **with the why** ("Technology IS over its 30% cap and 35.6pp must come out, but INTC is not among the names selected to absorb it — at 3.6% of NAV it already sits below the 5.35% level the 4 larger positions are trimmed to"). Read-only accounts (everything but `605420660`, incl. `965848641` where AVGO sits) are labelled advisory-informational in the deterministic `action_reason`. **Falsification checks run:** reverting to blanket-trim → 6 tests fail; invested-basis formula → 4 fail; pro-rata → 7 fail. **No migration, no schema change** (`formula_version` is text; the allocation rides existing `action_reason` + `risk_drivers`), no order path touched, no LLM on any number. Gates: tsc + build + **vitest 649 passed / 6 skipped** (was 569 — +80 new, 0 regressions). `features/risk-sector-breach-allocation/FEATURE_ARCHITECTURE.md` + arch 08 updated; `system-map.json` **diagram unchanged** (no agent-to-agent flow/handoff/table/schedule changed — this is internal to the HOLDINGRISK node's compute), but the HOLDINGRISK node description asserted `hr-v1` + the old trim rule and is corrected, with a history entry appended. Branch `worktree-agent-a6596ea918d82369a`, **not pushed**. **OPEN — follow-up, deliberately not built:** `computeRiskMetrics().sectorBreakdown[].weightPct` stays invested-relative for the account roll-up display, so its sector bar can differ from the risk engine's NAV figure on a cash-heavy account; unifying it touches the paper path, `constructPortfolio` inputs, and the Risk page. |
| SEC Form 4 URL defect — US insider data had NEVER worked (+ insider availability honesty, insider universe) | Claude / Opus 4.8 | completed | 2026-07-16 | **VERIFIED LIVE, not assumed.** (1) **The Form 4 URL was wrong since inception** — both `app/api/markets/edgar-insiders/route.ts:142` and `lib/data/edgar-insider.ts:80` built `<accession>.xml`, which is not an EDGAR artifact. Reproduced against live SEC: Visa `0001403161-26-000090` → route URL **404**. `if (!xmlRes.ok) continue` swallowed it → `transactions: []` → UI rendered a confident "No recent insider trades found." over a **100% failure rate**. **The audit's proposed fix was itself wrong and I did not ship it:** `primaryDocument` for Form 4 always points at the XSL *rendering* (`xslF345X06/form4.xml`), which SEC serves as **`text/html`** — it returns **200** but contains zero `<rptOwnerName>`/`<nonDerivativeTransaction>` tags, so it parses to zero transactions (a *silent* empty — strictly worse than the 404). Correct target is the same path with the `xslF345X0N/` prefix **stripped**; verified 200 + parseable across V/AAPL/TSLA/COST/MA/PG (TSLA → `Musk Elon`). Filename varies by filer agent (`form4.xml`, `tm2618092-2_4seq1.xml`, `wk-form4_1784149645.xml`) so it must come from `primaryDocument`, never hardcoded; archive path needs the **unpadded** CIK (padded 301-redirects). Fix = `buildForm4XmlUrl`/`stripXslRenderPrefix`, shared by both copies. (2) **`insider_score` could never reach its own threshold** — prod: 0 of 442 rows ever >50 (`us 50.00`×327, `us 10.00`×36, `india 50.00`×74, null×5), while `/api/markets/smart-money` filters `.gte("insider_score", 55)` ⇒ `highInsider` **always `[]`**, reading as "we checked, insiders aren't buying" when the truth was "insider scoring never produced a usable value". (3) **The "second scorer" is actually a 3-source cascade, not a race** — `resolveInsider` (`lib/research-agent.ts:138`) tries Massive Form 4 → EDGAR → AV, first `available:true` wins; the 36 `10.00` rows are **Massive** (added 07-14, matching their first_seen), and its own comment already documents that EDGAR+AV were dead. Ordered fallback, no concurrency, no race. (4) **Availability needs NO schema change** — `decision_observations.availability_mask` **already** persists the `available` flag and links via `signal_id`; smart-money now joins it to filter `highInsider` (fail-closed: unprovable ⇒ excluded) + returns `insiderCoverage`. **The audit's suggested heuristic ("treat a 50 with no corroborating evidence as unavailable") is refuted by prod data: 18 us rows are `available:true` AND exactly `50`** — real balanced data the heuristic would have silently deleted. India's 74×`50.00` is a default fill in `agent_signals`, but `availability_mask.insider` is already `false` for all 74 (India has no EDGAR; `dimensionsFor` never adds the insider dim) — the honest record existed, it just wasn't read. (5) **Universe was half-dead** — `agent_watchlist` **does not exist** in prod (only `watchlist`; verified via information_schema), and supabase-js reports that in `error`, so `(watchlist ?? [])` erased half the universe silently; a bare rename would NOT have fixed it since `watchlist` has no `is_active` column (uses `expires_at`). `.limit(1).single()` took **1 of 7** broker accounts — confirmed Webull wins by a **294ms** tiebreak (21:00:04.355 vs .061), so Agentic/Trading contributed zero. Now unions all accounts via `symbolsFromLatestLiveSnapshots` + fails loud on PostgREST `error`. (6) Added `maxDuration = 60` (route had none); route now returns `status: complete\|partial\|failed` + `coverage{scanned,unresolved,failed,filingsScanned,...}` and per-transaction **`filingLagDays`** so the UI can separate "no open-market P/S trades in the last 6 filings" (a **real** finding — most Form 4 activity is A/M/F/G comp, not P/S) from "fetch failed", and can show the filing lag. 26 new tests across 3 files, **all verified failing on pre-fix code**. Gates: tsc clean, **vitest 595 passed / 6 skipped** (569 baseline + 26, no regression). No migration, no LLM, no money path, display-only. Branch `worktree-agent-adc78d18c56456349`, **not pushed**. **OPEN — owner decisions, proposed NOT built:** (a) **persist availability on `agent_signals`** (a real `insider_available` column) — the `decision_observations` join works but is indirect and only covers signals that produced an observation (410/436 rows carry `signal_id`); (b) **Form 4 caching** — zero insider tables exist; every page load refires ~56 sequential SEC calls + a 797KB CIK map + 7.4s of hardcoded sleeps, and Form 4s are append-only with a multi-day filing lag = the ideal cache candidate. Both are schema changes ⇒ deliberately left as proposals. **NOT MINE / handoff:** `components/dashboard/MarketsPage.tsx:731` still hardcodes "No recent insider trades found." and `:835` still shows only `t.date` — the honest empty state + filing-lag display need that file's owner to consume the new `status`/`coverage`/`filingLagDays` contract. |

| Held positions not re-scored daily — root-cause + fix (US + India) | Claude / Opus 4.8 | completed | 2026-07-16 | **PROVEN root cause (not the suspected silent catch): holdings are exempt from the candidate cap but NOT from the cron's wall-clock budget, and their order was STABLE — so the budget decapitated the same tail every run, permanently.** Prod evidence, run `a4530e8f` (07-16, US): all 56 held symbols occupied batch slots 1-56 (holdings-first ordering was working); slots 1-30 → 30/30 signals, slots 31-56 → **0**. FEZ sat at slot 30 — the exact last symbol the budget reached — which is why exactly one Robinhood holding appeared to be scored. `result_summary` = "30 signals, 0 failed, 72 deferred (budget)", status `done`, **no alert**. What tipped it over: a Webull account (29 positions) joined `live_account_snapshots`, taking US batches 42 → 102/111 symbols; its snapshot is captured ~0.3s later so it sorted first and consumed the entire budget, starving every Robinhood holding (AVGO slot 55 → unscored 07-13 onward while Risk Analytics advised trimming it). `enqueueDeferred` cannot rescue a holding: `gatherSymbols` rebuilds holdings from the broker snapshot and `addCandidate` early-returns on `holdingSet`, so a deferred holding re-entered at the same starved index forever. **Ruled out:** hypothesis 1 (the `catch { return [] }` never fired — holdings were present and first in the batch); hypothesis 3 (no threshold/dedup drop — slots 1-30 persisted 30/30, incl. `neutral`); hypothesis 4 (nothing upstream consumed the budget; holdings genuinely led). Hypothesis 2 confirmed, with the *stable ordering* as the reason it never self-corrected. Note `f178ee8b`'s holding-symbols refactor (landed 16:19 UTC, **after** the 13:00 UTC run) only swapped capture-order for alphabetical — it changes *which* holdings starve, not *that* they starve. **FIX (both markets):** (1) holdings **staleness-ordered** least-recently-scored-first (`orderHoldingsByStaleness`, pure/deterministic, reads our own `agent_signals`) so the cut rotates — worst-case staleness bounded at `ceil(n/throughput)` runs instead of never; (2) `fetchHoldings` **fails loud** — checks PostgREST `error`, raises `research-holdings-fetch:us` (critical) and **throws** instead of degrading to "owns nothing" (scoring new BUYs while blind to positions needing a SELL is the worst possible degradation); (3) **India parity** — rejected Kite call → `research-holdings-fetch:india-kite` warn + continue on paper book (token lapse is recoverable), India paper DB fault aborts like US; (4) **deferred-holdings alert** naming every held symbol the budget missed — the only possible signal, since the queue can't carry holdings. Regression tests **verified failing on pre-fix code** ("promise resolved `[]` instead of rejecting"; 26/56 permanently starved under stable order). No migration, no schema change, no LLM on any scoring path. Gates: tsc + build + **vitest 569 passed / 6 skipped**. **OPEN — owner decision (design change, deliberately NOT built):** 56 holdings × ~30/run means *every holding every session* cannot fit one 150s `maxDuration`; closing it needs raised `RESEARCH_PARALLEL` (AV free tier is 5 req/min) or a dedicated holdings cron. Branch `worktree-agent-af7814d884620479a`, not pushed. |

| Router cutover prep + relationship-graph P0 adversarial review | Codex / GPT-5 | completed | 2026-07-16 | Fixed enforce-mode fail-open behavior and silent PostgREST failures; separated machine pass from owner review; bound evaluated intent sets; blocked legacy activation of router-enabled versions; implemented Option A as immutable per-session returns with explicit price basis and exact pair coverage; tightened owner-only RLS; corrected SEC KILL to provisional and reran enhanced 92-symbol probe with sample/file hashes. Prod migrations applied; router remains disabled both markets. Gates: tsc, build, 561 tests passed / 6 skipped. |

| Router cutover prep + relationship-graph P0 (KILL) + return-observation (held) | Claude / Opus 4.8 | completed | 2026-07-16 | Three parallel worktrees. **(1) Router cutover prerequisites** (`ff131807`, migration `20260716210000_router_cutover_prerequisites`): intent classification per market (`score_affecting`/`eligibility_affecting`/`narrative_only`/`unsupported`; `analyst.consensus` narrative-only US / unsupported India and excluded from gated intents so it can never block cutover); **runtime evidence-degradation guard shipped MEASURE-ONLY** (subtractive-only `long→neutral`, `short` untouched + schema CHECK, never suppresses an exit, defaults to abstain, only clean runs re-baseline, unparseable config → measure_only); frozen dual-run parity comparator (semantic axes compared BEFORE values ⇒ tolerance unreachable across TTM/quarterly/annual/forward or adjusted/unadjusted; cohort scorer reuses the PRODUCTION scorer and retains abstained/failed rows); activation RPC binds approval to candidate+baseline+evaluation ID+eval code version+strategy version+market+expiry; Massive bars adapter split into `massive-bars-v1`/`eodhd-bars-v1`/`twelvedata-bars-v1` with true per-source provenance + contract-version bump. **`router_enabled` verified FALSE both markets; zero evaluations exist ⇒ nothing can authorize a cutover.** Deferred (owner decisions): §7 numeric sample floors/tolerances/TTL/rollback SLO; the cohort BUILDER (deliberately — it's the piece that risks provider bursts and must be designed with reverse-shadow cache reuse); rollback drill; no API/UI. **(2) Relationship-graph P0 feasibility: KILL** — probed live SEC EDGAR over the real US universe (92 symbols → 79 domestic 10-K filers, 167 requests, ~0.9 req/s, $0): **named customer WITH revenue share = 5/79 (6.3%)**, 10 links, 5 suppliers; identity resolution 60% (a silent false positive BCRED→BX appeared immediately); PIT works (`acceptanceDateTime` 79/79, and it *differs from* `filingDate` — using filingDate would be wrong); freshness ~6.5 months stale. **Root cause is structural: GAAP requires the AMOUNT of a ≥10% customer, not the NAME**, and issuers take anonymity on exactly the highest-exposure links (NVDA 22%/14%, ENPH 39%, AMAT 19%, MU 17%, INTC 43%; the semis complex withholds universally). 4 of 5 survivors point at big-box retail. n=5 can't reach EdgeIC's `nObs>=12`. Falsifiable revisit: ~500+ covered US issuers; re-probe = 3 min. Deliverables `P0_COVERAGE_STUDY.md` + reproducible `scripts/sec-customer-coverage-probe.mjs`. No table, no migration. **(3) Return-observation contract: BUILT, MERGE HELD** — +0 provider calls (benchmark series hoisted + promise-deduped), 0.197ms/symbol, RLS + append-only + PIT enforced at the DB, beta and `beta_unmeasurable_reason` mutually exclusive by CHECK (a sector proxy cannot be written into beta). **Held because the table stores summary stats (vol/beta), NOT per-session returns — pairwise correlation is impossible from it, and the "re-derive from candles later" fallback is lookahead-by-construction (splits retroactively restate history; India candles aren't persisted). Table at 0 rows = the free moment to fix the shape. Awaiting owner decision: store frozen return series (~40MB/yr) vs summary-only.** Gates: tsc + build + **vitest 533 passed**. |

| Market-switcher retrofit adversarial review and remediation | Codex / GPT-5 | completed | 2026-07-16 | Reviewed `f178ee8b..f1c097c3` and fixed confirmed residuals: Agents India NAV still used $10k, India could invoke the US-only Trader proposal POST, embedded backtest labeling/market was ambiguous, market-scoped client fetches could race and show the prior market, three service-role history GETs lacked owner gates, and CSV parsing rewrote/accepted malformed exchange symbols. Production verified 249 intact lowercase watchlist rows, constraint/default/NOT NULL enforcement, zero suffix mis-maps, and nullable legacy-row handling. Trader POST remained byte-identical; PaperTrader/PositionMonitor routes untouched. 474 tests passed, 6 skipped; typecheck and production build green. |

| Market-switcher retrofit: audit all 33 pages + scope every gap (5 parallel worktrees, octopus) | Claude / Opus 4.8 | completed | 2026-07-16 | Audited all 33 dashboard pages + tabs (4 read-only auditors, file:line evidence). **Diagnosis: the multi-market retrofit was abandoned mid-way — everything built AFTER migration 057 (Smart Money, Brain, Backtest, Decision Review, Research Funnel, Scanner, Strategies, Portfolio, Risk) was already clean; everything BEFORE (Mentor, Trading, Activity, Intelligence-Analysis) was never revisited.** Six root-cause patterns, not 20 bugs: (1) *read-then-dropped* — page reads market then spends it on ONE query (Mentor 1-of-7, Learning weights-only, ScoreTracker watchlist-only) — passes code review, cross-sums currencies; (2) server components never read the `mkt` cookie (Trading/Activity structurally blind); (3) `"us"` hardcoded as a literal; (4) schema outran the queries (057/085 added `market`, the `.eq()` never followed); (5) parameterless API routes; (6) currency hardcoded at leaf render. **FIXED:** watchlist casing (GET read `'US'`/`'India'`, POST wrote lowercase → **US view showed 7 of 249 rows, 115 symbols hidden; India could never be non-empty**; the guarding CHECK had been dropped and theme-scout's omitted `market` inherited DEFAULT `'US'` = a third casing) → migration `20260716202749` normalizes all rows, restores the CHECK (verified it rejects `'US'`), DEFAULT→`'us'`; **3 currency cross-sums** (Learning NAV, Mentor equity/win-rate, ScoreTracker chart) — the hardcoded `10000` baseline made the ₹ book read **+9,789% instead of −1.10%**; Mentor thesis singleton was keyed only by time (served the other market's thesis — fixed FIRST or the rest no-ops); Trading/Activity now cookie-scoped with US-only surfaces (Robinhood queue, SPY bench) hidden/relabelled under India; routes `learner-brain`/`strategies/versions`/`trader`(GET)/evolution `learner_runs`/newsletters made market-aware — **nullable-market tables use `.or(market.eq.us,market.is.null)` so 1 live trade proposal + 5 newsletters aren't silently hidden**; Home briefing (server query AND `/api/briefing` route AND the Regen POST, which defaulted to `us`); chart/leaf currency sweep; symbol page keeps suffix-derived market (correct — a symbol belongs to one market) with currency threaded. **Left global (verified live: no `market` column):** `learning_log`, `feature_registry_history`, `learning_priors`, `llm_call_log`, `research_packets`; Settings/Admin/Vault/You/Features; `/dashboard/edges` renders both markets deliberately. **Deploy hazard hit:** an agent applied the watchlist backfill to prod while its code sat in a worktree — the old deployed filter then matched 0 rows and the watchlist showed empty until the code shipped (`72390347`). Merged tree: tsc + build + **vitest 452 passed**; no scoring/sizing/gating/order path touched. |

| Per-market cap + holdings research + score freshness + correlation construction | Codex / GPT-5 | completed | 2026-07-16 | Added mandate-owned per-market capacity (10) and score freshness (2 sessions), latest-live + paper holdings priority, DB canonical/tighten-only cap enforcement, stale-score fail-closed exits, Settings controls, tests, docs, and production migrations. Rejected correlation P0/P1 activation because candidate pair evidence is not persisted; corrected the architecture to require immutable PIT observations and shadow parity first. 446 tests, typecheck, build, production RLS/ACL/cap/no-force-close verification green. |

| Paper autonomy + Home/System Health remediation | Codex / GPT-5 | completed | 2026-07-16 | Enforced latched per-market controls, mandate thresholds, market-local sector and 10-name caps, no-average-down and notional gates in the row-locked fill RPC; made FIFO full/partial exits atomic and fail closed on lot drift; repaired mandate-based score exits and `opened_at` time stops; replaced stale Home policy/fill/live copy; made System Health deterministic and fail-closed. Three migrations applied and rollback-only production RPC tests passed with zero synthetic residue. 439 tests, typecheck, and production build passed. |

| Earnings-PIT contract + India Markets hardening + backfill-cron/weights (parallel worktrees, octopus) | Claude / Opus 4.8 | completed | 2026-07-15 | Three approved builds, merged. **Earnings-PIT data contract** (PEAD/revision *enabler*, data-capture only — no scored feature): `earnings_calendar` PIT columns + append-only `earnings_consensus_snapshots` vintages, `lib/data/earnings-pit.ts` (`eps_actual_first` filled once from Finnhub, never overwritten; consensus vintages on change; all carry `available_at`), capture wired into the earnings-refresh route, owner-gated coverage-report route. `post_earnings_drift` archetype → `pre_earnings_proximity_reweight_v1` (behavior unchanged; it was never real PEAD; `pead_*` reserved). Coverage 0 today (accrues forward). **India Markets hardening**: server-side `/api/markets/india` adapter (killed browser→Yahoo; capability-probed, fail-soft, paced), full NIFTY-50 breadth w/ versioned constituents + 80% coverage floor + `resolvedN/eligibleN`, honest states (loading/complete/partial/temporarily_unavailable/not_supported), India-only `india_market_snapshot` cache fully isolated from US `price_cache`, market-aware heading, 14 fixture tests. Crons `kairos-india-markets-fill` 10:15 + retry 10:45 UTC. Display-only. **Backfill cron** `kairos-symbol-profiles-backfill` 11:40 UTC (US+India). **signal_weights display** → per-market champion via new `lib/champion-weights.ts` across 6 readers; global table kept (learner writer still uses it). **Review catch:** the India migration shipped RLS *disabled* (anon-readable) — I enabled RLS + `authenticated`-read (`20260715162000`) and verified in prod. Merged tree: tsc + build + vitest **423 passed**; all 5 migrations + 3 crons verified in prod. |

| Decision Review + Stock Context + Markets ETF-cache fix (parallel worktrees, octopus-merged) | Claude / Opus 4.8 | completed | 2026-07-15 | Three approved features built in isolated worktrees + merged. **Decision Review** (`app/api/decision-review/route.ts`, `DecisionReviewPanel.tsx`, tab under Research Journal `?tab=review`): read-only counterfactual over `decision_observations × observation_labels` — score vs actual forward return; per-symbol attribution tagged `n=1 · ILLUSTRATIVE`; aggregate cohorts (per market, by direction/score-band) gated at a 20-matured floor (verified: US long=24 actionable, short=2 gated). No migration, no LLM. **Stock Context** (`symbol_profiles` table + migration `20260715150000`, RLS-on verified; `lib/data/symbol-profile.ts`, `lib/revisit-reason.ts` +12 tests, `StockContextStrip.tsx`, backfill route, `/api/symbol-profile`): name·1-liner·sector·exchange·cap-tier·next-earnings·≤3 peers on the symbol page. Off money path (one-way import), deterministic one-liner (no LLM), peers context-only, India peers `[]` (no free source). **Markets fix** (`app/api/agents/price-cache-fill/route.ts` + cron `20260715140000` verified in prod `cron.job` jobids 71/72): the Markets page tiles were empty because `markets/synthesis` burst 8 concurrent Massive calls → free-tier rate-limited → 5 null → degraded read cached all day. Fix: one **grouped-daily** Massive call fills all 31 ETFs into `price_cache` once/day (~1s); synthesis reads cache-first; macro "what this means" self-heals when stale; caught+fixed a volume float→bigint upsert bug. Display-only, off money path. Merged tree: tsc + build + vitest **377 passed**. `features/{decision-review,stock-context,markets-data}/FEATURE_ARCHITECTURE.md` updated; `features/markets-data/CODEX_REVIEW_PROMPT.md` written (gitignored, local handoff). |

| LLM-discretion exit hole closed (deterministic direction gate) | Claude / Opus 4.8 | completed | 2026-07-15 | Codex flagged (Claude verified at exact lines) the LAST place LLM output could move money: held-position exits. `research-agent.ts` accepted the LLM thesis `direction:"short"` on held names as an executable exit signal (stored `score_source=deterministic_v1`), LearnerAgent's `llm_exit` flag fired on `direction==="short"`, PositionMonitor executed it — so an LLM opinion could close paper positions and TEACH the learner from LLM-created outcomes. FIX: direction gate extracted to pure `lib/signal-direction.ts` — held exit is now `isHeld && analystScore < mandate threshold` (deterministic; SELL-on-holdings locked rule preserved, exit survives thin evidence); LLM direction is narrative/advisory ONLY (`_original_direction`); learner flag renamed `score_reassess_exit` (score-only); PositionMonitor honors legacy `llm_exit` to drain (0 rows pending). 9 new tests (`tests/signal-direction.test.ts`) prove LLM `short`/`long`/garbage can never open/close/flip a position. Historical contamination = ZERO (queried: no closed trade ever exited via llm_exit or long→short flip; direction_flip long→neutral = deterministic score decay). Entries were already deterministic; consumers already require `deterministic_v1`. Scoped tighter than Codex's plan: skipped taint-migration (nothing to taint) + skipped `llm_exit_review` ledger (YAGNI; `_original_direction` preserves opinion). tsc + build + vitest 365 green. |

| Supabase Security Advisor remediation + briefing/budget fixes | Claude / Opus 4.8 | completed | 2026-07-15 | **Security (emailed advisor):** 16 `public` tables had RLS **disabled** → readable via the public anon key (incl. `agent_config`/`learner_config` = attacker could alter agent behavior); several SECURITY DEFINER RPCs (`kairos_call_agent`, `activate_evidence_policy`, `create_evidence_policy_version`, `claim_provider_refresh_jobs`, `rls_auto_enable`, `handle_new_user`) callable by `anon` via default `GRANT EXECUTE TO PUBLIC`. FIX (`20260715120000_security_rls_and_rpc_lockdown.sql`, applied+verified via `get_advisors`): RLS deny-all on 15 agent-internal tables (service_role bypasses → agents/crons unaffected; verified only `newsletters` is browser-read, gave it an `authenticated`-SELECT policy); `REVOKE EXECUTE … FROM PUBLIC` on the anon-callable definer RPCs (`get_daily_ai_count` kept for `authenticated` — browser AI counter); pinned `search_path=public` on 15 definer/trigger fns. Advisor after: **0 ERROR, 0 anon-executable definer RPCs**. Deferred WARN: 7 always-true service policies (tighten at multi-tenant), `pg_net` schema, Auth leaked-password toggle, 2 pgvector RPCs. **Briefing (`60e3bcc`):** `extractTickers` regex lacked a leading boundary → `RELIANCE`→`IANCE` false ticker; anchored both sides + `{2,10}` + India/macro acronyms in IGNORE. **System Health:** provider-budget near-cap/exhausted alerts (AV 25/day, EODHD 20/day) re-fire daily on free-cloud-only = expected steady state (cache fallback) → reclassified WARN→INFO (emit sites + open rows). NFLX "failed" = 2nd same-day run on spent budget; already scored 57. Arch 04 + 08 updated. |

| Canonical Evidence Router Phase 2 + Webull data + health fixes | Claude / Opus 4.8 | completed | 2026-07-14 | **Webull data (Phase 0/1):** live cron-context contract probe (`/api/broker-mcp/webull/probe-data`) proved `category:US_STOCK` REQUIRED (bare→-32603), quarterly-fraction financials, forward-EPS array, empty-ok vs error no-data modes (fixtures AAPL/MSFT/JPM/SPY/no-data in `features/data-source-policy/PROBE_RESULTS.md`); FIXED dead `lib/data/webull-data.ts` (category + real parsers) — analyst line now live in thesis; `kairos-broker-keepwarm` cron (daily 06/18 UTC) so the OAuth refresh chain never lapses. **Reviewed** Codex's data-source-policy + external-research-integrations docs (approve-with-changes; killed local-Windows-worker + TA-Lib-first + parallel-provenance). **Router Phase 2 foundation (router_enabled=false, shadow-only, built in parallel worktrees):** `lib/evidence/contracts.ts` (sole provenance contract); migrations `20260714010000` policy (immutable versions + active pointer + rules + runtime_config + capability_status + append-only evaluations + activation RPC, seeded us/india v1 disabled), `20260714020000` cache (`evidence_cache_v2` + append-only `provider_call_ledger` + `provider_refresh_jobs` + claim RPC), `20260714030000` pacing-repair (reconciled untracked 176), `20260714040000` create-version RPC — all applied+verified; 4 provider adapters (Finnhub/Yahoo/Webull/Massive) + `registry.ts`; `resolver.ts` (deterministic, fail-closed, ≤2 sync attempts); policy APIs (`/api/settings/data-routing/*`); provider-health endpoint upgrade (null≠unlimited); 16 adapter golden tests (in CI); Settings→Data Routing Policy UI; dual-run `evidence-shadow` cron. **Proven live:** shadow US run resolved 28/29 fundamentals + 28/29 analyst, wrote 65 cache rows + ledger (insider/bars low = Massive 5/min pacing, drains over ticks). **Ops fixes:** India prewarm ran 5h AFTER India research (→sentiment 0/8) rescheduled to 03:00; prewarm+shadow now 7-day with weekend backlog self-skip; India trading re-enabled (paper; will re-trip if accuracy stays <40%); `RESEARCH_CANDIDATE_CAP` 10→50. tsc+build+tests green throughout. **Phase 4 cutover NOT done — gated** on shadow-evidence accumulation + the entry-eligibility-flip guard (must cover organic outage, not just policy edits). No scoring/order/money path wired. |
| External research document authority and provenance correction | Codex / GPT-5 | completed | 2026-07-14 | Applied Claude review: external-research `FEATURE_ARCHITECTURE.md` is the sole future implementation authority; lineage explicitly extends existing `experiment_runs` and links immutable `evidence_records` plus Router policy/cache/ledger references rather than creating a third provenance store. Catalog/deep-dive/audit are intake-only. Design-only. |
| Deep external repository capability and adoption audit | Codex / GPT-5 | completed | 2026-07-14 | Added `features/external-research-integrations/DEEP_CANDIDATE_CAPABILITY_AUDIT.md` and a Claude review prompt. Evidence-rated upstream review separates the specific Kairos-native capabilities worth building (Qlib-inspired experiment lineage, Vibe/ML4T lifecycle and diagnostics, counterfactual journal, Pine export) from reference-only/rejected runtimes. No code, migrations, dependencies, credentials, deployment, or execution changes. |
| Vibe-Trading deep capability map + Kairos adoption boundaries | Codex / GPT-5 | completed | 2026-07-14 | Wrote `features/external-research-integrations/VIBE_TRADING_CAPABILITY_DEEP_DIVE.md` from upstream pin `6fc038d`: comprehensive capability map and Kairos adoption boundary. Prioritized native strategy lifecycle/decay, experiment diagnostics, Pine export, and counterfactual review; deferred a two-role advisory committee; excluded Vibe data, broker, MCP, shell, persistent-memory, generated-code, cross-market, and runtime surfaces. No code, migrations, dependencies, credentials, deployment, or execution changes. |
| External repository capability catalog + Claude review prompt | Codex / GPT-5 | completed | 2026-07-14 | Claude review refinements applied: explicit clean-room rule for every `reference_only` repository, 25-repository arithmetic documented (17 + 3 + 1 + 4), and Kairos-owned Qlib-inspired experiment lineage prioritized before any Vibe advisory candidate. The separate external-research architecture retains GH Actions `--network none` future-hosting rule. Design-only; no code, migrations, dependencies, credentials, deployment, or execution changes. |
| Governed External Research Integrations architecture | Codex / GPT-5 | completed | 2026-07-13 | Revised after Claude review: removed local-worker/TA-Lib-first build target, deferred sandbox framework until a genuinely untrusted integration, made Canonical Evidence Router `EvidenceEnvelope` the only provenance contract, and specified future GitHub Actions `--network none` compute with quota/no-paid-overage + trusted-wrapper constraints. Immediate next work is router Phase 2 foundation only, parallelized within phase behind `router_enabled=false`. Design only; no code, migrations, dependencies, credentials, deployment, or execution changes. |
| GitHub Quantitative Finance top-15 comparative scan | Codex / GPT-5 | completed | 2026-07-13 | Reviewed the ten unique repositories from the current top 15 after excluding Qlib, FinceptTerminal, Vibe-Trading, Qbot, and QuantDinger. OpenBB is architecturally useful but AGPLv3, so no direct adoption; TA-Lib and ML4T are the only contained research-worker and validation candidates. |
| GitHub FinTech top-15 comparative scan | Codex / GPT-5 | completed | 2026-07-13 | Reviewed the current GitHub FinTech topic page (most stars): Qlib, Hyperswitch, vn.py, FinceptTerminal, FinGPT, Vibe-Trading, Qbot, FinRL, Tushare, AI Berkshire, Lago, Akaunting, QuantDinger, Ghostfolio and Blockchain Guide. High-value references: Vibe-Trading, Qlib (research-only), AI Berkshire (methodology), QuantDinger (isolated architecture/security review). Conditional later: Ghostfolio UX/import ideas and Lago only if Kairos becomes a paid SaaS. No cloning, dependency, credential, deployment, or implementation changes. |
| Vibe-Trading agent-system comparative audit | Codex / GPT-5 | completed | 2026-07-13 | Found Vibe stronger in declarative research-team DAGs, adversarial synthesis, hypothesis-to-backtest evidence, TradingView Pine export, behavioral trade-journal/shadow counterfactuals, layered attribution, document ingestion, turnover-aware optimization, run artifacts, specialist tools, channels and runtime engineering. Kairos remains stronger in durable Supabase coordination, deterministic scoring/execution, per-market portfolio truth, promotion governance and money-path safety. Recommended a small advisory Research Committee plus isolated Quant Worker, not importing 30 teams or execution authority. |
| Vibe-Trading comparative architecture audit | Codex / GPT-5 | completed | 2026-07-13 | Read-only evaluation complete. Recommendation: retain Kairos as product/system of record and sole money path; integrate a pinned Vibe-Trading build only as an isolated, read-only quant research and validation worker. No FinanceOS implementation or external code copied. |
| Data Source Policy and Evidence Router architecture | Codex / GPT-5 | completed | 2026-07-13 | Wrote `features/data-source-policy/FEATURE_ARCHITECTURE.md` + `CLAUDE_REVIEW_PROMPT.md`: canonical intent router, Auto/prefer/only/off UX, immutable policy versions + active pointers, capability maturity and shadow score/entry-flip evaluation, durable cache/queue/ledger/pacing, explicit unknown quota model, Webull contract/parser fixes, security, rollout/rollback, and acceptance tests. Design only pending Claude/owner review; no code, migration, provider activation, scoring, or orders changed. |
| Webull data-source and Python-sidecar adversarial review | Codex / GPT-5 | completed | 2026-07-13 | Official docs + live read-only MCP contract probe confirmed 71 tools, but found shipped Webull research calls omit required category=US_STOCK, EPS parser misses array/est shape, financials parser cannot read period arrays, and the 6h cache is process-local. Recommended canonical evidence-intent routing with Auto/preferred/off Settings policy, centralized quotas/freshness/health, durable cache, and provider adapters; defer Python sidecar. No order tools or production source activation. |

| Research scheduling/rotation architecture review | Codex / GPT-5 | completed | 2026-07-13 | Design-only scheduler architecture completed: persistent class/SLA queue, deterministic re-look scoring, evidence-refresh workers, 80-candidate/day target model, market-day cycle finalization, enqueue/claim RPCs, telemetry, and phased rollout. Found stopgap defects: permanent manual rows can monopolize cap; prewarm mutates queue and counts pacing-denied attempts as warmed; split runs would break ranking/paper sequencing. Live DB verified 94 US/12 India queued, pacing RPC + prewarm jobs active but their migration SQL is absent from repo. No implementation or deployment. |
| Data-source reality verification (EDGAR/Yahoo/provider claims) | Codex / GPT-5 | completed | 2026-07-13 | Confirmed live Yahoo crumb responses for AAPL + RELIANCE.NS and SEC Company Facts coverage; found architecture overclaims around Yahoo permission/quotas, EDGAR "unlimited", uniform three-source redundancy, normalized metric equivalence, India failover, and cold-start pacing. Confirmed US Yahoo/EDGAR fallbacks are proposal-only, not wired. No implementation or deployment. |

| Full US/India pipeline verification and remediation | Codex / GPT-5 | completed | 2026-07-11 | Fixed leaked cron credential/Vault scheduling, stale-check auth/time math, US label candle fallback, learner market collision/timeouts, SEC insider classification + AV quota waste, paper-run market attribution, Supabase invoker views/search path, and vulnerable disabled PWA chain. Applied migrations 159–160; 45 labels matured; 281 tests/typecheck/build/audit green; no orders submitted. See `CODEX_US_INDIA_PIPELINE_FINAL_AUDIT.md`. |
| Automated strategy validation and shadow routing | Codex / GPT-5 | completed | 2026-07-12 | Implemented migration 170, owner per-market controls, in-process Learner validation, atomic one-slot shadow activation, and active Friday cloud retry. No champion, paper, or live activation. See `features/automated-strategy-validation/IMPLEMENTATION_RESULT.md`. |
| Benchmark-alpha architecture review and correction | Codex / GPT-5 | completed | 2026-07-13 | Rewrote `features/benchmark-alpha/FEATURE_ARCHITECTURE.md` design only: fixed live market/currency/book-scope provenance, benchmark priceability ledger, scorecard status rows, common-window rebasing, annualized info-ratio math, YTD/1Y confidence floors, Performance Truth fit, and Phase-2 learner/promotion/posture guardrails. No implementation code in this pass per prompt. |
| Capital rotation architecture review and correction | Codex / GPT-5 | completed | 2026-07-13 | Rewrote `features/capital-rotation/FEATURE_ARCHITECTURE.md` design only: made P0 shadow-only, required atomic paper sell+buy, two-leg live approval/reconciliation, PositionMonitor exit precedence, post-swap gate replay, score persistence, cost/tax/turnover definitions, benchmark-alpha fallback limits, and append-only lifecycle audit. No implementation code in this pass per prompt. |
| Benchmark-alpha + capital-rotation implementation | Codex / GPT-5 | completed | 2026-07-13 | Built benchmark-alpha P1 measurement and capital-rotation P0 shadow-only. Added migration `20260713143000_benchmark_alpha_rotation_shadow.sql`, scorecard math/route/UI, rotation evaluator + PaperTrader insufficient_cash shadow logging, docs/system map/tests/result note. Applied migration to Supabase `dionkikgdmlaotvtbnfr` and verified 2 benchmarks, 4 safe rotation configs, 4 live provenance cols, append-only trigger, and cron. Full tests 332 pass/6 skip, typecheck and build green. No learner promotion wiring, paper rotation execution, live proposals, or orders enabled. |
| 2026-07-13 money-path adversarial review and fixes | Codex / GPT-5 | completed | 2026-07-13 | Fixed fail-open per-market controls, atomic OAuth PKCE state consumption, MCP token/error redaction, live_performance owner-only RLS, allocator NaN/zero-sum handling, and docs/reality drift for Webull/pruning/migrations. Applied targeted RLS SQL to FinanceOS Supabase `dionkikgdmlaotvtbnfr`; `db push` intentionally skipped because remote migration history has many out-of-band versions. 322 tests, typecheck, build, audit green. |
| US/India 10/10 readiness program | Codex / GPT-5 | in_progress | 2026-07-11 | Added provider exhaustion/stale-cache tests, broker lifecycle simulations, atomic promotion, market-local risk/budget windows, free-tier caps, readiness ledger, and market-separated US/USD + India/INR shadow evidence. Fixed Claude follow-up regressions: nonexistent signal column, swallowed query errors, India quote adapter, and observation-confidence source. Remaining: Monday soak/evidence accumulation, model artifacts, and owner-approved broker canaries. |
| Risk Analytics US/India + Robinhood multi-account verification | Codex / GPT-5 | completed | 2026-07-11 | Replaced dead private-REST holdings capture with six-account Robinhood MCP capture/pricing; persisted canonical snapshots; republished six live daily risk runs; fixed stable-vs-wire account IDs, partial failures, paper fallback, read-only badges, P&L signs, delta ordering, loss-proxy honesty, held ETF/asset classifications, and India credential/error copy. Browser US/India/buttons/tabs verified; 302 tests, typecheck, and build green. See `CODEX_RH_MCP_MULTIACCOUNT_ARCHITECTURE_RESULT.md`. |
| Agents, Markets, US/India Live Portfolio, Research Journal end-to-end audit and remediation | Codex / GPT-5 | completed | 2026-07-12 | Live browser + Supabase contract audit; upgraded stock research journal to evidence-attributed thesis/score rationale; fixed confirmed page and cross-flow defects. |
| Research Journal novice-first evidence and context upgrade | Codex / GPT-5 | completed | 2026-07-12 | V2.0–V2.1 shipped: asset-aware identity/history, score/coverage/confidence separation, plain-English action, sector/external links, hype/technical interpretation, downstream-block reasons, detailed audit, and owner-gated on-demand cached current news. US/India browser verified; 305 tests, typecheck and production build green. V2.2 point-in-time peers/themes remains measure-only architecture follow-up. |

| Daily Per-Holding Risk Analytics (full build) | Claude / Opus 4.8 | review | 2026-07-11 | Built the approved feature end-to-end. **Engine** `lib/risk/holding-risk.ts` (`computeHoldingRisk`, formula `hr-v1`): deterministic 0–100 risk-control pressure index (caps name30/sector20/vol-beta15/cluster15/drawdown10/liquidity10) + posture with strict precedence (verified stop/thesis-break→exit_review; drawdown ALONE never; hard concentration/cluster breach→trim; confidence<0.5→review; else hold). LLM writes `strategy_note` ONLY — cannot change score/posture/action. `lib/risk/correlation.ts` aligned-return clusters. **Schema** migrations 154 (3 append-only tables `holding_risk_runs` lifecycle-guarded + `holding_risk_snapshots`/`account_risk_snapshots` UPDATE+DELETE-blocked, owner-email RLS, anon REVOKEd), 155 (claim/publish RPCs), 156 (pg_cron US 21:30 UTC / India 11:00 UTC) — **all applied + verified** (tables, 3 triggers, 6 RLS policies, both cron jobs active). **Cron** `POST /api/agents/holding-risk?market=` fails closed on missing/stale snapshot. **Read API** `GET /api/portfolio/risk-daily` (owner-RLS, complete runs only, same-formula Δ). **UI** `PortfolioRiskPage.tsx` Daily Per-Holding Risk panel: per-account tabs, cadence header, per-holding score chip + posture pill + Δ1d + driver breakdown + advisory note. **ADVISORY-ONLY — wired to NO order path for ALL accounts** incl. read-only 965848641; no cross-currency roll-up. Docs: arch 04/05/08 + system-map.json (ACCTBOOK→HOLDINGRISK→RISKDASH + history). Gates: tsc 0 · vitest 281 pass/6 skip (33 new holding-risk/correlation) · build ok (/dashboard/risk 8.1 kB). No live/autonomous enablement; no real order placed/previewed/canceled/modified. |
| Holding-risk-daily architecture review | Codex / GPT-5 | completed | 2026-07-11 | Updated `features/holding-risk-daily/FEATURE_ARCHITECTURE.md`: fixed append-only/upsert conflict, account/currency provenance, Kite identity, score semantics, risk-vs-alpha action boundary, stale/missing data, idempotency, RLS, failure behavior, and acceptance tests. Documentation only. |

| Phase B P0 re-audit remediation (Codex#1-6) | Claude / Opus 4.8 | completed | 2026-07-11 | Fixed all 6. #1 CRITICAL migration **153** drops `p_broker` from advisory-lock key → market-wide scope match. #2 CRITICAL `verifyKiteTradingIdentity()` binds Kite `/user/profile` `user_id` to `active_account_india`. #3 HIGH gate lives at `placeEquityOrder` choke point (all Kite paths). #4 HIGH 153 rejects non-finite qty/notional/caps + validates side/env/broker/symbol/order_type. #5 MED added ACK live-path test (`attempts()===3`, 202, broker-id-in-alert) + `attempts()===1` on broker-reject. #6 LOW fix-log wording. Migration 153 verified applied. arch-08 + system-map.json updated (also repaired 8 pre-existing raw ctrl-chars in diagram). Gates: tsc 0, vitest 248 pass/6 skip, build OK. No real order placed; autonomous/live OFF. |
| Phase B P0 remediation adversarial re-audit | Codex / GPT-5 | completed | 2026-07-11 | Wrote `CODEX_PHASE_B_REMEDIATION_REVIEW_RESULT.md`. Found CRITICAL cross-broker budget-lock race and incomplete Kite token/account identity binding across standalone/canonical paths; HIGH incomplete RPC finite/input validation; A3 focused tests and tsc pass; A4 and edge cleanup clean. Live DB checked read-only. |

| 2026-07-14 Codex handoff adversarial review and remediation | Codex / GPT-5 | completed | 2026-07-15 | Reviewed `179b480...bf6b9a85` per `CODEX_SESSION_REVIEW_AND_ROADMAP.md`. Fixed Router fail-open activation, policy/runtime lookup failure handling, missing durable pacing, provider provenance loss, market-invalid provider choices, route-wide research deadline/deferred-alert logic, silent queue write errors, score-history date crashes, and briefing all-caps false tickers. Applied live FinanceOS migrations `evidence_router_acl_and_provenance` + `evidence_router_table_acl`: Router RPCs/service pacing are service-role-only; router tables have no anon or authenticated-write grants; both market policies remain `router_enabled=false`. Verified `npx tsc --noEmit`, production build, and Vitest (356 passed, 6 skipped). No scoring, position, cash, order, or trade path activated. |

> **All agents check this before starting work. Claim your task. Update when done.**
> Format: `| Task | Agent/Model | Status | Date | Notes |`

---

## 🚧 Session 2026-07-11 — Codex full-review remediation (Phase A P0)

| Task | Agent/Model | Status | Date | Notes |
| --- | --- | --- | --- | --- |
| Phase A P0 remediation (kill-switch book/account, Kite unify, durable ACK, PositionMonitor NAV, budget RPC grants) | Claude Opus 4.8 | review | 2026-07-11 | From 07_08_FULL_APP_REVIEW.md. **A1 (kill-switch book/account), A3 (durable broker ACK), A4 (PositionMonitor NAV accounting), A5 (budget-RPC grants + v2 session, applied) DONE. A2 (canonical Kite path) DEFERRED — needs architecture approval.** Gates green: tsc clean · vitest 243/6 skip (+5 Test 9c) · build ok. DB verified: v1/v2 RPC grants = {postgres,service_role} only; paper_portfolio has no open_positions col; live_account_snapshots schema-backed. Evidence: `07_11_CLAUDE_FULL_APP_FIX_LOG.md` + `07_11_P0_ACCEPTANCE_EVIDENCE.md`. No live/autonomous enablement. STOPPED after Phase A per prompt. |
| Phase B remediation (residual P0 items under owner "go fix all") | Claude Opus 4.8 | review | 2026-07-11 | Closes the Phase-A residuals. **A3 injected-failure integration test** (`tests/execute-order-ack.test.ts`, 4/4: 3-attempt ACK retry → 202 needs_reconcile, exactly-once broker submit, CRITICAL alert). **A2 additive identity/allowlist gate** on `app/api/kite/order/route.ts` — reads `strategy_config.active_account_india`, requires `broker_accounts{kite,india,trading}`, fail-closed 403/500 (no contract change; no kite row exists today so India live is blocked until owner adds one — intended). **A4 read-only NAV reconciliation report** `GET /api/paper/nav-reconcile` (owner-gated, zero writes; surfaces current drift US $61.28 / India ₹51,505.90). **A5 tz-boundary read-only proof** of v2 market-local session date + advisory-lock concurrency note; migration-152 tracker note corrected (tracked `20260711135037`). **Deleted** dead edge-fn `supabase/functions/_shared/kill-switches.ts`. Gates green: tsc clean · vitest 247/6 skip (+4) · build ok. Canonical Kite-path *unification* + correcting the 2 drifted NAV pools remain owner-gated. No live/autonomous enablement; no real order placed/previewed/canceled/modified. Evidence updated in both 07_11 docs. |

---

## Status Key
- `planned` — approved, not started
- `in_progress` — actively being worked on (DO NOT start if another agent claims this)
- `blocked` — waiting on dependency or Vaibhav decision
- `review` — done, needs Vaibhav or Reviewer check
- `completed` — shipped

---

## ✅ Session 2026-07-09 — Risk gates + Performance Truth Layer (all built)

| Task | Agent/Model | Status | Date | Notes |
| --- | --- | --- | --- | --- |
| Re-entry cooldown | Claude Sonnet 4.6 | completed | 2026-07-09 | Blocks same-symbol BUY for 5 calendar days (≈3 trading days) after position closes. Applied in paper-trade route after sector-cap check. `cooldownCutoff = now - 5d`, counts closed paper_trades with order_side=buy, market-aware. |
| Pyramid gate | Claude Sonnet 4.6 | completed | 2026-07-09 | If open position exists and fillPrice ≤ avg_cost → revert claim + skip with reason "pyramid_gate". Blocks averaging down into a losing position. Only allows adding to winners. |
| Time stop | Claude Sonnet 4.6 | completed | 2026-07-09 | PositionMonitor closes positions older than champion genome's `horizon_days` (default 10). `loadChampionGenome()` per market, cached in `horizonDaysByMarket`. Runs before score-exit check. Prevents slow bleeds that overstay the swing window. |
| Partial profit-taking (B) | Claude Sonnet 4.6 | completed | 2026-07-09 | At price target, close floor(qty/2) shares, move stop_loss to avg_cost on remainder (breakeven). Credits half-proceeds to cashByMarket. Only when qty ≥ 2 (single share = full close). FIFO lot close on paper_trades. |
| NAV drawdown circuit breaker (A) | Claude Sonnet 4.6 | completed | 2026-07-09 | PositionMonitor fetches paper_performance for each market, computes weekly NAV return. If < -5%, sets app_paused=true in strategy_config + fires critical System Health alert `nav-drawdown:<market>`. |
| Benchmark sync (C) | Claude Sonnet 4.6 | completed | 2026-07-09 | PositionMonitor fetches VOO (US) and ^NSEI (India) price daily via getQuote(), upserts paper_performance.bench_nav. Alpha now compares against live benchmark NAV. |
| P1 gate cron | Claude Sonnet 4.6 | completed | 2026-07-09 | Weekly Sunday 02:00 UTC cron at /api/agents/evaluation/p1-gate/cron. Counts closed evaluable paper_trades per market. ≥ 20 → reportIssue(`p1_gate_ready:<market>`, info). < 20 → resolveIssue. Added to vercel.json crons array. |
| P0 Performance Truth Layer | Claude Sonnet 4.6 | completed | 2026-07-09 | Migrations 133/134/135 applied (investment_mandates, strategy_evaluations append-only, mandate_id FK on agent_signals/paper_trades/decision_observations). lib/evaluation/run-evaluation.ts (pure deterministic math, no LLM). /api/agents/evaluation/run + /results + /mandates routes (owner-gated). PerformanceTruth.tsx additions: mandate selector + Run Eval button + eval history table. eligible_for_live_review advisory only — never read by broker gateway. |
| TypeScript scope fix | Claude Sonnet 4.6 | completed | 2026-07-09 | position-monitor route.ts: `market` variable was declared inside `if (pos.created_at)` block (time stop), not visible to partial profit-taking block below. Hoisted to top of position for-loop. Build was failing `Cannot find name 'market'`. |

---

## ✅ Session 2026-07-10 — Scoring P0/P1/P2 + Live PA0 schema/UI/audit

| Task | Agent/Model | Status | Date | Notes |
| --- | --- | --- | --- | --- |
| Scoring + live-auto architecture hardening review | Codex / GPT-5 | completed | 2026-07-10 | Corrected four architecture docs. Fixed wrong RH account, Gateway bypass, non-atomic auto budget, fallback NAV, missing exit/reconcile prerequisite, scoring confidence math, rank-universe design, LLM boundaries, calibration leakage, lifecycle/promotion, and ledger semantics. Added Claude staged verification/build prompt. Documentation only; no production code. |
| Scoring P2 — shadow setup experts (6 archetypes) | Claude Sonnet 4.6 | completed | 2026-07-10 | Migration 138: `policy_version_id` dropped NOT NULL on shadow_decisions; `setup_type text` col + index added. lib/scoring/archetypes.ts: 6 archetype configs (quality_momentum/value_inflection/post_earnings_drift/etf_trend/india_quality_momentum/india_sector_rotation) with prior weights + router + computeArchetypeScore. ResearchAgent: after existing shadow_versions block, calls routeToArchetypes() + inserts archetype shadow rows (policy_version_id=null, setup_type=archetype.id). All archetypes score in shadow; only champion v1 creates actionable signal. Build green. |
| Scoring P1 — universe snapshots + cross-sectional rank (measure-only) | Claude Sonnet 4.6 | completed | 2026-07-10 | Migration 137: universe_snapshots + universe_snapshot_scores tables. Cron creates PIT snapshot before parallel workers, passes snapshot_id to each processSymbol call. Post-run: computes percentile rank (0=worst, 1=best), inserts universe_snapshot_scores. decision_observations.universe_snapshot_id populated. features blob adds schemaVersion:'v1' + decisionTs. No paper/live change. Build green. |
| Scoring P0 — provenance safety + mechanical direction gate | Claude Sonnet 4.6 | completed | 2026-07-10 | Migration 136 applied: `score_source`+`scoring_version` on `agent_signals`; 10 summary cols on `decision_observations` (with NOT VALID range guards); 3 new lifecycle states on `strategy_versions` (`measure_only`, `live_review_eligible`, `live_approved`). Code: ResearchAgent writes `score_source='deterministic_v1'`+`scoring_version='v1.0'` + `evidence_confidence` on every signal+obs insert; mechanical direction gate replaces LLM direction for non-exit signals (score≥threshold→long, else neutral). DeepSeek writes `score_source='llm_advisory'`. PaperTrader + TraderAgent: `.or("score_source.is.null,score_source.neq.llm_advisory")` structural exclusion gate added. Build green. Deployed. |
| Live PA0 — schema, audit RPC, Settings UI (deployment flag stays false) | Claude Sonnet 4.6 | completed | 2026-07-10 | Migration 139: strategy_config +8 live_auto_* cols (all false/null/conservative defaults, NOT VALID range guards); trade_proposals +4 cols + status constraint expanded (queued_auto, manual_review_required); broker_order_events append-only table + boe_block_mutation() UPDATE/DELETE trigger. Migration 140: reserve_live_order_budget_v2 RPC (adds p_execution_actor param; approved_by_user=(actor='owner'); fixes v1 budget gap — counts unknown_needs_reconcile+partially_filled; REVOKE public/anon/authenticated GRANT service_role). lib/autonomy.ts: AUTONOMOUS_LIVE_ENABLED=false const. app/api/settings/live-auto/route.ts: GET returns config+deployment_flag_active; PATCH enable (requires AUTONOMOUS_LIVE_ENABLED=true+confirmation_text='ENABLE AUTO'+lease_hours 1-24, journal write before config); disable; update_caps (min_confidence floor≥0.6). Settings page agents tab: LiveAuto card with deployment flag status, DB toggle+lease display, typed confirmation enable panel, disable button, caps inputs. Build green. |

---

| Docs: system-map.json + arch chapters for PA0-PA2 | Claude Sonnet 4.6 | completed | 2026-07-10 | system-map.json: AUTOSHADOW + SHADOWPROP nodes + edges + history. docs/arch/04-database-schema.md: live_auto_* cols on strategy_config, trade_proposals autonomous cols, broker_order_events table, migrations 136-140. docs/arch/05-crons-and-scheduling.md: autonomous-shadow Vercel cron. docs/arch/08-risk-and-safety.md: PA2 sizing table + PA3 blocker noted. |
| Live PA2 — sizing kernel + budget dry-run in shadow path | Claude Sonnet 4.6 | completed | 2026-07-10 | execution-kernel.ts: computeAutonomousSizing() — no-fallback NAV (4h stale = fail closed), Kelly from paper_trades (≥10 closed), per-order cap clamp, floor 2%, qty rounds down (< 1 = no size). autonomous-shadow.ts: fetches live_account_snapshots NAV (account 605420660 read-only), paper_trades for Kelly, getQuote() per qualifying signal, budget dry-run (informational: broker_orders spend vs daily cap). queued_auto proposals now have qty/estimated_value/pct_of_nav/price_at_proposal. Sizing failure → downgrade to manual_review_required. Note: broker preview+submit NOT implemented — route handlers cannot call Robinhood MCP (TraderAgent itself documents this limitation at line 429). Actual execution remains owner-triggered via MCP session. Build green. |
| Live PA1 — shared execution kernel + shadow autonomous decisions | Claude Sonnet 4.6 | completed | 2026-07-10 | lib/trading/execution-kernel.ts: pure evaluateAutonomousExecution() with 9 ordered gates (deployment_flag/db_toggle/lease/long_only/score/confidence/max_positions/max_orders/notional). lib/trading/autonomous-shadow.ts: runAutonomousShadow() — snapshots policy, queries qualifying signals (deterministic, long, score≥threshold, last 24h), creates trade_proposals (execution_mode='autonomous_shadow'), runs kernel, updates status to queued_auto or manual_review_required, journals run. Routes: POST /api/agents/autonomous-shadow/run (requireOwner), POST /api/agents/autonomous-shadow/cron (CRON_SECRET). Cron: weekdays 07:30 UTC. Gate 1 always fires in current deployment (AUTONOMOUS_LIVE_ENABLED=false) — all proposals land manual_review_required intentionally. No broker calls, no budget reservation, no order submission. Build green. |

---

## ✅ Session 2026-07-10 (cont'd) — Phase 1 P0 audit fixes

| Task | Agent/Model | Status | Date | Notes |
| --- | --- | --- | --- | --- |
| Phase 1 P0 fixes — Codex audit 4.2/10 → 8 confirmed findings repaired | Claude Sonnet 4.6 | completed | 2026-07-10 | Migration 147 (schema reproducibility). 7 code fixes: conviction/100 confidence normalization (was always 0, blocked all autonomous signals); L4 autonomousWorkerAllowed() gate in execute-order.ts; India USD-cap isolation in execution-kernel.ts; duplicate SELL atomic claim in live-exit-monitor.ts; cancel-on-kill BUY-only in sync/route.ts. F2 (RPC security) already fixed in prod (anon_can_exec=false confirmed). F6 (kill-switch live data) deferred — AUTONOMOUS_LIVE_ENABLED=false, blocking prerequisite documented in POST_UPGRADE_FIX_LOG.md. Build green. |

---

## 🔴 In Progress

| Task | Agent/Model | Status | Date | Notes |
| --- | --- | --- | --- | --- |
| Paper-trade dedup and rotation adversarial audit | Codex / GPT-5 | completed | 2026-07-22 | Fixed pre-dedup query truncation, lingering duplicate retries, silent query failures, current-run persistence self-counting, cross-market rotation counters, and hedge-source selection. Found US incomplete P1 rotation enabled in production; disabled it, added DB OFF constraint + deployment gate, removed old money-moving RPC, and replaced it with a no-write exact-claim verification stub. Migration applied/verified. |
| ATR paper-exit evidence pipeline | Codex / GPT-5 | completed | 2026-07-22 | Added prospective point-in-time ATR labels, ATR-normalized MAE/MFE, three versioned close-observed candidate outcomes, market-local authenticated evidence reporting, and sample gates. Fixed pre-existing oldest-page label starvation. Production migration applied and verified RLS-on with zero execution dependencies; paper/live/broker behavior unchanged. |
| 2026-07-11 current-tree full app adversarial review | Codex / GPT-5 | completed | 2026-07-11 | Reviewed current commit `cbf8617`, live Supabase read-only state, US/India pipelines, scoring/learning, manual/autonomous money paths, providers, UI, security, and reliability. Wrote `07_08_FULL_APP_REVIEW.md` and `CLAUDE_CODE_07_11_FULL_APP_FIX_PROMPT.md`. Found P0 kill/SELL semantics, unscoped Kite account, post-submit durability, broken paper NAV projection, exposed v1 RPC, and evidence-empty learning. 238 tests pass/6 skipped; typecheck fails; localhost returned 500. |
| Post-remediation deep quant/system re-audit | Codex / GPT-5 | completed | 2026-07-10 | Wrote CODEX_REMEDIATION_REVIEW_RESULT.md, CODEX_POST_UPGRADE_DEEP_REVIEW_RESULT.md, and CLAUDE_CODE_POST_UPGRADE_FIX_PROMPT.md. Found incomplete migration/RPC grants, renewed zero-confidence dead-end, missing L4/live-approved gates, India cap mixing, paper-based live kill switches, unsafe/US-only exits, unofficial RH REST, discovery/formula/evolution gaps, and meme reversal false-positive. 142 tests pass; build timed out unverified. |
| Full-system adversarial audit | Codex / GPT-5 | completed | 2026-07-10 | Wrote CODEX_FULL_SYSTEM_AUDIT_RESULT.md. Found autonomous gateway bypass, missing on-disk migrations 139/140, broken signal confidence query, India currency/NAV mixing, no live exit lifecycle, learning/calibration defects, API/RLS/vault gaps, and material docs drift. 116/116 tests pass. |
| Momentum factors + trade-behavior mirror adversarial review | Codex / GPT-5 | completed | 2026-07-10 | Wrote CODEX_MOMENTUM_BEHAVIOR_REVIEW_RESULT.md. Found Feature Registry is log-only, current archetypes are static reweights rather than momentum models, EdgeIC retains survivorship/horizon-selection risk, fade premise is unproven, and trade CSV/enrichment cannot support behavioral claims before parser/episode/outcome repair. Added exact falsification test and revised phased recommendation. |
| Edge/Factor broaden IC evidence (measure-only) | Claude/Opus 4.8 | completed | 2026-07-09 | Curated static liquid universe (lib/edges/universe.ts, US+India, labeled NON-PIT/survivorship) + deeper multi-year candle history + universe/offset/historyDays params on edge-scout & edge-ic so the broad list is processed in bounded, cached, paged slices. Still measure-only; no trading/scoring/sizing change. Purpose: make IC trustworthy before P2. |
| Edge/Factor Discovery P1 IC gate (measure-only) | Claude/Opus 4.8 | completed | 2026-07-08 | DONE + deployed (commit f2cc41a). lib/edges/ic.ts (rank-IC vs forward returns by horizon, Newey-West t for overlapping returns, IR, lifecycle classifier) + app/api/agents/edge-ic (bounded measure-only) + IC scorecard on /dashboard/edges. Verified live: 24 edge_ic_history rows (8 edges×3 horizons); classifier flagged momentum/trend/rel-strength shadow_eligible (t≥2), weak edges measure_only, st_reversal benched_negative@20d; trading tables unchanged (98/6/11/2). Honest caveat: illustrative not proof (30-name tech-heavy universe, ~50 recent dates, survivorship). Evidence in features/edge-factor-discovery/P0_BUILD_RESULT.md (P1 section). P2 NOT built. |
| Edge/Factor Discovery P0 (measure-only) | Claude/Opus 4.8 | completed | 2026-07-08 | DONE + deployed (commit be06584). migration 132 applied+verified (edge_catalog, edge_universe_members, edge_signals, edge_signal_inputs, edge_ic_history). lib/edges/* (8 price/volume edges, cross-sectional z, bounded cached candle resolver). app/api/agents/edge-scout (owner-or-cron, measure-only, bounded maxSymbols/maxDays, idempotent, provider report). Read-only /dashboard/edges catalog page. NO change to analyst_score/agent_signals/paper fills/sizing/live orders (verified). Build+typecheck green (51/51). Evidence + verification table in features/edge-factor-discovery/P0_BUILD_RESULT.md. Verified live: bounded run wrote 25 universe + 154 edge_signals (8 edges) + 154 PIT input rows; rerun idempotent (still 154); trading tables unchanged (agent_signals 98, paper_trades 6, paper_order_events 11, broker_orders 2 before AND after). P1 (IC gate) NOT built. |
| 07/08 review fixes (P0+P1+P2 auth/durability) + strategic autonomy ladder (S1) | Claude/Opus 4.8 | done | 2026-07-08 | P0-1..P0-5 verified fixed (auth gates + fail-closed override audit + reproducible migs 121/122). P1-1 durable RAG re-ingest (mig 123 + lib/rag/ingest.ts). P1-2 enrich owner-gate. P1-3 alerts auth + lib/alerts/emit.ts. P2-2 admin LLM APIs auth. P2-4 watchlist GET auth. S1 autonomy ladder: mig 124 `autonomy_level` (default L3_live_manual) + lib/autonomy.ts + both live gateways enforce `liveOrdersAllowed` above trading_enabled; L4/L5 defined-but-not-honored (AUTONOMOUS_LIVE_ENABLED=false, owner-click still required). P1-5/P2-1/P2-3/P2-5 + Builds 2–6 = DEFER_STRATEGIC (documented, not overbuilt). Full classification in 07_08_CLAUDE_FIX_LOG.md. Build passes. |

---

## ✅ Session 2026-07-08 (cont'd) — Strategic Report Tier-3 + Tier-4 (all built)

> User re-supplied VOYAGE_API_KEY + WANDB_API_KEY and said "go" to build ALL remaining tiers with my defaults (pgvector not Qdrant; reuse WandB Weave not a separate OTel vendor; native typed layers not heavyweight frameworks — no new signups). Every RAG/observability feature is env-gated: absent key → graceful no-op, never throws.

| Item | What was built | Migrations |
|---|---|---|
| #10 Trade-history RAG | `lib/rag/embeddings.ts` (Voyage voyage-3.5, 1024-dim, graceful-null) + `lib/rag/trade-memory.ts` (`buildSetupDocument`/`indexClosedTrade`/`retrieveSimilarTrades`/`summarizeMemories`). `trade_memories` table + `match_trade_memories` RPC (hnsw cosine). Wired into ResearchAgent thesis prompt ("prior similar setups & outcomes", cross-symbol, same market) + indexed at close time in position-monitor & learner. Tainted/excluded trades never enter corpus. `/api/agents/rag-backfill` bootstraps corpus. | 118 |
| #9 Reranker | `lib/rag/rerank.ts`: Voyage **rerank-2** (reuses VOYAGE_API_KEY — no HuggingFace). Two-stage: ANN over-fetch 20 → rerank → top-k. Identity fallback when disabled. | — |
| #11 Binary context filter | `lib/rag/contextual.ts` wires `filterChunksByTicker` (entity guard) into the document-RAG path + traces the keep/reject decision. Trade-memory path is cross-symbol by design (no ticker filter). | 119 |
| #12 Contextual Retrieval | `lib/rag/contextual.ts`: per-chunk LLM context header (Anthropic contextual retrieval) via cheap `summarize` tier; graceful passthrough on failure. | — |
| #13 Vector DB | **pgvector 0.8.0** in existing Supabase (no Qdrant). `trade_memories` + `doc_chunks` embedding columns, hnsw indexes. | 118, 120 |
| #14 Tracing | `lib/observability/weave.ts`: `traceRag()` writes durable `rag_traces` (source of truth) + mirrors to WandB Weave when WANDB_API_KEY set. Every retrieve/rerank/filter/index op traced. | 119 |
| #15 Orchestration | `lib/agents/graph.ts`: native typed `StateGraph` (nodes/edges/conditional routing/shared state/`onStep` checkpoint seam) modeling LangGraph without the dep. | — |
| #16 Ingestion | `lib/rag/ingest.ts`: LlamaIndex-style pipeline (chunk → #11 filter + #12 contextualize → embed → store) into `doc_chunks` + `retrieveDocChunks` (ANN→rerank). Sentence-aware splitter with overlap. | 120 |
| #17 A2A contracts | `lib/agents/contracts.ts`: versioned typed envelopes for every handoff (Signal→Decision→Fill→Outcome) + `validateMessage()` runtime guard. | — |

**Framework decisions (recorded):** #13/#14/#15/#16 chose native/existing-infra over new vendors (pgvector over Qdrant, WandB reuse over separate OTel, native StateGraph over @langchain/langgraph, native pipeline over llamaindex.ts) — same architecture value, no bundle bloat / cold-start hit / new signup. All keys already in Vercel Production + Preview.

**Strategic Intelligence Report: ALL 17 items now built (Tiers 1–4 complete).**

---

## ✅ Session 2026-07-08 — Strategic Report Tier-1 + Tier-2 (all built)

> User mandated building all unbuilt items from the Strategic Intelligence Report. This session completed Tier-1 (prompt caching, ticker filter, prompt versioning, DeepSeek data gate) and Tier-2 (Agent Evolution discovery_source, B3 structured triage, Data Provider Abstraction, Learning Integrity Phase 1B).

| Item | What was built | Migrations |
|---|---|---|
| #1 Prompt caching | `lib/llm-router.ts`: `callClaude`/`runClaudeAgentLoop` pass `cache_control: ephemeral` on system blocks; `LLMResult` tracks `cacheWriteTokens`/`cacheReadTokens`; cost formula uses 1.25×/0.10× cache rates | — |
| #2 Ticker filter | `lib/ticker-filter.ts` (NEW): `filterChunksByTicker()` + `chunkMentionsTicker()` — entity-level RAG chunk validation, strips exchange suffixes (.NS/.BO) | — |
| #3 Prompt versioning | `agent_config` gains `prompt_version/hash/notes/updated_at`; `agent_runs` gains `prompt_version/hash` | 113 |
| #4 DeepSeek data gate | `lib/research-agent.ts`: model = `claude-haiku` when `technical`/`fundamental` dims included, `deepseek-v4-flash` fallback when APIs returned nothing | — |
| #5 Learning Integrity Phase 1B | `paper_trades`/`broker_orders` gain 5 taint columns; `execute_paper_fill` RPC auto-stamps quality at fill time from `v_decision_quality`; `features.quality` structured per-dim state logged on every `decision_observations` row | 116, 117 |
| #6 Agent Evolution — discovery_source | `SymbolEntry` gains `discovery_source` type; all 9 sources tagged in `gatherSymbols()`; `decision_observations` gains `discovery_source` column + index; research inserts it per signal | 114 |
| #7 B3 structured triage | `health-triage` route now requests JSON `{summary, issues[{issue_key, severity, root_cause, blast_radius, suggested_fix}]}`; `health_triage` table gains `structured_issues jsonb`; GET returns it | 115 |
| #8 Data Provider Abstraction | `lib/data/provider-interface.ts` (NEW): `DataProvider` interface, `OverviewResult/CandleResult/NewsResult` types, `AlphaVantageProvider` implementation, `getProvider()`/`tryProviders()` registry | — |

**Remaining unbuilt (Tier-3 require external API keys, Tier-4 needs infra decisions):**
- Tier-3: gte-reranker (HuggingFace key), voyage-3.5 trade RAG (VOYAGE_API_KEY), WandB Weave binary filter (WANDB_API_KEY), Contextual Retrieval (depends on voyage)
- Tier-4: Qdrant, OpenTelemetry, LangGraph 1.0, LlamaIndex, A2A typed messaging

---

## ✅ Session 2026-07-07 (cont'd) — Agent Mind (all 3 phases) + learning_priors seed

> User approved the Agent Mind feature (surface what the agents believe, how it evolves, macro-to-holdings read). Also seeded/cleaned learning_priors.

| Piece | Notes |
|---|---|
| learning_priors cleanup + seed | Fixed a data bug (every prior was duplicated — 15 unique stored twice). Added 15 new source-tagged priors (12-1 momentum, low-vol, quality, earnings revisions, PEAD, credit spreads, ISM/PMI, real yields, VIX term structure, cycle rotation, cluster insider buying, breadth-over-depth, regime-first). 30 clean rows. |
| Migration 096 | learning_priors_history (belief-drift), unique(category,principle), macro_interpretations. All service-role-only. strategy_versions already had parent_version_id. |
| Migration 097 | Daily macro-read crons (us 9:30 ET, india 10:00 IST). |
| Phase 1 — Beliefs | /api/agent-mind/priors (GET/PATCH, owner-only) + Intelligence "Beliefs" tab. View/toggle/add priors; every change writes learning_priors_history + decision_journal. No LLM writes a belief. |
| Phase 2 — Brain | /api/agent-mind/brain (GET, owner-only) + Intelligence "Brain" tab: champion weights + why, belief drift, learner log, self-invented features, regime posture, track record, evidence-context banner (confidence shown next to N so small samples never read as authoritative). Pure DB reads. |
| Phase 3 — Macro read | /api/agent-mind/macro-read (GET owner / POST owner+cron) + Markets "What this means for your book" card. Cheap model over macro regime + holdings + macro priors; cached ≤1 call/day/market; advisory only, degrades to raw data if model down. |

Guardrails held: nothing here trades/sizes; no LLM mutates a belief; owner-gated; no hot-path market-data spend.

---

## ✅ Session 2026-07-07 (cont'd) — Codex review of Robinhood MCP: all 12 findings fixed

> External Codex adversarial review of the real-money path (CODEX_ROBINHOOD_MCP_REVIEW.md). Verdict: don't enable live orders until findings 1–8 fixed. All 12 + the AV note now fixed.

| # | Sev | Finding | Fix |
|---|---|---|---|
| F1 | CRIT | buildArgsFromSchema could emit valid-but-wrong order (esp. `amount`=dollars vs shares) | Dropped `amount` from qty aliases; enum-coerce side/type/tif to the schema's exact spelling (fail closed if unresolvable); verify `review_equity_order` preview ECHOES intended symbol/qty before place. |
| F2 | CRIT | robinhood_mcp_enabled not enforced on order path (only snapshot) | Enforced in Gateway (broker.id==robinhood_mcp) + inside the adapter (defense in depth). |
| F3 | CRIT | needsReconcile dropped → duplicate order after ambiguous timeout | Added to BrokerOrderResult; adapter preserves it; Gateway sets status `unknown_needs_reconcile` (HTTP 202); migration 095 adds that status to the active-order unique index + the friendly dup-check. |
| F4 | HIGH | risk-profile route only auth'd, not owner-gated | requireOwner() on PATCH + GET. |
| F5 | HIGH | SELL checked the read-only 965848641 snapshot, not the 605420660 trading account | For robinhood_mcp live sells, verify held qty via live MCP (robinhoodHeldQty) for the active account; fail closed if unverifiable. |
| F6 | HIGH | getActiveBroker silent fallback in order path | New getActiveBrokerForOrder (fail closed); Gateway uses it. |
| F7 | HIGH | refresh claimed CAS but did plain read/upsert | Real CAS on the refresh-token row's updated_at; loser re-reads; vaultSet now throws on write error. |
| F8 | HIGH | success with no order id treated as success | Live place with no parseable order id → needsReconcile, not success. |
| F9 | MED | allowlist lookups didn't filter by broker | Added .eq("broker", …) everywhere (adapter id robinhood_mcp → allowlist key 'robinhood'). |
| F10 | MED | OAuth state secret insecure fallback (cron/service-role/hardcoded) | Require OAUTH_STATE_SECRET ≥32 chars in prod; dev-only fallback; no cross-secret reuse. |
| F11 | MED | mcpRpc could misread partial/error responses as success | Require JSON-RPC id match + result present; treat tools/call result.isError===true as failure. |
| F12 | MED | guardOrderRequest cron bypass used plain `===` | Now verifyCronSecret (timing-safe, fail-closed). |
| note | LOW | AV budget counter failed OPEN on error | Now fails closed (serves cached) to protect the free-tier quota. |

Codex confirmed the notional-cap fallback (already fixed to fail closed) and the Kelly/trailing-stop/verifyCronSecret fixes are correct. Live orders still require: robinhood_mcp_enabled ON + all Gateway gates + human Approve/Send. First live order should be a tiny watched qty (no RH sandbox).

---

## ✅ Session 2026-07-07 (cont'd) — Robinhood MCP feature: unblocked parts (OAuth still pending)

> User approved building the parts of the Robinhood MCP feature that DON'T depend on Robinhood's real OAuth endpoints (which we don't have yet). The OAuth /login+/callback + live token acquisition + SDK transport stay stubbed until the user provides the endpoints/scopes. No order can reach Robinhood today: robinhood_mcp_enabled defaults off, isConfigured() is false with no token, and submitRobinhoodOrder() returns not-connected.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Migration 093 — scaffolding | Claude | 2026-07-07 | `broker_accounts` allowlist (service-role-only, seeded with today's two hardcoded RH accounts: 605420660=trading, 965848641=view_only); `strategy_config.active_account_us/india`, `live_account_source` (default claude_exec), `robinhood_mcp_enabled` (default FALSE), `max_order_notional`. |
| Migration 094 — dup-submit guard | Claude | 2026-07-07 | Partial unique index on broker_orders(proposal_id) WHERE active — hard backstop against the concurrent-double-click race. |
| Execution Gateway hardening (R6) | Claude | 2026-07-07 | app/api/broker/orders/route.ts now: requireOwner + guardOrderRequest (request-guards extended to read APP_BASE_URL so it works on Vercel), env must be explicit (no silent paper default), both-direction broker.envs check, checkKillSwitches at submit, fresh-quote notional cap (max_order_notional or 15% of live equity), price-drift re-check vs approval, sell-only-if-held, symbol/qty validation, fail-closed account-allowlist resolution. Benefits the existing Alpaca/Kite path too. |
| robinhood_mcp broker adapter (deterministic, stubbed) | Claude | 2026-07-07 | lib/robinhood-mcp.ts + lib/brokers/adapters/robinhood-mcp.ts + registry entry. Write path is a direct typed callTool sketch (R1: NO LLM ever) — currently returns not-connected. isConfigured()=hasRobinhoodToken() (false until OAuth stores a token). disconnectRobinhoodMcp() wipes locally regardless of remote reachability. Fail-closed account allowlist re-checked inside the adapter (last line before the wire). |
| Settings scaffolding | Claude | 2026-07-07 | Robinhood MCP card (status, Connect [disabled until oauth_ready], Disconnect, robinhood_mcp_enabled toggle, snapshot-source switch, US active-trading-account selector from the allowlist), + supporting owner-gated routes: /api/robinhood-mcp/status, /api/robinhood-mcp/disconnect, /api/broker-accounts. risk-profile PATCH/GET extended with the new fields (active_account_* validated against the allowlist server-side). |
| OAuth flow — UNBLOCKED & built | Claude | 2026-07-07 | User provided (and I verified against Robinhood's live well-known metadata) the real endpoints: auth https://robinhood.com/oauth, token https://api.robinhood.com/oauth2/token/, register https://agent.robinhood.com/oauth/trading/register, scope=internal, PKCE S256, public client (auth_method none), resource=…/mcp/trading. Built: dynamic client registration (RFC 7591), /api/robinhood-mcp/login (owner-gated, PKCE + HMAC-signed HttpOnly state cookie), /api/robinhood-mcp/callback (state verify + code exchange + vault token storage), refresh with expiry check. lib/robinhood-mcp.ts rewritten from stub to full OAuth + a deterministic MCP JSON-RPC client (initialize → tools/list → review_equity_order → place_equity_order), NO LLM in the write path. Arg schema discovered at runtime via tools/list; FAILS CLOSED if a required order field can't be mapped (no sandbox exists, so no guessing a real order). refresh-snapshot route now branches on live_account_source (cloud MCP writes the snapshot directly via service client, no loopback CRON_SECRET hop). oauth_ready=true; Connect button live. Still gated: robinhood_mcp_enabled default OFF + all Gateway checks + human Approve/Send. |

---

## ✅ Session 2026-07-07 — Security audit fixes + agent-system bug sweep

> Two parallel audits (security review of the Robinhood MCP feature vs. real code; agent-system + API-budget audit). Feature stays Draft/unapproved; these are the standalone bugs/vulnerabilities the audits surfaced in shipped code.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Vault + broker_orders RLS lockdown (migration 089) | Claude | 2026-07-07 | `api_key_vault` had a `USING(true)` policy for ALL roles + anon/authenticated write grants — anyone with the public anon key could overwrite/delete broker tokens without logging in. `broker_orders` had RLS disabled. Both now service-role-only. Applied live; non-breaking (all app access is service-client). |
| Owner-gated snapshot GET + open-redirect + timing-safe cron secret | Claude + agent | 2026-07-07 | `GET /api/live-account/snapshot` returned live equity/positions with NO auth → now `requireOwner()`. `/auth/callback` `next` param validated (blocked `?next=@evil.com` open redirect). New `verifyCronSecret()` (crypto.timingSafeEqual, fails closed on unset secret) applied across 25 cron routes. |
| Both Kelly sizing bugs | Claude | 2026-07-07 | paper-trade passed percent-scale caps into the fraction-based `lib/risk/sizing.ts` → every position pinned to the 2% floor once the calibrated model activated (conviction scaling dead). trader reimplemented Kelly with Thorp's `p/a−q/b` fed per-trade returns → leverage-scale numbers that always pegged the ceiling. Both now use the shared correct impl (payoff-ratio Kelly), caps as fractions, result scaled to percent. |
| AV free-tier budget: cache heavy fetchers + budget guard (migration 090) | Claude | 2026-07-07 | Four heaviest AV callers (INSIDER/OVERVIEW/DAILY_ADJUSTED/NEWS_SENTIMENT) bypassed the day-cache → ~60-100 calls/day vs the 25/day free budget, starving the scorer. Routed all through `avCachedFetch`; theme-scout's global feeds cached + its OVERVIEW check shares research's cache key (dedupe); removed theme-scout's free-text `topics=` no-op call. Added `av_budget` daily counter — stops spending real calls past the ceiling, serves cached instead. |
| Trailing-stop vs dynamic-stop conflict (migration 091) | Claude | 2026-07-07 | PositionMonitor trailed every stop at a hardcoded 7% and overwrote stop_loss each run, discarding the MAE-derived stop distance from fill. Now persists the initial stop (immutable, via trigger) and trails at the position's own distance. |
| Legacy trade_queue generator hardened | Claude | 2026-07-07 | `/api/agents/trade` (writes trade_queue, parallel to trade_proposals) lacked the kill-switch + owner gates the main trader has. Added both. Its approve path only emits a manual paste command (no autonomous execution). Full UI consolidation onto trade_proposals flagged as a separate architecture-gated change. |
| India learner + Kite holdings-first + wire india_screen_cache (migration 092) | Claude | 2026-07-07 | Learner was hardcoded `LEARN_MARKET="us"` — India never evolved. Now market is a per-run param (body/query), idempotency + run record scoped by market, new Friday `kairos-learner-india` cron passing `{"market":"india"}`. Research India block now pulls real Kite holdings (isHeld→SELL enabled) + dual-bucket candidates from the nightly india_screen_cache (was static first-8 NIFTY names). |

---

## ✅ Session 2026-07-06 (cont'd) — Mobile-responsive pass + per-market trading toggle

> User: whole app looked bad on mobile ("don't miss anything, remember same for future feature work") + wanted a per-market live-trading on/off separate from the existing Kite disconnect kill-switch (view holdings always, toggle auto-trading independently for US/India).

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Mobile-responsive: DashboardShell + global CSS | Claude | 2026-07-06 | `useIsMobile()` matchMedia hook + slide-in drawer nav (`DashboardShell.tsx`); global overflow-x safety net + table/img overflow rules in `globals.css`. |
| Mobile-responsive: all ~30 dashboard pages/components | Claude + parallel agents | 2026-07-06 | CSS-only fixes (grid `auto-fit/minmax`, `clamp()` padding, table `overflowX` wrappers, `flexWrap`) — no JS breakpoint logic per-page. Verified via `tsc`, `vitest` (86/86), clean `next build`. |
| Kite disconnect kill-switch | Claude | 2026-07-06 | `disconnectKite()` wipes local token even if Kite's revoke call fails/unreachable; Settings UI Disconnect button + note that revoking at kite.zerodha.com is the authoritative, app-independent kill switch. |
| Robinhood MCP integration — architecture draft only | Claude | 2026-07-06 | `features/robinhood-mcp-integration/FEATURE_ARCHITECTURE.md`, Status: Draft, unapproved, unimplemented. Coexists with (does not replace) the manual Claude-Code-paste flow; read-only account-snapshot scope only. |
| Migration 088 — per-market trading enable/disable | Claude | 2026-07-06 | `strategy_config.trading_enabled_us` / `trading_enabled_india` (both default true). Gated in `app/api/broker/orders/route.ts` for live orders only — reordered the check to run after `market` is resolved from the symbol, alongside the pre-existing global `trading_enabled`. Viewing holdings/balances was already market-agnostic and untouched. Settings → Agents now has an independent toggle pair, distinct from the Kite Disconnect kill-switch (this never revokes a credential, just pauses new live orders for that market). |

---

## ✅ Session 2026-07-06 (cont'd) — India-specific morning/evening email briefings (NSE-hour anchored)

> User asked whether the emailed morning/evening briefings fire separately for US vs India, matching each market's own open/close, like research/position-monitor already do.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Briefing route made market-aware | Claude | 2026-07-06 | `app/api/briefing/generate/route.ts` was 100% US-only and ET-anchored (hardcoded `paper_portfolio.market='us'`, SPY/QQQ/DIA/VIXY via Massive, no market param at all). Now accepts `market: "us"\|"india"` in the cron body: uses the market's own timezone (IST for India, no DST) for date/session logic, filters every market-scoped query (paper_portfolio/positions/signals/trades/runs), swaps the index snapshot to NIFTY/SENSEX via `fetchIndiaIndices()` (India indices are raw point levels, not $-priced ETPs, so no currency prefix), and threads currency (₹/$) through every dollar-formatted line in both the email HTML and the LLM's data context. India has no live-broker section (Kite live state isn't wired into this briefing) — that block is simply omitted for India rather than showing US Robinhood data mislabeled. |
| Migration 085 — briefings market column | Claude | 2026-07-06 | `briefings`/`newsletters` had no market column and a `(date,session)` unique constraint — would have silently clobbered India's brief against the same day's US one. Added `market`, changed the constraint to `(date,session,market)`. |
| Migration 086 — India briefing cron | Claude | 2026-07-06 | New `kairos-brief-morning-india` (9:50 AM IST, 20min after `kairos-research-india`) and `kairos-brief-evening-india` (4:30 PM IST, 45min after `kairos-position-monitor-india`/NSE close) — mirrors the existing US brief-morning/brief-evening pattern (fire after the relevant agent activity has landed, not on a fixed clock disconnected from it). |
| Position price source for India | Claude | 2026-07-06 | Found while wiring this: India position price enrichment fell back to `price_cache`, a US-only-populated table — India positions with no `current_price` set would always show "price unavailable". Now uses `fetchIndiaQuote()` (the same Yahoo source PaperTrader itself fills India orders from) when the market is India. |

---

## ✅ Session 2026-07-06 (cont'd) — Closed every remaining market-switcher gap: Agents currency (₹), Morning Briefing India section, remaining Decision Journal write sites

> User said "fix all" on the three remaining disclosed gaps from the prior entry.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Agents page currency (₹) | Claude | 2026-07-06 | Passed `market` down from the server page; `AgentsPage` now computes `currency = market === "india" ? "₹" : "$"` and every price display (NAV, cash, positions, trades, backtest rows) uses it. Live Robinhood trade proposals deliberately stay `$` — that account is US-only regardless of the switcher. Registry updated to "full". |
| Decision Journal — broker order/fill sites | Claude | 2026-07-06 | `app/api/broker/orders/route.ts` and `.../sync/route.ts` now tag `market` at insert time (trivially available: `market` var / `order.market`). Settings/risk-profile changes and cron-gap alerts remain genuinely global (not market-specific) — left untagged, documented as such rather than a bug. |
| Morning Briefing — India section | Claude | 2026-07-06 | `app/dashboard/page.tsx` now also fetches India's paper_portfolio/positions/pending-signals/recent-runs (cheap, unconditional). `DashboardHome` renders an "India Paper Portfolio" card (NAV/P&L/cash/positions/high-conviction signals/last research run) below the US hero, shown only when `profile.market_focus` enables India. Also fixed a real bug found while doing this: the page's `positions`/`recentTrades`/`recentRuns`/`recentSignals` queries were UNFILTERED by market, silently blending India data into what the US hero card presented as pure-US NAV/positions — now scoped to `market=us` to match `paperPortfolio`. |

Every dashboard page the switcher touches is now genuinely wired, or explicitly documented as intentionally single-market (Live Portfolio, India page) via the nav badge system from the prior entry.

---

## ✅ Session 2026-07-06 (cont'd) — Critical: kairos_call_agent overload ambiguity (most cron jobs failing), decision_journal type-mismatch bug, remaining market-switcher wiring

> User asked why the evening briefing didn't generate and pushed to finish wiring the remaining unwired pages (Research Journal mislabel, Agents, Decision Journal, Morning Briefing).

| Task | Agent | Completed | Notes |
|---|---|---|---|
| **CRITICAL: kairos_call_agent overload ambiguity** | Claude | 2026-07-06 | Two Postgres overloads of `kairos_call_agent` existed (an old 3-arg version with no timeout param, and the fixed 4-arg version) — any pg_cron call that didn't pass all 4 args explicitly failed with "function is not unique." Confirmed via `cron.job_run_details`: proposal-reminder, broker-sync, position-monitor, nav-snapshot, rescore, and brief-evening were failing on nearly every scheduled fire — this is why the evening briefing didn't generate. Dropped the redundant 3-arg overload live + migration 083. |
| **CRITICAL: decision_journal type-mismatch bug** | Claude | 2026-07-06 | Found while adding the market column: `signal_id`/`paper_trade_id` were typed `bigint` but `agent_signals.id`/`paper_trades.id` are `uuid` — every `paper_fill`/`paper_exit` insert (which pass the real uuid) has been silently failing since the table existed. Confirmed live: 0 of 7 ever-created rows had these populated. Fixed both column types + added `market` (backfilled via join) in migration 084, applied live. |
| Decision Journal market wiring | Claude | 2026-07-06 | `/api/journal` now accepts `?market=`, page wired to `useMarket()`. Write sites tagged with `market` at insert time where available (paper-trade, position-monitor, manual close, strategy promotion override, Kite orders — hardcoded `india`). A few entry types with no derivable market (settings changes, broker-order/cron-gap alerts) default to the US view. |
| Agents page — partial wiring | Claude | 2026-07-06 | Server component now reads the `mkt` cookie and filters signals/trades/positions/performance/runs by market. NOT marked "full" — ~15 price displays in the 1500-line client component still hardcode `$`, not yet currency-converted for India. Registry marked "partial" honestly rather than overclaiming. |
| Research Journal mislabel fix | Claude | 2026-07-06 | Was falling through to the home page's registry entry (which is `us-only knownGap`) because I forgot to add its own entry last turn — it actually already works for both markets via its own in-page picker. Added the missing entry. |
| Morning Briefing | — | Deferred | Needs actual new UI (India NAV/signal/activity sections), not just a data filter — banked as a follow-up rather than rushed. |

---

## ✅ Session 2026-07-06 (cont'd) — Market-switcher wiring gap, nav clarity badges, India cron realignment, India NSE status pill

> User asked why it's hard to tell from the UI which left-nav sections actually respect the US/India switcher, asked to fix any unwired ones, and separately asked whether India's agents fire at the right time relative to NSE market hours (raised alongside a concern about Windows cron double-firing pg_cron, which was also resolved this session — all 15 `\Kairos\` Windows Scheduler jobs disabled).

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Windows Task Scheduler decommission | Claude | 2026-07-06 | Found all 15 `\Kairos\` jobs still `Ready`, hitting `localhost:3000` — the exact routes pg_cron now hits in the cloud. Duplicate trades were already prevented by paper-trade's signal-claiming idempotency (shared DB), but duplicate LLM/API spend was real whenever a local dev server happened to be running. Disabled all 15 with user approval. |
| Market-switcher wiring audit | Claude | 2026-07-06 | Surveyed all ~20 dashboard routes: only 9 actually read `useMarket()` (Portfolio, Risk, Markets, Strategies, Calendar, Scanner, Backtest, Settings, Intelligence) despite the switcher's own tooltip claiming "across the app." Watchlist, Score Tracker, and Agent History wired to the global switcher this session (API + component changes, `lib/market-support.ts` registry corrected to stop overclaiming "full" support on Agents/Journal/Morning Briefing, which don't actually filter). |
| Nav market-scope badges | Claude | 2026-07-06 | Added a small flag/warning badge directly in the left-nav sidebar (`components/dashboard/DashboardShell.tsx`), reading the existing `lib/market-support.ts` registry — previously that info only existed in a footer badge at the bottom of each page's content, which is exactly why it was "hard to tell from the UI." Distinguishes intentional single-market pages (🇺🇸/🇮🇳 flag) from known gaps that should respect the switcher but don't yet (⚠ US only). |
| India NSE market-status pill | Claude | 2026-07-06 | Added a second status pill next to the existing US market-status pill (top bar), showing NSE open/closed/pre-open using IST hours (9:15 AM–3:30 PM) and the same fixed-date holiday list as the research cron's NSE gate. Shown only when India is enabled in `profile.market_focus`, mirroring `MarketSwitcher`'s own visibility rule. Both pills now explicitly flag-labeled (🇺🇸/🇮🇳) — previously the single pill had no country label at all. |
| India research cron timing fix | Claude | 2026-07-06 | Found `kairos-research-india` fired at 16:30 IST — 1hr AFTER NSE's 15:30 close — meaning India's research (score off latest candle) and its internally-chained paper-trade fill (fetch a "live" quote) both landed on the exact same closing print, no realistic decision-to-fill gap. The US equivalent fires pre-open (scores off yesterday's close) and its chained fill lands on a genuinely live post-open quote. Realigned India to fire at 9:30 AM IST (15min after NSE's 9:15 open) instead — still scores off yesterday's finalized close, but the chained fill now gets a real intraday quote. `position-monitor-india` (15min after close) was already correctly timed and left unchanged. See migration 082. |

---

## ✅ Session 2026-07-06 (cont'd) — Codex adversarial review fix pass: evidence semantics, shared scoring contract, Theme Scout, Kelly sizing, LearnerAgent evidence-binding

> User requested an independent LLM (Codex) audit of the core scoring/trading/learning loop after the ultra-review pass, worried that missing/rate-limited external data (Alpha Vantage, Yahoo, etc.) could silently corrupt research/learning signals. Verified each finding against current code + live schema (several of Codex's line-number claims referenced pre-Decision-41 code and were stale/false — noted below), then fixed every confirmed issue in the requested priority order. See **Decision 43**.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Sentiment/insider availability always-true bug | Claude | 2026-07-06 | `fetchSocialSentiment()` never returns null (even when both StockTwits and AV news fail) and `scoreInsider()` never returns null either — so `dataQuality.sentimentDataAvailable = !!socialResult` / `insiderDataAvailable = !!insiderResult` were always `true`, meaning a fully-failed fetch still counted as "available neutral evidence" in weight renormalization. Added a real `has_data`/`available` flag to each, threaded through `lib/data/scores.ts`. |
| India fundamentals false-positive availability | Claude | 2026-07-06 | `lib/india-data.ts`'s `fetchIndiaOverview()` always sets `ov.Symbol` on any quoteSummary hit, even with zero real fields populated — `!!avOverview?.Symbol` treated a totally sparse response as available. `scores.ts` now requires 2+ of the 5 real fundamental fields (`hasMinFundamentalFields()`) before marking fundamentals available, for both US and India. |
| Macro scoring read the wrong table | Claude | 2026-07-06 | `fetchMacroScore()` queried `macro_signals` for `danger_score`/`regime` — those columns live on `macro_regime` (migration 028); `macro_signals` is per-indicator rows with no such columns. Every query silently errored → macro was excluded from every single scoring run since Decision 41 shipped renormalization, meaning MacroSentinel's weekly regime assessment never actually influenced a live score. Fixed to query `macro_regime`. |
| Shared weighted-score contract | Claude | 2026-07-06 | New `lib/scoring/weighted-score.ts` (`computeWeightedAnalystScore`) — the exact renormalization logic previously inlined only in `lib/research-agent.ts`. Now also used by `lib/validation/engine.ts`'s `scoreRow`, fed by a new `availability_mask` column select in `lib/learning/dataset.ts`. Previously the Validation Engine coalesced missing scores to 50 with a fixed weight split — a challenger could pass/fail on a different objective than the one that runs live. Also fixed a drift bug: the `availability_mask` persisted to `decision_observations` was a SEPARATE computation from the `included` object that actually drove live weighting, and disagreed for ETFs — now the same object is reused for both. |
| Abstain on thin evidence | Claude | 2026-07-06 | `processSymbol()` now forces `direction: "neutral"` when fewer than 2 dimensions are included OR the LLM thesis JSON parse fails — previously a parse failure fell back to `analystScore >= threshold ? "long" : "neutral"`, which could open a long position backed by a single technical-only signal with no thesis. Exit signals on held positions (`isHeld && direction === "short"`) are explicitly exempted — CLAUDE.md's locked SELL-signal-on-holdings rule always wins. |
| Theme Scout: schema + ownership + ticker validation | Claude | 2026-07-06 | Backfilled migration 081 for watchlist columns (`source`/`theme`/`reason`/`auto_added`/`expires_at`/`updated_at`/etc.) that were live but never committed, and relaxed `user_id` to nullable to match the live schema. Theme Scout now sets `user_id` to the app's profile owner (previously null → the app-facing Watchlist API's dedupe-by-symbol workaround was masking the real bug). Added `tickerExists()` — every LLM-suggested or AV-mentioned candidate must now pass an Alpha Vantage OVERVIEW existence check before insertion; failures are reported as `quarantined` in the response instead of silently entering the watchlist. Upsert errors are now logged + alerted instead of swallowed. |
| Kelly no-edge sizing bug | Claude | 2026-07-06 | `lib/risk/sizing.ts`'s `positionSizePct()` clamped negative/zero Kelly (no edge) to the floor size instead of zero — a calibrated model saying "no edge here" could still open a real floor-sized position. Now returns 0 when `kellyFraction <= 0`; the floor only applies once there's a genuine positive edge. Updated `tests/sizing.test.ts` to assert the corrected behavior. |
| Finite-number guards in PaperTrader | Claude | 2026-07-06 | `app/api/agents/paper-trade/route.ts` — `sizedPct`, `fillPrice`, `maxSpend`, `qty`, `totalCost` are now explicitly checked with `Number.isFinite()` before the fill RPC; previously a NaN from an upstream model/config value passed every `<= 0` / `< 1` comparison silently (NaN comparisons are always false) and could have reached `execute_paper_fill` with a NaN qty. Also: added the 5 missing `max_*_exposure_pct`/`max_avg_pairwise_corr` columns to the initial `strategy_config` select (previously never fetched, so Portfolio Constructor limits always silently used hardcoded defaults); fail-closed on the legacy multi-step fallback path in `VERCEL_ENV=production` if `execute_paper_fill` is ever missing there; added a `dynamic_rr_unavailable` stage-event log when the MAE/MFE percentile lookup returns null. |
| LearnerAgent evidence-bound weight mutations | Claude | 2026-07-06 | `update_signal_weight` previously trusted the LLM's own `n_trades`/`confidence` arguments for its numeric gates, with nothing binding them to a specific `query_score_correlation` result. Extracted the correlation logic into a shared `computeScoreCorrelation()` helper; `update_signal_weight` now recomputes it server-side fresh for the target dimension and gates on that — refuses the mutation entirely if only the weaker `paper_trades_fallback` is available (ledger required), requires N≥10 from the ledger (not the LLM's claim), and requires the correlation's sign to agree with the direction of the proposed weight change. The LLM's own numbers are now logged for context only. |
| Findings verified as pre-existing/already-fixed or false alarm | Claude | 2026-07-06 | Theme Scout's `watchlist.user_id not null` claim was stale — live schema has it nullable (confirmed via `information_schema`); the real issue was ownership (fixed above), not a hard constraint violation. `execute_paper_fill` RPC is confirmed present live, so the legacy-fallback risk was theoretical (still hardened defensively). |

---

## ✅ Session 2026-07-06 (cont'd) — Cloud-cron migration, pg_net timeout fix, Posture/Goals spec

> Vercel deployed as a silent cron-trigger target; 21 `pg_cron` jobs in Supabase now fire every agent independent of the laptop. Posture/Goals spec built in full. See **Decision 38**.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Vercel deployment + Supabase `pg_cron` migration | Claude | 2026-07-06 | Repo deployed to Vercel (git-integration auto-deploy), Deployment Protection disabled, `CRON_SECRET` gate verified live. 21 cron jobs registered via `kairos_call_agent()` (vault-stored secret, never hardcoded). Includes `macro-sentinel`/`theme-scout` which had never been scheduled anywhere before. |
| pg_net timeout bug fix | Claude | 2026-07-06 | Default 5s pg_net timeout would've silently killed every LLM-backed cron call. Added `maxDuration` to 16 Vercel routes + configurable `timeout_ms` param on the Supabase helper function; 3 heaviest jobs bumped to 300s. |
| Posture/Goals spec (Decision 38) | Claude | 2026-07-06 | Part A: kill-switch + exit-hysteresis now scale per risk profile (resilient fallback to old hardcoded defaults). Part B: time-bound postures with auto-revert (`decision_journal` logged), checked in the research cron. Part C: `trading_goals` table (agent-unread by design) + `/api/goals` feasibility math + dashboard `GoalCard`. |
| Ops Calendar + Broker Registry + Model Freshness (Decision 39) | Claude | 2026-07-06 | 30-day agent calendar (top of dashboard, expected-set derived from actual `pg_cron` schedule since `market_focus` never existed). Broker adapter registry (`lib/brokers/registry.ts` + alpaca/kite adapters) — Gateway routes now go through it, sync loop handles multiple brokers. Weekly model-freshness check (`/api/models/check`, informational only, never auto-switches). |
| Research Journal (Decision 40) | Claude | 2026-07-06 | Daily per-symbol funnel trail (research → screener bucket → portfolio constructor → execution, each stage logged with a reason) + a longer-horizon Evolution tab. Found and fixed a dormant bug while building it: `decision_observations.signal_id` had been hardcoded null since Phase 1 shipped — the join key never actually worked. New `/dashboard/research-journal` page. |
| Quick fixes: manual paper-position close, seeded-trade tagging, sector-breadth bug | Claude | 2026-07-06 | Added a "Close Position" button (paper positions had agent-only exits, no manual override). Tagged the one seeded/demo META trade with a SEEDED badge so it's not mistaken for a real agent decision. Fixed `/api/markets/breadth` — failed quote fetches were defaulting to `changePct:0` and still populating the Pullers/Draggers display (only the A/D count excluded them), which is why every sector constituent showed "+0.000%". |
| Vercel type errors + false-pass local builds | Claude | 2026-07-06 | Two real TS errors (`.catch()` on a Supabase `PromiseLike`; a loose `string` not assignable to `BrokerAdapter`'s status union) had been failing every Vercel deploy since the Posture/Goals push — a stale `tsconfig.tsbuildinfo` incremental cache made local `npm run build` falsely report success both times. Fixed both, gitignored the buildinfo file, and confirmed with a clean `rm -rf .next` rebuild before trusting the result going forward. |
| `profiles.market_focus` column never existed | Claude | 2026-07-06 | The India market switcher (`MarketSwitcher`) had been silently hidden this whole time — it gates on `profiles.market_focus.includes("india")`, but that column didn't exist in the live schema at all (confirmed via `information_schema`), so it always defaulted to US-only. Added the column, set to `'US,India'` for the active profile. No app code change needed (`layout.tsx` already does `select("*")`). |
| Theme Scout ordering + watchlist expiry | Claude | 2026-07-06 | Theme Scout was firing AFTER research (fire-and-forget) — same-day theme discoveries weren't researched until the next day. Now awaited BEFORE `gatherSymbols()` (US-scoped only), `research/cron` `maxDuration` bumped 60→150s to cover it. Also: `gatherSymbols()` never filtered `watchlist.expires_at` — a 30-day-expired Theme Scout pick kept getting re-researched forever; now matches the same filter the Watchlist API already had. |
| Research Journal duplicate-symbol display | Claude | 2026-07-06 | Multiple real research runs the same day (scheduled + manual test triggers) each wrote a fresh `decision_observations` row, so the funnel showed flat duplicate rows. `/api/agents/research-journal` now groups by symbol, keeps the latest observation, shows a "×N runs" badge instead. |
| Sentiment scoring bug + weight renormalization (Decision 41) | Claude | 2026-07-06 | `scoreSentiment()` checked field names (`bullish_pct`) that never matched the real data shape (`stocktwits_bullish_pct`, `av_news_sentiment`) — sentiment was silently always neutral-50 even with strongly bullish real data. Fixed. Also: the weighted score now renormalizes across only applicable+available dimensions (ETFs no longer penalized for structurally-absent fundamental/insider data; unavailable macro no longer diluted at full fixed weight) instead of always applying the fixed 5-way split against fabricated neutral defaults. Applied weights recorded into `decision_observations.features.weighting`, surfaced in Research Journal. |
| Sidebar reorg + India nav clarity | Claude | 2026-07-06 | Regrouped the sidebar (Daily / Portfolio / Agents & Research / Discovery / Learn / Settings) now that it has ~25 items. Clarified that "India" is the India *live* Zerodha Kite view (parallel to "Live Portfolio", not a duplicate of Paper Portfolio, which already handles both markets via its own switcher) and relabeled/regrouped accordingly. |
| Ultra-review fix pass (Decision 42) | Claude | 2026-07-06 | 10-angle multi-agent adversarial review of the full session diff, prompted by the density of silent bugs already found earlier. Fixed in priority order: same-day kill-switch never actually fired (compared a not-yet-written "today" row); screener bucket ordering let momentum crowd out value candidates; an Alpaca order-status regression from tonight's own type fix; posture override silently clobbered by profile defaults; broker registry error-swallowing; Kite/India orders had zero reconciliation path; consolidated 3 duplicated risk-profile constant tables into `lib/risk-profiles.ts`; currency-blind win/loss classification (fixed `$0.5` band) replaced with a relative `±0.1%` band via `lib/trade-outcome.ts`; weight-renormalization zero-division edge case; manual-close qty-drift logging; `schedule.ts` days/time mismatch; mislabeled shadow "agreement %" renamed; and — the biggest structural gap — none of ~10 schema changes applied live via Supabase MCP this session had a committed migration file, backfilled as `supabase/migrations/072`-`080`. |

---

## ✅ Session 2026-07-06 (Learning-core Phase 3 — genome, feature registry, shadow decisions, regime features, governance rewiring)

> Completes the full learning-core roadmap (Phases 1–3 + all P0s + Execution Gateway paper-stage). See **Decision 37**.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Typed strategy genome | Claude | 2026-07-06 | Migration 063 (`strategy_versions.genome`, `agent_signals.genome_hash`) + `lib/validation/genome.ts` (hard-bounded validation, diff counting, hashing). Genome-less rows unaffected. |
| Feature registry + whitelisted compiler | Claude | 2026-07-06 | Migration 064 (`feature_registry`) + `lib/validation/feature-compiler.ts` (from-scratch tokenizer/parser/evaluator — `+ - * /`, `log/abs/min/max/lag` only, no eval/no dynamic code). New `propose_feature` learner tool. |
| Feature IC-check job | Claude | 2026-07-06 | `lib/validation/feature-check.ts` (Spearman rank-IC + Fisher-z significance, pure/tested) + `app/api/validation/feature-check` (weekly cron) — promotes proposed→quarantined→active, auto-retires decayed features. |
| Shadow decisions | Claude | 2026-07-06 | Migration 065 (`shadow_decisions`, extends `strategy_versions.state` with `shadow_paper`). ResearchAgent records what up to 3 shadow versions would decide alongside the champion's real decision — no fills, no cash. Off by default. |
| Regime features | Claude | 2026-07-06 | `lib/validation/regime.ts` (trend/vol-tercile vs SPY/^NSEI, pure/tested) appended to every observation's `features.regime`. No hard bull/bear switch. |
| Governance rewiring | Claude | 2026-07-06 | Learner auto-guard now trips on champion health (drawdown>15%/90d peak, calibration drift>0.25, data-availability<60%/10 obs) instead of a raw win-rate streak. Confirmed it only ever gated `update_signal_weight` — other tools unaffected. |
| 85 tests passing, tsc + build clean | Claude | 2026-07-06 | 5 new test files this session alone (genome, feature-compiler, feature-check, regime + existing suite). |

---

## ✅ Session 2026-07-06 (Execution Gateway — Alpaca paper orders + fixed a discovered Trade Queue bug)

> Built the paper-stage of the Execution Gateway (spec Part A) and, in the same change, fixed a pre-existing (empty-table, no real-data-loss) bug: the Trade Queue UI read a dead `trade_queue` table while Approve/Reject/the Gateway all use `trade_proposals`. See **Decision 36**.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Alpaca order adapter | Claude | 2026-07-06 | `lib/brokers/alpaca-orders.ts` — submit/get/cancel, reuses the existing vault-key pattern from `lib/brokers/alpaca.ts`. |
| broker_orders lifecycle + routes | Claude | 2026-07-06 | Migration 068. `POST /api/broker/orders` (human-click only, live gated on trading_enabled) + `/sync` (30-min cron, position reconciliation + alert). |
| Fixed trade_queue/trade_proposals mismatch | Claude | 2026-07-06 | Repointed the Smart Money page's query to the real table (`trade_proposals`) and the correct pending status (`pending_review`). Approve/Reject now actually functional; both tables confirmed empty in prod, so no real data was ever affected. See Decision 36. |
| "Send to Alpaca (paper)" UI | Claude | 2026-07-06 | Button on approved proposals in the Trade Queue history table. Confirm dialog, paper-env only from this UI. |
| system-map.json — GATEWAY node | Claude | 2026-07-06 | New node; REALORDER description clarified as Robinhood-specific. |

---

## ✅ Session 2026-07-06 (Learning-core Phase 2 — Validation Engine + calibrated Kelly sizing + transactional fill RPC, all verified live)

> Validated live via the reconnected Supabase MCP: migrations 061/062 applied, `execute_paper_fill` RPC tested end-to-end (disposable signal, all 5 writes landed atomically, then fully reversed). See **Decision 35** (Validation Engine + sizing) and **Decision 34** (transactional-fill RPC + the pre-existing signal_id bug it uncovered).

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Transactional paper-fill RPC | Claude | 2026-07-06 | `execute_paper_fill` (migration 071) — claim-reverify → event → trade → position → cash → signal-flip in ONE transaction, row-locked. Verified live end-to-end; falls back to the legacy multi-step JS sequence if absent. See Decision 34. |
| P0: cron-gap detector, vitest suite, Portfolio Constructor | Claude | 2026-07-06 | All three P0 app-improvements from the learning-core spec completed and verified (47 tests passing across 8 files by end of session). |
| Validation Engine | Claude | 2026-07-06 | `lib/validation/engine.ts` (migration 061 `validation_experiments`) — deterministic walk-forward replay, 1000-draw block bootstrap (seed 42), pass rule: p_improvement>=0.80, CI floor, n_effective>=12, >=3/5 folds won. See Decision 35. |
| Fail-closed promotion gate | Claude | 2026-07-06 | `promote_champion` now HTTP 412s without a passed validation experiment (journaled `force_unvalidated` override exists, not the default path). See Decision 35. |
| Calibrated P(win) + Kelly sizing | Claude | 2026-07-06 | `lib/validation/calibration.ts` (migration 062 `model_artifacts`) — logistic regression replaces raw analyst_score as the sizing input; half-Kelly via `lib/risk/sizing.ts`, capped at the flat position_size_pct. Dormant until 60+ matured observations/market. |
| Dynamic MAE/MFE R:R | Claude | 2026-07-06 | `lib/risk/percentiles.ts` — stop/target from the ledger's actual outcome-percentile distribution, replacing the fixed 7%/20% profile constants. Signal-provided values always win. |
| Learner auto-fires validation | Claude | 2026-07-06 | `update_signal_weight` fires `/api/validation/run` the moment a challenger is created — evidence is usually ready before a human looks at the Strategy Registry. |
| Weekly fit-calibration cron | Claude | 2026-07-06 | New `app/api/validation/fit-calibration` route, Fridays 4:45 PM ET (before the 5 PM learner). |
| system-map.json — VALIDATE node | Claude | 2026-07-06 | New node between CHALLENGER and PROMOTE; TRADER + PROMOTE descriptions updated to reflect the gate + Kelly sizing. |

---

## ✅ Session 2026-07-06 (Supabase MCP reconnected — migrations applied, critical fill-event bug fixed)

> Reconnected Supabase MCP gave direct schema access for the first time in several sessions. Applied all pending migrations (060, 069) and one urgent live fix: **`paper_order_events.signal_id` was `bigint` while `agent_signals.id`/`paper_trades.signal_id` are `uuid`** — every paper fill's order-event insert had been failing since migration 034 (confirmed: zero rows ever in `paper_order_events`). Fixed live (migration 070, lossless — table was empty) and verified end-to-end: restarted the stale production server, re-ran ResearchAgent, confirmed `decision_observations` now writes real per-candidate feature rows. See **Decision 34**.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Applied migration 060 (observation_labels) | Claude | 2026-07-06 | Previously blocked by Supabase SQL-editor Redis/RLS-dialog issues; applied directly via reconnected MCP. |
| Applied migration 069 (portfolio_limits) | Claude | 2026-07-06 | `strategy_config` gains max_gross/sector/name_exposure_pct, max_portfolio_vol_pct, max_avg_pairwise_corr — Portfolio Constructor limits. |
| Fixed paper_order_events.signal_id type bug | Claude | 2026-07-06 | Migration 070. Long-standing bug (predates this session) silently failing every fill's audit-event write. See Decision 34. |
| Verified end-to-end on live DB | Claude | 2026-07-06 | Restarted stale `next start` server (was serving a pre-session build), re-triggered research, confirmed decision_observations populates with real feature blobs (5 dims × N candidates per run). |
| Confirmed migrations 057/058 already live | Claude | 2026-07-06 | Multi-market + India screen cache both confirmed applied from earlier sessions. |

---

## ✅ Session 2026-07-06 (Learning-core Phase 1 — decision ledger + matured horizon labels)

> An independent architecture review (Codex) found LearnerAgent's dataset statistically untrustworthy: signal→trade join by symbol (collision-prone), label = policy P&L on filled-longs-only (selection bias, mixes alpha with beta/holding-time/exits), no valid out-of-sample evaluation. This is the keystone fix everything else (validation engine, calibrated sizing, genome, shadow A/B — see `features/learning-core/`) is built on. See **Decision 33**.
> **⚠️ OPEN ITEM: migrations `059_decision_observations.sql` + `060_observation_labels.sql` must be applied MANUALLY in the Supabase SQL editor** — Supabase MCP permission-denied this session. Everything is guarded/additive: the app behaves byte-for-byte unchanged until they land, then the ledger starts accruing automatically.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Migrations 059/060 — decision ledger + labels | Claude | 2026-07-06 | `decision_observations` (append-only via trigger; one row per scored candidate, filled or rejected — raw `computeScores().evidence`, availability mask, weights used, score, action) + `observation_labels` (forward 2/5/10/20-trading-day `fwd_return`/`benchmark_neutral_return`/MAE/MFE, written only after horizon maturity). **NOT auto-applied — user must run both in the Supabase SQL editor.** See Decision 33. |
| ResearchAgent — observation write | Claude | 2026-07-06 | `lib/research-agent.ts` writes a `decision_observations` row for every scored candidate (fail-soft if 059 absent). Captures the full raw feature blob at zero refactor cost — future-proofs Phase 3 feature discovery. See Decision 33. |
| Label-maturation cron | Claude | 2026-07-06 | New `app/api/agents/label-maturation/route.ts` (nightly, 6:00 PM ET) matures each horizon once enough calendar time has passed; US prices from `price_cache`, India from `fetchIndiaCandles`; benchmark = SPY (US) / `^NSEI` (India); 10bps cost haircut on `fwd_return`. Registered in `run-agents.ps1` + `register-tasks.ps1`. See Decision 33. |
| Walk-forward dataset builder | Claude | 2026-07-06 | `lib/learning/dataset.ts` — `loadLabeledDataset` (ledger×labels join) + `walkForwardFolds` (pure function: purge = drop train rows whose label window overlaps the test window; embargo = skip a horizon's worth of time after each test window). Read-only; Phase 2's validation engine consumes it. See Decision 33. |
| LearnerAgent — ledger-first correlation + personal-history quarantine | Claude | 2026-07-06 | `query_score_correlation` now reads the ledger FIRST (all scored candidates, horizon-aligned, benchmark-neutral — not just fills/policy-P&L), tagged `source: "observation_ledger"` with an explicit INTERIM caveat; falls back to the legacy paper-trades path (now correctly joined by `signal_id`, not symbol) while the ledger is thin. `query_trade_decisions`/`semantic_search_decisions` (10yr personal history) now return `role: "behavioral_evidence_only"` — cannot satisfy `n_trades` or justify `update_signal_weight`. System prompt updated to state this explicitly. See Decision 33. |
| system-map.json — LEDGER node | Claude | 2026-07-06 | New `LEDGER` node (RESEARCH → LEDGER → LEARNER) documenting the ledger's role as the ground truth for the learning/evolution roadmap. See Decision 33. |

---

## ✅ Session 2026-07-05 (Phase 5 — India parity: global switcher + support registry + direct NSE feeds)

> India goes from paper-trading/self-learning (Phase 4) to **first-class across the whole dashboard**, behind a global market switcher, with an honest per-page coverage badge. Direct NSE feeds lift the two remaining free-data ceilings (full-market scan + India insider/options), failing soft to Yahoo/NIFTY-100. See **Decision 32**.
> **⚠️ OPEN ITEM: migration `058_india_screen_cache.sql` must be applied MANUALLY in the Supabase SQL editor** — Supabase MCP was permission-denied this session (`057` already applied by the user). Guarded code degrades to US-only until 058 lands.
> **⚠️ OPEN ITEM: NSE feeds may be geo-blocked from a US IP** — `lib/nse-data.ts` fails soft, so full-market scan + India insider/options degrade to their Yahoo/NIFTY-100 fallback with an honest note when NSE is unreachable.
>
> **Follow-up (2026-07-05):** the four remaining **partial** India panels were brought to **full** parity — Markets (real sector heatmap `fetchIndiaSectors` + NIFTY-50 breadth), Risk Analytics (real beta-vs-NIFTY, 1y daily-candle regression; `computeRiskMetrics` now async), Strategies (real India fit-scores from `signal_score_history` market='india'), Earnings (market-wide NSE results calendar `fetchNseEarnings`, Yahoo per-symbol fallback). New adapters: `fetchIndiaSectors` (`lib/india-data.ts`), `fetchNseEarnings` (`lib/nse-data.ts`); `market-support.ts` flipped all four to `full`. Genuine remaining US-only bits: Markets TradingView/macro-sentinel tiles, Strategies Algo Library; NSE feeds may geo-block from a US IP (graceful fallback). See Decision 32.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Global market switcher + per-page support footer | Claude | 2026-07-05 | `lib/market-context.tsx` (`MarketProvider`/`useMarket()` → `us\|india`, persisted to `localStorage` + `mkt` cookie) in the `DashboardShell` header, **hidden unless `market_focus` includes India**; server pages read the `mkt` cookie. `lib/market-support.ts` maps route → `{level: full\|partial\|us-only\|india-only, note}` as the single source of truth → a coverage badge at the bottom of every page. See Decision 32. |
| Markets panel — India | Claude | 2026-07-05 | India via Yahoo: NIFTY / SENSEX / BankNifty / India-VIX, plus a real sector heatmap (10 NSE sector indices via `fetchIndiaSectors`) + market breadth (NIFTY-50 advancers/decliners). Level: **full** (upgraded from indices-only 2026-07-05; TradingView/macro-sentinel tiles remain US-only). See Decision 32. |
| Risk Analytics panel — India | Claude | 2026-07-05 | Per-₹ book, VaR vs NIFTY, plus real portfolio **beta vs NIFTY** (1y daily-candle covariance/variance regression, value-weighted; `computeRiskMetrics` now async). Level: **full** (upgraded from partial 2026-07-05; was "beta coming soon"). See Decision 32. |
| Backtest panel — India | Claude | 2026-07-05 | Yahoo `.NS` candles, alpha vs NIFTY. Level: **full**. See Decision 32. |
| Scanner panel — India | Claude | 2026-07-05 | Full NSE market via nightly cache; NIFTY-100 live fallback. Level: **full**. See Decision 32. |
| Strategies panel — India | Claude | 2026-07-05 | Real India **fit-scores** computed from its signals' dimension scores (`signal_score_history` market='india'). Level: **full** (upgraded from partial 2026-07-05; was empty "US only" state; Algo Library remains US-only). See Decision 32. |
| Earnings panel — India | Claude | 2026-07-05 | Market-wide NSE results calendar (`fetchNseEarnings` via NSE event-calendar) with per-symbol Yahoo fallback. Level: **full** (upgraded from partial/tracked-names-only 2026-07-05). See Decision 32. |
| Smart Money panel — India | Claude | 2026-07-05 | Signals + trade queue both markets; India insider + option-chain PCR/OI live from NSE. Level: **full**. See Decision 32. |
| NSE direct feed adapter | Claude | 2026-07-05 | `lib/nse-data.ts` — cookie-handshake adapter for NSE's free JSON: full equity list (`EQUITY_L.csv`), insider (`corporates-pit`), option chain (`option-chain-indices`/`option-chain-equities`). **This lifted the two earlier ceilings** (full-market scan + India insider/options). Fails soft (NSE geo-throttles some non-India IPs) → callers fall back to Yahoo/NIFTY-100 with an honest note. See Decision 32. |
| Scanner cache + migration 058 | Claude | 2026-07-05 | Migration `058_india_screen_cache.sql` (new table) + nightly cron `POST /api/scan/india/refresh` scores the full NSE list in 600-name oldest-first slices; Scanner reads the cache, falls back to live NIFTY-100. **NOT auto-applied — user must run 058 in the Supabase SQL editor.** See Decision 32. |
| Market-scoped agents + India crons | Claude | 2026-07-05 | `research`/`paper-trade`/`position-monitor` accept `?market=us\|india`; US 9 AM tasks pinned to `?market=us` (no longer double-process India). India Task Scheduler tasks (PC clock = ET, all post-NSE-close 15:30 IST = 06:00 ET): scan-india-refresh 5:30 AM, research-india 6:15 AM, position-monitor-india 6:35 AM. See `scripts/run-agents.ps1` + `scripts/register-tasks.ps1`. See Decision 32. |
| Guarded rollout + system-map.json | Claude | 2026-07-05 | Pre-058 (missing column/table) every India-parity path degrades to US-only, never 500s. `system-map.json` updated with NSE nodes (full equity list, insider, option chain) feeding the scanner cache + Smart Money. See Decision 32. |

---

## ✅ Session 2026-07-05 (Phase 4 — multi-market learning: per-currency pools + per-market champions)

> Market is now a **tag** (us | india), not a fork — one app, panels filter by market, currencies NEVER summed. Supersedes Decision 29 (India was score-only): India now paper-trades in its own ₹ pool, closing the India learning loop. See **Decision 31**.
> **⚠️ OPEN ITEM: migration `057_multi_market.sql` must be applied MANUALLY in the Supabase SQL editor** — Supabase MCP was permission-denied this session (no psql/DATABASE_URL). Guarded code runs unchanged until 057 lands; India activates automatically once the `market` column + ₹ pool row exist.

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Migration 057 — market as a tag across the paper stack | Claude | 2026-07-05 | `market` column added to `paper_portfolio`/`paper_positions`/`paper_trades`/`paper_performance`/`agent_signals`/`signal_score_history`; `paper_performance` unique key `(date)` → `(date, market)` so each market keeps its own NAV curve. New India ₹ pool seeded at ₹1,000,000; `strategy_versions` gains `market` and India is seeded as a CLONE of the US champion (prior). **NOT auto-applied — user must run it in the Supabase SQL editor.** See Decision 31. |
| PaperTrader — per-currency fills | Claude | 2026-07-05 | `app/api/agents/paper-trade/route.ts` fills EACH signal into its market's pool in native currency: US via `getQuote` (AV/Robinhood USD), India via free Yahoo `.NS` (INR). Sizing = `position_size_pct` of THAT pool's cash. India now produces closed outcomes → **closes the India learning loop** (previously scored but never paper-traded). See Decision 31. |
| PositionMonitor — per-market exits | Claude | 2026-07-05 | `app/api/agents/position-monitor/route.ts` monitors/exits per market in native currency (India prices via Yahoo), crediting each close back to its own pool. See Decision 31. |
| LearnerAgent — per-market champion weights | Claude | 2026-07-05 | `app/api/agents/learner/route.ts` analyzes ONE market's cohort per run (US today) and proposes challengers ONLY for that market's champion — a bad India run can never shift US scoring. India diverges from its US-clone prior once it clears the same 10+ closed-trade phase gate. See Decision 31. |
| ResearchAgent — reads market-matched champion | Claude | 2026-07-05 | `lib/research-agent.ts` reads the champion weights for the symbol's market rather than a single global champion. See Decision 31. |
| Settings — `market_focus` trimmed + non-destructive gate | Claude | 2026-07-05 | `market_focus` reduced to US + India only (Europe/Asia/Crypto/Global removed as noise). Toggle is NON-destructive: India ON → NIFTY scoring + ₹ fills + India learning cohort; India OFF → stops NEW India research/fills but KEEPS open India positions monitored-to-close + all history/weights (re-enable resumes). Real Kite holdings/execution unaffected by the toggle. See Decision 31. |
| Portfolio UI — market selector | Claude | 2026-07-05 | Portfolio/paper surfaces gain a per-market selector so US ($) and India (₹) NAV curves and positions are viewed independently and never blended into one number. See Decision 31. |
| Guarded rollout + system-map.json | Claude | 2026-07-05 | Every path is pre-057-safe: with no `market` column / single pool, behavior is byte-for-byte the old US-only app. `system-map.json` updated to split the paper pool + learner per market. See Decision 31. |

---

## ✅ Session 2026-07-05 (India — Zerodha Kite + Yahoo multi-market integration)

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Phase 1 — Kite auth (daily one-click login) | Claude | 2026-07-05 | `lib/kite.ts`, `app/api/kite/login\|callback\|status`. Kite Connect v3: login → `request_token` → SHA256-checksum exchange → `access_token` stored in `api_key_vault` as `KITE_ACCESS_TOKEN`, treated as expired if not generated today (Kite tokens expire 6 AM next day, SEBI rule). Verified live — real `/user/profile` returns the user's name. Settings → Agents shows a "Zerodha Kite · India" connection card. See Decision 30. |
| Phase 2 — India scoring on free Yahoo data | Claude | 2026-07-05 | `lib/india-data.ts`, `lib/india-universe.ts`, `research-agent.ts`, paper-trade route. Indian NSE (`.NS`) stocks run the SAME 5-dimension pipeline as US, but data is FREE Yahoo Finance (Kite Personal tier has no market data) — chart endpoint for price+candles (no auth), cookie+crumb `quoteSummary` for fundamentals, mapped into the AV-OVERVIEW shape the scorer expects. Candidates from a static NIFTY-50 list. US-only inputs (social/options/insider) skipped → neutral baseline, flagged in score-detail. Wired off `profiles.market_focus` including "India". Verified: RELIANCE.NS scored on real Yahoo fundamentals (P/E 21.85, ROE 9.1%, rev +12.5% → fundamental 73) over 124 real candles. **India is scored + tracked but NOT paper-traded** — INR pricing would corrupt the single-USD paper pool; `PaperTrader` excludes `asset_class="india"`. See Decisions 28, 29. |
| Phase 3 — real Kite execution + holdings | Claude | 2026-07-05 | `lib/kite.ts` additions, `app/api/kite/portfolio`, `app/api/kite/order`, `app/dashboard/india` page + nav. Real NSE/BSE holdings read (verified: 5 real INR holdings from the live account) and real order placement (`POST /orders/regular`, product `CNC`). Human-in-the-loop safety: authenticated-user-only (never cron/agent), requires explicit `confirm:true`, writes a `decision_journal` audit row; `/dashboard/india` uses a two-step confirm with a "REAL MONEY" warning that never fires on first click. Reads/orders degrade to a "reconnect" state when the daily token is stale. See Decision 30. |
| system-map.json + docs updated for India | Claude | 2026-07-05 | Added YAHOO/INDIA/KITE nodes to `system-map.json`; India / multi-market section in ARCHITECTURE.md; Decisions 28–30 in PROJECT_DECISIONS.md. Recorded data-source reality: US = Alpha Vantage + FinancialDatasets + Massive; India = free Yahoo (`.NS`) for data + Zerodha Kite for execution/holdings; no direct non-US equity beyond India. |

---

## ✅ Session 2026-07-05 (closed-loop learning, score history, exit de-confliction, Langfuse agent-loop tracing)

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Closed-loop learning — ResearchAgent consumes champion weights | Claude | 2026-07-05 | `processSymbol` in `lib/research-agent.ts` now reads the promoted champion's `weights_snapshot` first (normalizing both key formats — seed `{fundamental:0.3}` vs challenger `{fundamental_weight:0.3}`), falling back to static `PROFILE_WEIGHTS` then `signal_weights` only when no champion is promoted. Loop was OPEN (promoted weights were never read); now CLOSED. `research_packets.raw_data` records `_using_champion_weights`. See Decision 24. |
| Score history — `signal_score_history` table + ScoreTrajectory | Claude | 2026-07-05 | Migration 054: append-only per-symbol score history (analyst_score + 5 dimensions, direction, source, created_at; RLS + `(symbol, created_at desc)` index; never mutated after insert). ResearchAgent appends best-effort per score (no-ops until migration applied) and injects a `SCORE TREND` note into the thesis prompt. New `GET /api/charts/score-history?symbol=X` feeds the symbol-detail chart (previously starved of data). PRD-specced, never built. See Decision 25. |
| LearnerAgent Phase B backstop de-confliction | Claude | 2026-07-05 | Phase A (smart `llm_exit` re-score) and Phase B (blunt >7-day time cutoff) raced; crude Phase B usually won. Phase B now skips any position already carrying the `llm_exit` flag and its cutoff moves 7 → 14 days, making it a last-resort backstop. `app/api/agents/learner/route.ts`. See Decision 26. |
| Langfuse tracing extended to `runAgentLoop` | Claude | 2026-07-05 | The multi-step tool-calling loop (LearnerAgent, MentorAgent) was invisible in Langfuse (only `llm_call_log`); now wrapped in a Langfuse trace/generation span (system prompt in, final text out, tokens, cost, tool-call trail as metadata). Extends Decision 15. LangChain/LangGraph still unused — loop stays hand-rolled against Anthropic/DeepSeek SDKs. See Decision 27. |

---

## ✅ Session 2026-07-04 (late — bug sweep: sector chart, briefing, live account, paper prices, privacy mode)

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Sector chart pagination fix + Massive 500-bar cap discovery | Claude | 2026-07-04 | `bc07c7a` — `next_url` wasn't followed on 1Y+ periods; fixed, then found a real provider limit: Massive free tier caps aggs responses at ~500 bars, no pagination beyond that |
| Sector chart replaced with real TradingView widget | Claude | 2026-07-04 | `77f371f` (Symbol Overview embed — didn't hydrate reliably) → `9f81fd5` (final: reused `TradingViewChart` tv.js Advanced Chart component from symbol detail page, tab switcher across 11 sector ETFs). Removed `components/charts/SectorLineChart.tsx` + `app/api/charts/sector-history/route.ts`. See Decision 21. |
| Weekend briefing recap | Claude | 2026-07-04 | `909bcc4` — weekend-specific editor's-note prompt synthesizing last 7 days + week ahead, replacing the bland "nothing to do" daily prompt |
| Newsletter history wiring | Claude | 2026-07-04 | `909bcc4` — `sendBriefingEmail` now also inserts into `newsletters` table on send; Intelligence → Newsletter tab was always empty before this |
| Market Synthesis cache-poisoning fix | Claude | 2026-07-04 | `909bcc4` — only cache synthesis result to `briefings` on success, not on transient Massive failure |
| Live Robinhood account $0/$0 fix | Claude | 2026-07-04 | `359b2c6` — upsert (not insert) on `live_account_snapshots`; added missing `.eq("account_id", "965848641")` filter everywhere reads lacked it; verified against real Robinhood app |
| Stale paper position prices fix (META stuck at entry price) | Claude | 2026-07-04 | `9165958` — `position-monitor` + `learner` routes queried nonexistent `closed_at` column, silently no-op'd every run since written; fixed queries, fixed `opened_at` column name, fixed stray polygon.io endpoint, rewrote close logic to match real schema (delete `paper_positions` row, close matching `paper_trades` row) |
| Alpha Vantage day-cache wired into batch quotes | Claude | 2026-07-04 | Same commit `9165958` — `getBatchQuotes`/`fetchAVQuote` were uncached, burning the 25/day AV quota on every Live Portfolio load |
| Live Portfolio false "-100% total loss" fix | Claude | 2026-07-04 | Same commit `9165958` — `q?.price ?? fallback` never triggered on explicit `price: 0`; now falls back to avg cost honestly |
| Briefing "Learning Log" query fix | Claude | 2026-07-04 | Same commit `9165958` — was querying nonexistent columns on `learning_log` (that table is the learner's own weight-mutation audit log); switched to querying recently-closed `paper_trades` |
| Privacy Mode (new feature) | Claude | 2026-07-04 | Eye-icon toggle masks live-account dollar figures on Dashboard home + Live Portfolio, default-hidden, resets on remount; master switch in Settings → Preferences (localStorage). `components/dashboard/PrivacyMask.tsx`. See Decision 22. **Uncommitted as of this entry.** |
| execClaude/MCP tool-calling architecture audit | Claude | 2026-07-04 | **NOT FIXED — flagged only.** `execClaude` cannot call MCP tools (no API key, no MCP config); every "tool call" it makes is trusted text output. Found in `research-agent.ts` screener, `mentor/evaluate`, and — highest severity — the real-money order paths `agents/trader` + `agents/trade/approve`. Requires explicit user sign-off before fixing. See Decision 23 and ARCHITECTURE.md "Known Architecture Risk" section. |
| ARCHITECTURE.md dashboard nav map | Claude | 2026-07-04 | Added full nav-item-by-nav-item map (path, what it shows, backing API/tables) so an external reviewer can test every page against documented behavior |

---

## ✅ Session 2026-07-01 (latest — Kairos rename + wiring)

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Platform rename FinanceOS → Kairos | Claude | 2026-07-01 | layout.tsx title, package.json, DashboardShell sidebar brand + tagline "Right signal. Right moment." |
| BOM + encoding corruption fix | Claude | 2026-07-01 | PS5.1 corruption fixed across 14+ files; curly quotes, em dashes, emoji sequences all cleaned; build restored clean |
| market_focus wired to ResearchAgent | Claude | 2026-07-01 | `gatherSymbols()` queries profiles.market_focus; non-US regions append region ETFs (India/Europe/Asia/Crypto/Global); marketFocus passed to thesis LLM prompt |
| Learner schedule: weekly (Fridays only) | Claude | 2026-07-01 | Cron-triggered learner now skips Mon–Thu; only runs Fri 5 PM; manual triggers unrestricted |
| Cron script updated: trader + schedule | Claude | 2026-07-01 | Added `trader` endpoint; corrected schedule comments (08:00 brief → 09:00 research → 09:45 trader → 16:15 pos-monitor → 16:30 brief → 17:00 nav → Fri learner) |
| market_focus multi-select chip UI | Claude | 2026-07-01 | Settings page: chip buttons US/India/Europe/Asia/Crypto/Global; comma-separated text in DB |
| Weekend + holiday skip in research cron | Claude | 2026-07-01 | research/cron and learner routes skip weekends + 2026 US holidays |
| Newsletter wired: EDGAR, proposals, weights | Claude | 2026-07-01 | 3 new data queries + 3 new email sections; deployed as v4 |
| ARCHITECTURE.md + WORK_LOG updated | Claude | 2026-07-01 | Full session 2026-07-01 documented |

---

## ⚠️ Known Pending

| Item | Priority | Notes |
|---|---|---|
| Server restart required | HIGH | Running `npm start` with old build. `Ctrl+C` then `npm start` to apply Kairos rename + all July 2026 changes |
| TraderAgent → Robinhood place_equity_order wire | HIGH | Approve button in SmartMoney updates DB status but does NOT call `place_equity_order` on account 605420650 |
| InfoTooltip wiring across dashboard pages | MED | `components/dashboard/InfoTooltip.tsx` exists but not wired to individual cards |
| Schedule: Task Scheduler tasks need creating | MED | Windows Task Scheduler tasks documented in `scripts/run-agents.ps1` but not yet created as scheduled tasks |

---

## ✅ Session 2026-07-01 (continued)

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Apply migration 035 — evidence_records | Vaibhav + Claude | 2026-07-01 | Applied. evidence_records (immutable), corporate_actions, macro_signals (vintage_at, indicators_json) |
| Apply migration 036 — strategy_registry | Vaibhav + Claude | 2026-07-01 | Applied. strategy_versions, experiment_runs, agent_signals←version_id, paper_performance←spy_nav/alpha; seeds v1.0.0 champion |
| Apply migration 037 — trader_proposals | Vaibhav + Claude | 2026-07-01 | Applied. trade_proposals (HARDCODED 605420660), decision_journal |
| SEC EDGAR Form 4 insider trades | Claude | 2026-07-01 | `/api/markets/edgar-insiders` — SEC.gov CIK lookup + Form 4 XML parse; writes notable buys to evidence_records; SmartMoneyPage "Form 4 (EDGAR)" tab with lazy-load table |
| knowledge/event-patterns/ population | Claude | 2026-07-01 | Created `event-patterns/macro-events.md` (Fed/CPI/NFP/ISM/auctions + agent scoring adjustments), `event-patterns/earnings-patterns.md` (PEAD/SUE/red flags/sector patterns), `signal-library/proven-signals.md` (10 signals + anti-signals + correlation matrix); KNOWLEDGE_INDEX updated |

---

## ✅ Session 2026-07-01 (latest)

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Phase 0: `lib/data/quotes.ts` — deterministic quote adapter | Claude | 2026-07-01 | AV GLOBAL_QUOTE → price_cache fallback; freshness/stale detection; `computeFillPrice` (ask + 0.05% slippage) |
| Phase 0: `lib/data/technicals.ts` — RSI/EMA from candles | Claude | 2026-07-01 | Pure-math RSI(14), EMA(20/50) from OHLCV; `scoreTechnicals()` → 0-100 |
| Phase 0: `lib/data/scores.ts` — `computeScores()` | Claude | 2026-07-01 | All 5 scores deterministic: fundamental from AV OVERVIEW, technical from candles, sentiment from social, macro from macro_signals table, insider from AV transactions |
| Phase 0: `lib/data/evidence.ts` — evidence write helpers | Claude | 2026-07-01 | `writeEvidence`, `writeBatchEvidence`; SOURCE_TIERS map; fire-and-forget inserts to evidence_records |
| Phase 0: Migration 034 — paper_order_events | Claude + Vaibhav | 2026-07-01 | **Applied.** Append-only immutable event log; UPDATE/DELETE blocked by trigger; price provenance columns on paper_trades |
| Phase 0: `processSymbol` rewrite in research-agent | Claude | 2026-07-01 | LLM no longer generates scores; fetches AV OVERVIEW + candles → computeScores() → LLM writes thesis+direction only (512 tokens, Groq) |
| Phase 0: paper-trade route price upgrade | Claude | 2026-07-01 | Replaces execClaude/MCP price fetch with `getQuote()` + `computeFillPrice()`; writes immutable fill event to paper_order_events; SPY benchmark alpha tracking; decision journal entries |
| Phase 1: `lib/data/evidence.ts` + Migration 035 — evidence_records | Claude | 2026-07-01 | Migration **NOT YET APPLIED**. evidence_records table (append-only immutable); corporate_actions table; macro_signals altered (vintage_at, indicators_json) |
| Phase 1: `/api/agents/corporate-actions` — splits/dividends sync | Claude | 2026-07-01 | AV SPLITS + DIVIDENDS for held + watchlist symbols; upserts corporate_actions; GET filters by symbol/type/since |
| Phase 2: Migration 036 — strategy_registry | Claude | 2026-07-01 | **NOT YET APPLIED.** strategy_versions table; experiment_runs table; agent_signals ← strategy_version_id; paper_performance ← spy_nav/spy_return_pct/alpha_pct; seeds v1.0.0 Phase0-Baseline as champion |
| Phase 2: `/api/agents/backtest` — JS backtest engine | Claude | 2026-07-01 | Replays agent_signals vs price_cache candles; eligibility gate (Sharpe≥0.5, win_rate≥40%, drawdown<25%, min 20 trades, expectancy>0); persists to experiment_runs; SPY alpha |
| Phase 2: `/api/strategies/versions` — champion/challenger governance | Claude | 2026-07-01 | GET: list versions + nested runs; POST: promote_champion, retire, reject, or create new version |
| Phase 4: `/api/journal` — decision journal CRUD | Claude | 2026-07-01 | GET: filter by symbol/type/resolved; POST: create entry or resolve with outcome; links signal→fill→outcome |
| Phase 5: Migration 037 — trader_proposals | Claude | 2026-07-01 | **NOT YET APPLIED.** trade_proposals table (30-min expiry); decision_journal table; account_number default 605420650 |
| Phase 5: `/api/agents/trader` — TraderAgent proposals + approval | Claude | 2026-07-01 | HARDCODED AGENTIC_ACCOUNT=605420650; builds proposals from qualifying signals; approve/reject; expiry check; account mismatch safety block; kill-switch re-check at approval |
| AgentsPage — Experiments tab (🔬) | Claude | 2026-07-01 | Strategy versions list (champion badge, state, experiment metrics); Run Backtest button → /api/agents/backtest; results grid with gate pass/fail and failure reasons |
| AgentsPage — Proposals tab (⚡) | Claude | 2026-07-01 | Pending proposals list; Approve/Reject buttons; price drift warning (>3%); expiry countdown; risk check badges; "Generate Proposals" trigger |

## ✅ Session 2026-06-30

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Migration 032 — asset_class on agent_signals | Claude + Vaibhav | 2026-06-30 | `asset_class` column; seeds ETF/metal symbols; index |
| Migration 033 — learner controls | Claude + Vaibhav | 2026-06-30 | `learning_priors` (15 Bayesian priors), `learner_config` (per-dimension toggles), `signal_weights_history` (rollback snapshots); `mutations_paused`/`pause_reason` on `learner_runs` |
| LearnerAgent full rewrite — true DeepSeek tool-use agent | Claude | 2026-06-30 | 9 tools: read_priors, query_learner_config, query_signals_with_outcomes, query_score_correlation, query_macro_context, read_past_learnings, write_hypothesis, update_signal_weight, finish; auto-guard blocks mutation if last 3 runs win_rate < 35%; phase gate: requires 10+ closed trades |
| Learner Controls tab + Weight History tab in AgentsPage | Claude | 2026-06-30 | Per-dimension learn_from/allow_mutation toggles + min_confidence; weight history table with rollback + factory reset |
| `/api/agents/learner-controls` route (GET/PATCH/POST) | Claude | 2026-06-30 | GET: config + last 30 snapshots; PATCH: update dimension config; POST: rollback or factory_reset |
| Metals basket cap fix in research-agent | Claude | 2026-06-30 | Metals (GLD/SLV/GDX/IAU) were cut by 10-symbol cap; fixed to append unconditionally after cap |

---

## ✅ Session 2026-06-29

| Task | Agent | Completed | Notes |
|---|---|---|---|
| Dynamic exit price management | Claude | 2026-06-29 | migration 026; `price_target`, `stop_loss`, `highest_price`, `target_updated_at`, `exit_reason` on `paper_positions`; trailing stop = max(original_stop, highest_price × 0.93) |
| Risk profile system | Claude | 2026-06-29 | migration 027; `risk_profile`, `score_threshold`, `position_size_pct`, `stop_loss_pct`, `target_pct` on `strategy_config`; Conservative/Balanced/Aggressive presets; `/api/settings/risk-profile` GET/PATCH; Settings → Agents tab card |
| Visual Agent Mermaid diagrams | Claude | 2026-06-29 | `components/dashboard/AgentDiagram.tsx`; clickable flowcharts per agent; color-coded by node status; click → detail drawer; 7 JSON files in `public/agent-diagrams/` |
| TradingView CSV watchlist import | Claude | 2026-06-29 | Import CSV button + modal in WatchlistPanel; `EXCHANGE:TICKER,Description` format; batch POST with progress |
| Signal backtest tab | Claude | 2026-06-29 | AgentsPage backtest tab; joins agent_signals to paper_trades by symbol+date ±3 days; shows Hit Rate, Misses, Open, Avg Return |
| Position Monitor | Claude | 2026-06-29 | `/api/agents/position-monitor/route.ts`; TradingPage card + Run button; scheduled weekdays 4:15PM |
| Insider transactions in ResearchAgent | Claude | 2026-06-29 | `scoreInsider()` in `lib/research-agent.ts`; Alpha Vantage INSIDER_TRANSACTIONS; 90-day buy/sell ratio; injected as pre-fetched context in LLM prompt |
| Smart Money Trades in MarketsPage | Claude | 2026-06-29 | `/api/markets/insider-trades/route.ts`; Alpha Vantage insiders + House Stock Watcher congressional trades; Insiders/Congress tabs |
| LLM Cost Monitor | Claude | 2026-06-29 | `/api/admin/llm-costs/route.ts`; queries `llm_call_log`; burn rate, projected daily, per-model breakdown, 24-bar hourly chart; Settings → Agents tab; DashboardHome banner if projected daily > $2 |
| Mentor nav + judgment score chart | Claude | 2026-06-29 | Mentor link restored in DashboardShell sidebar (🎓 under Learn); `/api/mentor/scores/route.ts`; MentorPage Recharts LineChart; reference lines at 50/70/90 |
| MacroSentinel recession agent | Claude | 2026-06-29 | `/api/agents/macro-sentinel/route.ts`; 8 Alpha Vantage indicators; weighted danger score 0-100; GREEN/YELLOW/ORANGE/RED regimes; advisory-only; migrations 028; MarketsPage gauge + signal table; DashboardHome colored banner; Mondays 8AM |
| Mermaid build fix | Claude | 2026-06-29 | Downgraded mermaid v11→v10; fixes ESM/webpack incompatibility with es-toolkit; build passes |
| Windows Task Scheduler — 7 tasks | Claude | 2026-06-29 | ResearchAgent weekdays 9AM, PaperTrader 9:30AM, PositionMonitor 4:15PM, LearnerAgent Mondays 6AM, ThemeScout Sundays 8PM, DeepSeekAgent weekdays 9AM, MacroSentinel Mondays 8AM |

---

## 🟣 Review

| Task | Agent/Model | Status | Date | Notes |
|---|---|---|---|---|
| Governed agentic quant platform architecture | Codex (Architect) | review | 2026-06-27 | Canonical spec written; awaiting Vaibhav review before implementation planning |

---

## 🟡 Planned (Approved, Ready to Build)

| Task | Assigned To | Status | Date | Notes |
|---|---|---|---|---|
| Signal backtest validation | Builder | planned | — | Run `/api/agents/backtest` after signals accumulate; verify gate metrics |
| knowledge/event-patterns/ population | Builder | planned | — | Fed decision patterns, earnings behavior feeds LearnerAgent |
| Mobile push notifications for proposals | Builder | planned | — | Phase 5; low priority until proposals flow is exercised |

---

## 🔵 Blocked

| Task | Blocked By | Notes |
|---|---|---|
| LearnerAgent weight mutation (Phase 1 gate) | 10+ closed paper trades | Auto-guard + phase gate built; waiting on paper trading data |
| Experiments tab metrics | Migration 036 not applied | spy_nav/spy_return_pct/alpha_pct columns missing until 036 applied |
| Proposals tab | Migration 037 not applied | trade_proposals + decision_journal tables missing until 037 applied |
| TraderAgent actual Robinhood order | Proposals tab approved proposal | Route is built; Robinhood MCP call must be triggered from UI after user approves |

---

## ✅ Completed

| Task | Agent | Completed | Notes |
|---|---|---|---|
| PRD.md written | Claude | 2026-06-01 | Full spec in PRD.md |
| AGENTS.md written | Claude | 2026-06-01 | Multi-agent coordination layer |
| Knowledge base created | Claude | 2026-06-01 | 6 files, see knowledge/ |
| Robinhood MCP connected | Vaibhav + Claude | 2026-06-01 | 6 accounts found, agentic: ••••0660 |
| Robinhood accounts read | Claude | 2026-06-01 | get_accounts called successfully |
| Design system migration | Claude | 2026-06-27 | fo-* tokens, shadcn components, DashboardShell rewrite |
| Governed agentic quant platform architecture | Codex | 2026-06-27 | Canonical spec; see PROJECT_DECISIONS.md |
| LLM Router (Claude/DeepSeek/Groq routing by task) | Claude | 2026-06-29 | lib/llm-router.ts; routes by task type, cost tier |
| Groq Llama 3.3 70B free tier integration | Claude | 2026-06-29 | Added to router after DeepSeek; free-tier slot |
| DeepSeek R1 integration | Claude | 2026-06-29 | Cheaper chat/summarize tasks; API key in vault |
| Multi-paper-account comparison (agent_label system) | Claude | 2026-06-29 | Tracks which LLM produced each signal; P&L by model |
| LLM call log + admin history page | Claude | 2026-06-29 | Cost + token tracking per model; /dashboard/activity |
| API key vault (PIN protected) | Claude | 2026-06-29 | /dashboard/settings; encrypted key storage |
| Earnings calendar → Massive API (no LLM) | Claude | 2026-06-29 | Direct API, no LLM cost on calendar fetch |
| Markets overview → Massive API (live index quotes) | Claude | 2026-06-29 | Direct API, real-time quotes |
| Features page | Claude | 2026-06-29 | /dashboard/features; timeline of all shipped features |
| Social sentiment (StockTwits + Alpha Vantage) | Claude | 2026-06-29 | Sentiment signals for ResearchAgent screener |
| Strategy classification engine (7 strategies) | Claude | 2026-06-29 | Momentum, GARP, Value, Dividend, Growth, Macro, Contrarian |
| DeepSeek research agent variant | Claude | 2026-06-29 | Parallel agent; produces signals for LLM P&L comparison |
| Symbol detail page (candlestick, agent signals, chat) | Claude | 2026-06-29 | /dashboard/symbol/[ticker]; EMA/SMA/volume overlays |
| Symbol navigation from Portfolio, Markets, Activity | Claude | 2026-06-29 | Clickable tickers link to symbol detail page |
| Sector line chart (multi-sector performance) | Claude | 2026-06-29 | SectorLineChart component; multiple sectors on one chart |
| DB migration 010 (llm_call_log, agent_labels) | Claude + Vaibhav | 2026-06-29 | Applied to production Supabase |
| DB migration 011 | Claude + Vaibhav | 2026-06-29 | Applied to production Supabase |
| price_cache table (on-demand candle caching) | Claude | 2026-06-29 | Caches Massive API candle responses |
| Bell dropdown fix (position: fixed) | Claude | 2026-06-29 | Sticky header stacking context — fixed to viewport |
| Watchlist toggles (migration 020 + PATCH API) | Claude | 2026-06-29 | research_enabled, alert_on_signal, alert_on_earnings per symbol |
| WatchlistPanel rewrite (quotes, source badges, settings) | Claude | 2026-06-29 | Price quotes via /api/markets/quote; per-item expand |
| BriefingPage symbol auto-suggest | Claude | 2026-06-29 | Regex extraction of tickers from brief text; watchlist + Watch chips |
| pgvector enabled + agent_memory table (migration 021) | Claude + Vaibhav | 2026-06-29 | Applied to production; vector(1536) + ivfflat index |
| PageHeader on Strategies + Settings pages | Claude | 2026-06-29 | Consistent what/why/look-for on all dashboard pages |
| Vault Change PIN button + API handler | Claude | 2026-06-29 | change_pin action updates app_settings.vault_pin; persists across restarts |
| Vault Edit key button + preserve existing value | Claude | 2026-06-29 | Edit pre-fills form; blank key_value = keep existing; API skips update if blank |
| PageHeader on Activity, Learning, You, Trading, Agents, Mentor | Claude | 2026-06-29 | All 8 dashboard pages now have consistent what/why/look-for header |
| Options chain tab in SymbolDetailPage | Claude | 2026-06-29 | Chart/Signals/Options/Chat tabs; put-call ratio, avg IV, calls+puts table |
| Options chain API route | Claude | 2026-06-29 | /api/options/chain?symbol=X; Alpha Vantage REALTIME_OPTIONS; revalidate 300s |
| Batch quotes endpoint | Claude | 2026-06-29 | /api/markets/quotes?symbols=A,B,C; parallel fetch, max 20 symbols, 1 round-trip |
| WatchlistPanel → batch quotes | Claude | 2026-06-29 | 15–20 serial calls replaced with 1 batch call; limit raised 15→20 |
| Company name auto-fetch on watchlist add | Claude | 2026-06-29 | POST /api/watchlist calls Alpha Vantage COMPANY_OVERVIEW if name not provided |
| Alpha Vantage API key added | Vaibhav + Claude | 2026-06-29 | In .env.local + vault DB; powers options, sentiment, company name |
| Vault providers expanded | Claude | 2026-06-29 | Added massive, alphavantage, robinhood, supabase providers + market_data/sentiment/options tasks |
| TypeScript error fixes | Claude | 2026-06-29 | theme-scout raw.text fix; tailwind darkMode array→string; supabase/functions excluded from tsconfig |
| Grammarly hydration error suppressed | Claude | 2026-06-29 | suppressHydrationWarning on <body> in app/layout.tsx |

### Design System Migration (2026-06-27) — What was built
- `app/globals.css` — full fo-* token system (trading-purple accent #6366F1, dark-first)
- `tailwind.config.ts` — fo-* color tokens wired to CSS vars
- `postcss.config.mjs` — Tailwind v4 postcss plugin
- `lib/utils.ts` — cn() utility (clsx + tailwind-merge)
- `components/ui/card.tsx` — shadcn Card/CardHeader/CardContent/CardFooter/CardTitle/CardDescription
- `components/ui/button.tsx` — shadcn Button (no Radix Slot dep, simplified)
- `components/ui/badge.tsx` — shadcn Badge with CVA
- `components/ui/sparkline.tsx` — Recharts sparkline, fo-green/fo-red auto-color
- `components/ui/pct-pill.tsx` — % change pill with ArrowUp/ArrowDown, fo-* colors
- `components/dashboard/DashboardShell.tsx` — rewritten, added /agents and /trading nav items
- Added deps: clsx, tailwind-merge, class-variance-authority

---

## 📋 Backlog (Not Yet Approved)

| Task | Type | Priority | Notes |
|---|---|---|---|
| TraderAgent — approval_required mode (real Robinhood orders) | Feature | High | After signal validation; account ••••0660 only |
| LearnerAgent — weekly weight mutation | Feature | Medium | After 10+ closed trades (Phase 0 gate) |
| strategy_config UI editor | Feature | Low | Phase 1 last |
| Mobile trade approval push notifications | Feature | Low | Phase 5 |
| knowledge/event-patterns/ population | Research | High | Fed decisions, earnings patterns, macro |
| SEC EDGAR Form 4 insider trades | Integration | Medium | Free API |
| Railway cron deployment | Infrastructure | Medium | Phase 5 alternative to pg_cron |

---

## Architecture Decisions Log (Quick Ref)

Full details in `PROJECT_DECISIONS.md`. Summary here:

| Decision | Chosen | Alternative | Reason |
|---|---|---|---|
| Styling | fo-* tokens + shadcn | Tailwind only | Design system consistency |
| DB | Supabase | Prisma/PlanetScale | Already active |
| Primary AI | Anthropic Claude | OpenAI | Already integrated, best reasoning |
| LLM routing | Claude → DeepSeek → Groq by task | Single model | Cost optimization + comparison |
| Trading | Robinhood MCP | Alpaca, IBKR | Robinhood native agentic support |
| Price data | Massive API (Polygon.io) | Yahoo Finance | Reliability + options data |
| Screener primary | FinancialDatasets screen_stocks | Robinhood run_scan | Deeper fundamentals |
| Vector store | pgvector (Supabase) | Pinecone | No new infra needed |
| Agent mode default | approval_required | auto | Safety first |
| Screener candidates/day | 3 max | 5 | 10% position sizing → max 10 positions |
| Regime detection | None (scoring adapts naturally) | Explicit bull/bear mode | Fragile; scoring handles it |
| Learning schedule | Weekly batch | Per-trade | Need 10+ trades; per-trade is noise |

---

## How to Add a Task (Any Agent)

1. Add to "Planned" or "Backlog" section
2. Tag with your model name in "Assigned To"
3. Move to "In Progress" when you start
4. Move to "Completed" when done, note what you built

---

*Last updated: 2026-06-29 by Claude (Sonnet 4.6)*
| Webull Trading API live transport restoration | Codex / GPT-5 | completed | 2026-07-19 | Verified official HMAC/token/gate contracts; fixed missing PENDING type and the handoff's wrong token endpoint/vault shape. Restored the sole signed fetch path with vault app key/secret/token, exact host signing, DB-backed read-only preflight, one-shot nine-gate production order permits, timeout/redacted errors, and sandbox-account isolation. Adapter/flag/migration remain disabled; no Webull call or order made. Gates: 107 focused + 1,114 full tests pass, tsc and production build clean. |
| Market-holiday research catch-up | Codex / GPT-5 | completed | 2026-07-19 | Extended catch-up to verified full NYSE/NSE equity-market holidays using one market-local calendar. Daily per-market jobs self-skip trading days, special sessions, and unsupported years; unsupported years raise a deduplicated System Health warning. Corrected the prior inaccurate India dates using NSE CMTR71775 + CMTR72260. Vercel was Ready before prod migration; new daily jobs verified active and old weekend jobs absent. Gates: 1,106 tests pass/6 skip, tsc clean, Next build artifact clean, GitGuardian clean. |
| Hybrid-stop and Webull Trading API architecture audit + Git routing repair | Codex / GPT-5 | completed | 2026-07-17 | Corrected Webull's documented 15-day token inactivity rule, replaced unsafe flat broker flags with order/TIF/session/account capability matrices, kept disaster-floor losses in P&L/risk truth while excluding only signal attribution, made unprotected holdings latch a scoped entry breaker without blocking exits/protection repair, and added corporate-action/expiry/replay safeguards. Verified MCP remains order-inert, 7 targeted tests and typecheck passed. Installed directory-scoped GCM routing: FinanceOS -> vaibhavtiwarigithub, FinNudge -> sharveeshrotriya; remotes unchanged and no FinNudge project file touched. |
| Adversarial audit and remediation of 2026-07-12 Claude changes | Codex / GPT-5 | completed | 2026-07-12 | Fixed provider/tool-loop fallback routing, false-success monitor reads, research run leakage, India/US Smart Money contamination, owner-gate gaps, migration-167 settings degradation, benchmark rebase mismatch, score redirect intent, and zero-NAV display. 305 tests, typecheck, build, and npm audit green; no orders or external mutations. |
| Cross-market Trading Mandates architecture and implementation | Codex / GPT-5 | completed | 2026-07-12 | Per-market horizon, strategy preference, user/champion governance, entry-time provenance, market-weekday exits, Settings, ResearchAgent, PaperTrader, PositionMonitor, LearnerAgent, and US proposal integration complete. Migration 168 applied directly to verified FinanceOS project dionkikgdmlaotvtbnfr and schema/RLS/seeds verified. 311 tests, typecheck, build, audit green; no orders or live enablement. Completion record: `features/trading-mandate/IMPLEMENTATION_RESULT.md`. |

| Downside hedging: deterministic inverse-ETF hedge sleeve | Codex / GPT-5 | completed | 2026-07-15 | Implemented the approved US paper-only hedge overlay, shipped OFF with execution independently OFF. Added deterministic regime/market confirmation, guarded state-machine RPCs, SH/PSQ allowlist, generic-agent exclusion, PositionMonitor accounting reconciliation, Settings controls, RLS/audit ledger, cron, tests, and architecture docs. FinanceOS migration applied and verified with no hedge trades; 398 tests, typecheck, production build, and desktop/mobile checks passed. |
| Relationship-graph architecture adversarial review and correction | Codex / GPT-5 | completed | 2026-07-15 | Replaced V3 with primary-source-grounded V4. Narrowed the first experiment to US-only sales-weighted disclosed customer momentum in the existing measure-only Edge lab; separated issuers/instruments, neutral relationships/events, and economic/knowledge time; reused Router/evidence/edge/learning governance; removed peer seeding, guaranteed-free-data claims, premature embedding adoption, direct candidate/suppression/hedge effects, and weak sample gates. Design only; no migration, provider activation, scoring, candidate, or order change. |
| Four-design adversarial review: anomalies, India Markets, external shadow, Router cutover | Codex / GPT-5 | completed | 2026-07-15 | Reviewed and replaced all four architecture drafts against current code/schema and primary references. Corrected PEAD data/look-ahead assumptions, India client-side Yahoo and false-breadth design, external-repo sandbox/supply-chain/provenance boundaries, and Router activation/runtime eligibility guards. Design only; no implementation, migrations, provider changes, or money-path activation. |
| 2026-07-15 implementation batch adversarial audit | Codex / GPT-5 | completed | 2026-07-15 | Fixed earnings look-ahead and false EPS-basis eligibility, added database-enforced PIT immutability and the missing daily capture cron, removed India page-load provider fan-out, bounded/deduplicated background fills, fixed breadth overcounting/stale UI handling/persistence errors, removed cross-market display-weight fallbacks, corrected profile market inference, and moved the colliding India retry cron. Production migration applied and RLS/policies/triggers/crons verified; typecheck, build, 430 tests, and client-bundle provider-host scan passed. |

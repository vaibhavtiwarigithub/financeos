.env.local
.env
node_modules/
.next/
*.log
tsconfig.tsbuildinfo
.DS_Store
.mcp.json
scripts/robin.env
scripts/logs/

# DB backups — real user data, never committed
backups/

# next-pwa generated service worker artifacts (built into public/ on prod build)
public/sw.js
public/sw.js.map
public/workbox-*.js
public/workbox-*.js.map
public/fallback-*.js

# Claude Code preview config
.claude/launch.json

.vercel
.env*

# graphify scan artifacts — embed source content, never commit
graphify-out/
.graphify_python
.graphify_detect.json
.graphify_*

.vercel
.env*

# Supabase CLI local link state — machine-specific, never commit
supabase/.temp/

# Playwright MCP scratch snapshots
.playwright-mcp/

# Codex/Claude review + audit prompt+result docs — local scratch, never commit
CODEX_*.md
CLAUDE_CODE_*.md

# Added by ggshield
.cache_ggshield

# temporary OOS sigma measurement harness + its output
lib/edges/__oos-measure.test.ts
oos-sigma-*.json

.env*
